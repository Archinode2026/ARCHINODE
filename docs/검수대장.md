# ARCHINODE 검수 대장

발견한 결함·개선점을 **전부 여기에 쌓는다.** 보고서(`archi-diary/bronze-*.md`)는 그날의 기록이고, "지금 무엇이 열려 있나"는 이 대장이 정본이다.
**2026-09-12부터 새 등록은 어드민 > 「검수관리」 탭(`reviewIssues`)에 한다** (한울님 "A절로 옮겨서 빨리 만들어"). 이 파일의 A-001~A-010은 탭의 [문서 대장 10건 가져오기]로 이관하고, 그 뒤 이 파일은 이관 전 기록으로만 남긴다. 탭이 라이브에 나가고 규칙이 게시되기 전까지는 임시로 여기에 계속 적는다.

## 규칙 (단번 검수판 · 엑사 검수관리 게시판을 문서로 축약)

- **등록은 누구나(브론즈·블랙·지휘 창·한울님), 판정은 사람만.** 에이전트가 올린 건은 `검토대기`로 들어가고, 한울님(또는 한울님이 위임한 지휘 창)이 「진짜 문제」로 올려야 수정 대상이 된다. 아무리 명백해 보여도 이 단계를 건너뛰지 않는다.
- **상태 순환**: `검토대기` → `진행중` / `보류` / `오탐반려` → (수정 뒤) `확인대기` → `확인완료` / `재수정요청`. 상태가 바뀌면 **이력 칸에 한 줄** 추가(날짜·누가·무엇).
- **네 칸(눌렀나·기대·실제·문제)은 비개발자가 읽는 글이다.** 파일 이름·줄 번호·콘솔 원문·함수명은 **개발 메모** 칸에만.
- 우선순위: **High**(방문자·브랜드가 겪는다) / **Medium**(운영이 겪는다, 또는 보안) / **Low**(콘솔·문구·정리).
- 번호는 `A-###`, 재사용 금지. 중복 발견이면 새 번호를 만들지 말고 기존 건 이력에 "재발견"을 적는다.
- 브론즈 보고 형식의 「검수대장 등록: 번호 목록」이 이 파일과 짝이다.

## 대장

| 번호 | 등록일 | 화면 | 무엇을 눌렀나 | 기대 | 실제 | 문제 | 우선 | 상태 | 개발 메모 | 이력 |
|---|---|---|---|---|---|---|---|---|---|---|
| A-001 | 2026-08-14 | 홈 화면, 트렌드 리포트, 카테고리 전체 | 홈에 들어갔다 | 로그인한 브랜드라면 상단에 "대시보드" 단추가 보이고, 트렌드 리포트에는 최근 리포트 목록이 뜬다 | 화면은 뜨지만 로그인 상태가 상단에 반영되지 않고, 트렌드 리포트 목록은 빈 상태 안내로 가려진다 | 홈에 올 때마다 뒤에서 오류가 두 번 나고, 브랜드가 리포트를 발행해도 목록에 안 뜬다. 5월 8일부터 잠복 | High | 확인대기 | `firebase-config.js:28` `const auth = (typeof firebase.auth === 'function') ? firebase.auth() : null;`(storage와 동일 패턴)로 수정. 연쇄로 `auth-ui.js:16` 가드도 `typeof auth === 'undefined' \|\| !auth`로 함께 수정(auth가 null이면 기존 `typeof undefined` 가드를 통과해 `auth.onAuthStateChanged`에서 새로 throw하는 걸 로컬 실측으로 발견). node-black CRITICAL 0. 커밋 `bc430e0`. 발견된 무관 기존 결함 2건은 별도 등록(카테고리 서브페이지 `header` 중복 선언 SyntaxError, products 쿼리 복합색인 누락) | 08-14 브론즈 등록 · 08-14 한울님 승인 대기(A안/B안) · 09-12 대장 이관 · 09-14 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 |
| A-002 | 2026-08-14 | 브랜드 온보딩 가이드 | 페이지를 열었다 | 조용히 뜬다 | 뜨긴 하지만 뒤에서 오류가 난다 | 파이어베이스를 안 쓰는 안내 페이지가 파이어베이스 설정을 불러온다. 기능 영향 없음 | Low | 검토대기 | `brand-guide.html:324` `firebase-config.js` 로드 제거. A-001과 함께 처리 권장 | 08-14 브론즈 · 09-12 이관 |
| A-003 | 2026-04-22 | 브랜드 상세 페이지 | 브랜드 소개를 본다 | 브랜드가 적은 글이 글자 그대로 보인다 | 브랜드가 소개란에 HTML을 넣으면 그대로 실행될 수 있다 | 입점 신청 쪽은 막았는데 보여주는 쪽이 아직 열려 있다. 엑사에서는 한 곳에서 시작한 같은 문제가 60곳으로 번졌다 | Medium | 검토대기 | `brands/view.html` innerHTML → textContent 또는 escapeHtml. 미완료업무 #2. 다음 단계 "XSS 출력 측 전수"의 첫 항목 | 04-22 화이트 · 09-12 이관 |
| A-004 | 2026-07-03 | 뉴스레터·입점 신청·자문 요청 폼 | (봇이) 폼을 반복 제출한다 | 막힌다 | 5초 간격만 막고, 그 이상은 무제한 | 봇 하나가 하루면 파이어베이스 무료 한도를 소진시켜 사이트 저장 기능이 멈출 수 있다 | Medium | 보류 | reCAPTCHA v3 무료 도입, 폼 3곳. `pre-launch-audit-2026-07-03-v3.md` P3-신규-1. 다음 단계(제품 기능)로 분류됨 | 07-03 블랙 · 09-12 이관·보류(다음 단계) |
| A-005 | 2026-07-03 | 브랜드 상세 페이지 | 그 브랜드가 쓴 매거진 글을 찾는다 | 브랜드 페이지에 그 브랜드의 글이 보인다 | 제품만 보이고 글은 안 보인다 | 브랜드가 글을 써도 자기 페이지에서 안 보인다 | Low | 보류 | `brands/view.html`에 articles(brandId, approved) 조회 추가, 15~30분. 다음 단계 | 07-03 블랙 · 09-12 이관·보류(다음 단계) |
| A-006 | 2026-07-02 | 검색·SNS 미리보기 | 링크를 카카오톡·슬랙에 붙였다 | 대표 이미지가 뜬다 | 37개 페이지는 벡터 이미지라 미리보기에 안 뜰 수 있다 | 공유했을 때 이미지 없는 카드가 나온다 | Low | 검토대기 | og:image가 SVG인 37페이지 → PNG로. `handoff-2026-07-02-final.md` 부록 B | 07-02 · 09-12 이관 |
| A-007 | 2026-08-14 | 입점 신청(휴대폰 390px) | 단추를 누른다 | 손가락으로 누르기 편하다 | 단추 5개 중 1개가 작다 | 경미 | Low | 검토대기 | 터치 타깃 44px 미만 1개. `bronze-2026-08-14.md` 5절 3항 | 08-14 브론즈 · 09-12 이관 |
| A-008 | 2026-08-14 | 입점 신청 | 언어 단추를 본다 | 영어 전용 페이지라 언어 단추가 없다 | "EN ▾" 드롭다운이 남아 있다 | 결함 아님(의도된 영어 단일 페이지). 숨기면 깔끔 | Low | 보류 | `list-your-brand.html` `data-langs="en"`, 미사용 `data-ko` 72개. brand-guide와 같은 방향으로 스위처 숨김 | 08-14 브론즈 관찰 · 09-12 이관 |
| A-009 | 2026-09-12 | 어드민 대시보드 | 브랜드를 정지했다가 복구한다 | 언제 누가 왜 정지·복구했는지 다 남는다 | 마지막 사유 하나만 남고 이전 것은 덮인다 | 분쟁 시 이력을 못 본다 | Low | 확인대기 | brands 문서에 `history[]` 배열(단번 검수판 패턴). 2단계 구현: `admin/dashboard.html` `updateBrand()` 의 update 에 `history: arrayUnion({status,by,at,note})` 동봉 + `adminLogs` 장부(`logAdmin`) + 브랜드 상세 모달 「이력」 절. 규칙 게시(`adminLogs`) 뒤 확인 | 09-12 지휘 창 · 보류(다음 단계) · 09-12 2단계 구현 — 브론즈 확인 대기 |
| A-010 | 2026-09-12 | 폼 4곳(입점·자문·원고·뉴스레터) | 인터넷이 끊긴 채 제출한다 | "연결이 느립니다" 같은 안내가 뜬다 | "접수 중"에서 멈춘 채 아무 안내가 없다 | 고객이 멈춘 건지 되는 중인지 모른다. 탭을 닫으면 접수가 사라진다 | Medium | 보류 | Firestore 오프라인 쓰기는 대기 상태라 실패 갈래가 안 온다. 엑사 9/8 "조용한 실패" 수정 패턴(타임아웃 안내 + 롤백). 다음 단계. 실측 안 함(엑사·단번 사례로 추정) | 09-12 지휘 창 · 보류(다음 단계) |
| A-011 | 2026-09-12 | 어드민 전체 (검수관리·브랜드 상세) | 등록 칸에 큰따옴표가 든 글을 저장하고 목록에서 본다 | 글자 그대로 보인다 | 큰따옴표 뒤 글이 잘리거나 엉뚱한 속성으로 붙는다 | 어드민 본인 화면에서만 나는 문제라 방문자 위험은 없지만, 브론즈·블랙이 붙여넣는 글에 큰따옴표가 흔해 실제로 겪을 가능성이 높다 | Medium | 검토대기 | `admin/dashboard.html:615` `escapeHtml`이 `"`·`'`를 안 바꿈 → 속성 컨텍스트(`title=` 824, `href=` 926·575, `data-ko` 736)에서 속성 주입. 속성용 이스케이프(`&quot;` `&#39;`) 추가 여부는 화이트·골드 판단. 노드-블랙 492fc12 검수 WARNING | 09-12 블랙 등록(탭 로그인 불가로 대장에) |
| A-012 | 2026-09-12 | 사이트 전체 (숨겨진 폴더) | 주소창에 `archinodekr.com/sales/cold-email-templates.md` 를 친다 | 없는 페이지라고 나온다 | 실비아 콜드메일 문안과 브랜드 후보 명단(엑셀)이 그대로 열린다 | 후보 브랜드 담당자 이름·이메일이 누구에게나 공개돼 있다. 개인정보·영업 자료 노출 | High | 검토대기 | GitHub Pages가 리포 전체를 서빙 — `docs/`·`sales/`·`silvia/`가 HTTP 200(노드-골드 실측). 해결: `sales/`·`silvia/`를 저장소에서 빼고(`git rm --cached`, 로컬 보존) `.gitignore` 추가, 또는 7a 단계에서 Firestore `prospects`로 이관. `docs/`는 규정집 뷰어가 fetch하므로 공개 유지 여부 별도 판단. **되돌리기 어렵지 않으나 "무엇을 공개할지"는 한울님 결정** | 09-12 골드 발견·지휘 창 등록 |
| A-013 | 2026-09-12 | 매거진 기사 | 매거진 목록에서 기사 카드를 눌렀다 | 기사 본문이 뜬다 | 제목만 탭에 뜨고 화면이 전부 흰색이다 | **116편 중 41편이 백지**(브론즈 42 중 oak-flooring 1건은 `</style>` 중복만, 렌더 정상). 오늘 자동 발행 3편도 포함이라 앞으로 나오는 기사도 계속 백지가 된다 | **High** | 확인완료 | `magazine/*.html` 41건(블랙 재계수 확정): 16행(tailored만 34행) `<style>` 안 `.article-hero{…url('../assets/magazine/placeholders/inte` 에서 잘린 뒤 바로 공지 바 마크업. `</style>`·`</head>`·`<body>` 없음. 로컬=라이브. 최초 e985d7c(05-06), 최근 1b4f583(09-12). 정상본 `agm-thermroof-…` 38~40행. **자동 발행 파이프라인(Cowork 계정 스킬 mag-*)의 템플릿도 같이 고쳐야 재발 안 함** — 리포 밖이라 한울님. 목록 `archi-diary/bronze-2026-09-12.md` 5-2. 공개 페이지 → 수정 커밋 후 「배포할까요?」 | 09-12 브론즈 R5 등록 · 09-12 화이트 재계수 41건(42번째 oak-flooring은 `</style>` 중복만, 렌더 정상) → 복구 커밋 563f415(41 기사 + `tools/fix-magazine-blank.js`) — 브론즈 라이브 재확인 대기, 파이프라인 템플릿은 한울님 · 09-12 브론즈 R7 라이브 재확인(라이브=17c8000 대조 ✓, 41/41 `</style>`·`</head>`·`<body` 각 1개·md5 로컬=라이브, 브라우저 8건 히어로·제목·본문 렌더 정상·innerText 2,307~3,240자·콘솔 오류 0·깨진 이미지 0) → **확인완료**. 「닫힌 건」 절 이동은 지휘 창. 파이프라인 템플릿(리포 밖) 재발 여부는 다음 자동 발행분에서 다시 확인 |
| A-014 | 2026-09-12 | 사이트 전체(휴대폰 375px) | 휴대폰으로 홈·브랜드·매거진을 열었다 | 상단 메뉴가 한 줄에 정돈돼 보인다 | "브랜드 로그인"·"건축을 잇다" 글자가 세로로 쌓여 안내 띠를 덮고, 브랜드·매거진·자문은 가로 스크롤이 생긴다 | 휴대폰 방문자 첫인상이 깨진다. A-001을 고치면 홈에서도 같이 나타난다 | **High** | 확인대기 | `.header-actions` 인라인 `<a href="brand-portal/login.html">`·`.logo-sub`에 `white-space:nowrap` 없음, `style.css:247` 모바일 쿼리가 `.main-nav`만 숨김. auth-ui.js Log In 삽입 시 451px. 실측 375: scrollWidth 451(brands·request-consultation·magazine). 768↑ 정상. 공개 페이지(style.css). **구현(09-14 화이트)**: `style.css`·`index.html`(자체 인라인 스타일이 같은 선택자를 재정의해 같이 수정) 모바일 미디어쿼리에 `.header .container`/`.logo-sub`/`.header-actions`/`.header-actions a,button` nowrap+flex-wrap, `.main-nav.active` top을 `var(--header-height)`→`100%`. 375px 실측(로컬): index·brands·request-consultation·magazine 4페이지 전부 scrollWidth 375(가로 스크롤 없음), 햄버거 드롭다운 위치 정상. 커밋 `95f9e3b`. node-black CRITICAL 0(492fc12 계열 별도, 이번 검수 세션) | 09-12 브론즈 R6 · 09-14 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 |
| A-015 | 2026-09-12 | 입점 신청(휴대폰) | 맨 아래로 내렸다 | 폭에 맞는 하단 정보 | 하단 네 칸이 옆으로 늘어나 가로로 밀린다 | 휴대폰에서 좌우로 흔들림 | Medium | 검토대기 | `list-your-brand.html:465` 인라인 `grid-template-columns:2fr 1fr 1fr 1fr` 모바일 규칙 없음. 실측 375 scrollWidth 483 | 09-12 브론즈 R6 |
| A-016 | 2026-09-12 | 세부 카테고리 69페이지 | 세부 카테고리(예: 소파)를 열었다 | 조용히 뜬다 | 뒤에서 오류가 난다 | 같은 코드가 두 번 들어가 뒤쪽 블록이 통째로 안 돈다(앞 블록이 대신) | Medium | 검토대기 | `categories/*/*.html` 69건: 245행 `const header` + 283행 `var header` → `SyntaxError: Identifier 'header' has already been declared`. 두 번째 `<script>`(null 가드판) 미실행. 하나로 통일 | 09-12 브론즈 R5 |
| A-017 | 2026-09-12 | 카테고리 페이지 외 19페이지 | 한국어로 보다가 열었다 | 맨 위 안내 띠도 한국어 | 띠만 영어("2026 Brand Listing — Now Free…") | 한 화면에 두 언어 | Low | 검토대기 | 공지 바 `data-ko` 없이 하드코딩 19페이지(`categories/*.html` 10·brands/view·404·about·brand-guide·for-brands·privacy·privacy-en·submit-trend·terms). index.html 776행 양언어 마크업으로 통일 | 09-12 브론즈 R5 |
| A-018 | 2026-09-12 | 카테고리 페이지 메뉴 18곳 | 한국어로 바꾸고 카테고리 메뉴를 열었다 | "가구", "욕실" 한 번씩 | "가구가구", "욕실욕실" | 메뉴가 어색하다 | Low | 검토대기 | `<span data-ko="가구">Furniture</span><small>가구</small>` KO 전환 시 중복. 18페이지. index.html은 span에 data-ko 없음 — 한 방식으로 통일(미완료업무 #3과 같은 계열) | 09-12 브론즈 R5 |
| A-019 | 2026-09-12 | 검색 등록(사이트맵) | 검색엔진이 사이트 지도를 읽는다 | 최신 기사까지 | 7/8 이후 기사 29편·브랜드 가이드가 없다 | 새 기사가 검색에 잘 안 잡힌다 | Low | 검토대기 | `sitemap.xml` lastmod 최신 06-29, 180건, 누락 31+. `scripts/make_sitemap.py` 재실행 필요 — 이 PC Python 없음 → Node 이식 별건 | 09-12 브론즈 R5 |
| A-020 | 2026-09-12 | 브랜드 매거진 글 페이지 | 주소로 열었다 | 하단에 회사 정보 | 저작권 한 줄만 | 사업자 정보 표시가 빠진 화면 | Low | 검토대기 | `trend-report/view.html` 자체 푸터, `.footer-business-info`·auth-ui.js 없음. 표시 의무 여부는 노드-네이비 | 09-12 브론즈 R5 |
| A-021 | 2026-09-12 | 자문 요청(휴대폰) | 동의 체크박스를 누른다 | 누르기 편하다 | 아주 작다 | A-007 같은 유형 | Low | 검토대기 | `request-consultation.html` checkbox 13×13px | 09-12 브론즈 R6 |
| A-022 | 2026-09-12 | 어드민 대시보드 상단바 | (숨겨진) 아티클 링크에 마우스를 올린다 | 색이 바뀐다 | 색이 안 바뀌고 뒤에서 오류가 난다 | 1단계에서 숨겨 지금은 안 보이지만 코드가 남아 있다 | Low | 검토대기 | `admin/dashboard.html:153` `onmouseover="this.style.color=\'#C8A96E\'"` — 속성값 안 `\'`는 JS 문법 오류(도입 9cedb1c). 다른 페이지엔 없음. `\'` → `'` | 09-12 블랙(93e00be 검수) |
| A-023 | 2026-09-12 | 어드민 대시보드 | 목록 중 하나가 못 읽히는 상태(규칙 미게시 등)에서 대시보드를 연다 | 읽힌 것은 보이고 못 읽은 것만 표시된다 | "Loading…" 화면이 영원히 남는다 | 규칙 하나가 늦게 게시되면 어드민 전체가 멈춘 것처럼 보인다 | Medium | 검토대기 | `admin/dashboard.html:322-334` `loadAll()`이 `Promise.all`이라 하나라도 reject면 `loadingScreen` 미해제·`dash_render` 미호출(`loadBrands` try/catch 없음). `Promise.allSettled` + 실패 카드 표시로. 기존 구조 | 09-12 블랙(93e00be 검수) |
| A-024 | 2026-09-12 | 매거진 기사 1편 | 기사(oak-flooring)를 연다 | 조용히 뜬다 | 뜬다(문제 없음) | 정리 대상: 닫는 태그가 두 번 들어가 있다. 지금은 무해 | Low | 검토대기 | `magazine/oak-flooring-return-home-as-comfort-2026.html` 38·39행 `</style>` 중복. 브론즈가 백지 42번째로 잘못 센 파일 | 09-12 블랙(563f415 검수) |
| A-025 | 2026-09-12 | 매거진 기사 전체 | 기사를 다시 방문한다 | 최신 버전을 받는다 | 옛 버전이 남을 수 있다 | 캐시 표시가 빠진 공통 파일 2개 | Low | 검토대기 | 116건에서 `../auth-ui.js`·`../search.js`가 `?v=` 스탬프 없이 로드. 다른 공통 js는 `?v=20260630`. 통일 대상 | 09-12 블랙(563f415 검수) |

| A-026 | 2026-09-12 | 어드민 뉴스레터·디지털 알림 페이지 | 구독자 삭제 단추를 누른다 | 삭제 확인이 뜬다 | 이메일에 작은따옴표가 있으면 단추가 죽는다 | A-011 계열. 형식 검증이 없으면 문자열 탈출 여지 | Medium | 검토대기 | `admin/newsletter-subscribers.html:212`·`notify-subscribers.html:213` `onclick="deleteSubscriber('id','escapeAttr(email)')"` — HTML 엔티티가 파서에서 되돌려진 뒤 JS가 읽음. `data-id`/`data-email` + 이벤트 위임으로 | 09-12 블랙(d1b18fc 검수) |
| A-027 | 2026-09-12 | 브랜드 상세(공개) | 승인된 브랜드 페이지 데이터를 주소로 읽는다 | 공개 정보만 보인다 | 과거 정지·거절 사유와 어드민 이메일이 이력에 남아 누구나 읽을 수 있다 | 복구된 브랜드의 옛 사유가 공개되는 것은 새 노출. 화면엔 안 그리지만 데이터는 열려 있다 | Medium | 보류 | `firestore.rules:57-59` approved 브랜드 공개 read + `brands.history[].note/by`(2단계). 해결: history 를 별도 컬렉션(어드민 전용)으로 빼거나 note 를 공개 read에서 제외 불가 → 컬렉션 분리. **한울님 판단**(계약서 4-7대로 구현됨) | 09-12 블랙(d1b18fc 검수) |

| A-028 | 2026-09-12 | 어드민(비로그인) | 로그인 안 한 채 어드민 주소 두 개를 연다 | 같은 로그인 화면으로 간다 | 대시보드는 브랜드 포털 로그인으로, 자문 목록은 전문가 로그인으로 간다 | 어드민이 어느 로그인 화면을 써야 하는지 헷갈린다 | Low | 검토대기 | `admin/dashboard.html:326` → `brand-portal/login.html`(next 없음) / `admin/consultations.html:283` → `auth/login.html?next=`. 어드민 개편 8단계(흡수) 또는 core 공통 리다이렉트로 통일 | 09-12 브론즈 R7 |

| A-029 | 2026-09-12 | 어드민 활동 로그·설정·규정집 화면 | 언어를 바꾼다 | 안내 문장도 바뀐다 | 표·버튼은 바뀌는데 상태 안내 문장(“0건 표시 중” 등)만 이전 언어로 남는다 | 2·3단계 공통 패턴. 경미 | Low | 검토대기 | `view-logs.js:181`·`view-settings.js:160·168·222·226·240`·`view-regulations.js:74·88·111·115`·`view-users.js:181`·`admin-nav.js:349`(검색 바닥 줄)·`view-leads.js lead_openForm` 안내문 — `textContent`에 `t()` 결과만. `tSpan` 짝으로 일괄 정리 | 09-12 블랙(6cce1d4 검수) · 09-12 블랙(3bb936d) 위치 추가 |
| A-033 | 2026-09-12 | 어드민 전체(배포 운영) | 어드민 코드를 고쳐 올린다 | 방문자가 새 코드를 받는다 | 캐시 표시(`?v=`)를 안 올리면 옛 파일이 남아 새 함수를 못 찾고 화면이 멈출 수 있다 | 6단계에서 실제로 스탬프 없이 두 번 바뀜(5단계에서 20260912b로 갱신). 재발 방지 장치 필요 | Medium | 검토대기 | `tools/deploy.ps1`에 "올릴 커밋에 admin/js·admin.css 변경이 있는데 `?v=` 값이 안 바뀌면 경고" 추가, 또는 노드-블랙 항목 10 강화. 계약서 리스크 6 | 09-12 블랙(3bb936d 검수) |

| A-030 | 2026-09-12 | 브랜드 포털 제품·글 목록 | 브랜드가 자기 제품·글 목록을 본다 | 글자 그대로 보인다 | 제품명·설명에 HTML을 넣으면 그대로 실행된다 | 브랜드 본인 데이터라 자기 화면에만 영향(Low). 포털에 공통 이스케이프 함수가 없다 | Low | 검토대기 | `brand-portal/dashboard.html:1048-1057 loadProducts`·`1076-1084 loadArticles` `${p.name}`·`${p.imageUrl}`·`${a.title}`·`${a.content}` 미이스케이프. 4a에서 `inbox_esc` 신설 → 공용화 후 적용. A-003 계열 | 09-12 블랙(91aa61d 검수) |

| A-031 | 2026-09-12 | 브랜드 포털 로그인(한국어) | 한국어로 로그인 화면을 연다 | 입력칸 안내문도 한국어 | 라벨은 한국어인데 칸 안은 영어("name@brand.com"·"Enter your password") | 한 화면에 두 언어 | Low | 검토대기 | `brand-portal/login.html:92·96·116` placeholder 하드코딩, `data-ko-placeholder` 없음(A-017 계열) | 09-12 브론즈 R8 |
| A-032 | 2026-09-12 | 어드민 대시보드 | 페이지를 연다 | 외부 부품이 바뀌어도 화면이 눈치챈다 | 버전만 고정, 무결성 검증표 없음 | CDN 파일이 바뀌어도 그대로 실행(예방) | Low | 검토대기 | `admin/dashboard.html` cdnjs marked·dompurify·font-awesome에 `integrity`·`crossorigin` 없음. SRI 해시 추가 | 09-12 브론즈 R8 · 블랙 6cce1d4 INFO |

| A-034 | 2026-09-12 | 사이트 전 폼(입점·자문·투고·뉴스레터·포털 문의) | 빈칸 오류를 보고 5초 안에 다시 보내기를 누른다 | 오류 안내가 다시 뜬다 | 아무 일도 안 일어난다 | 재제출 차단이 검증 실패 제출까지 잠그고 안내가 없다. 방문자는 "안 눌리네"만 겪는다 | Medium | 검토대기 | `form-throttle.js:8-19` 캡처 단계 preventDefault+stopPropagation, 검증 실패 제출도 타임스탬프 기록. 검증 통과 후에만 기록하거나 잠긴 동안 안내 문구. 전 폼 공통 | 09-12 블랙(f1a00b2 검수) |

| A-035 | 2026-09-12 | 어드민 접수 > 자문 요청(별도 페이지 5개) | 한국어로 쓰던 어드민에서 사이드바 「자문 요청」을 눌렀다 | 한국어 그대로 이어진다 | 갑자기 영어 페이지로 바뀐다 | 한 어드민 안에서 언어가 오락가락한다. **한울님 요청: 어드민에 한/영 전환 버튼도 두기** | **High**(한울님 직접 발견) | 확인대기 | `admin/consultations.html`·`trend-submissions.html`·`newsletter-subscribers.html`·`notify-subscribers.html`·`articles.html`이 `lang.js`를 안 실어 `html lang="en"` 고정 → 사이드바 `t()`도 영어. 해결: ① 5페이지에 `../lang.js?v=` 로드 + `data-en/ko` 짝 보강 ② 사이드바 상단에 한/영 토글(`window.toggleLang`) ③ 근본은 계약서 8단계(별도 페이지 흡수). 블랙 93e00be INFO에서 예고됐던 건. **구현(09-12 화이트)**: ① 5페이지 `../lang.js?v=20260630` 을 `admin-nav.js` 뒤에 로드 + 상단 nav-link·Log Out·Authenticating 짝(본문 전수는 A-017·A-029 때) ② `admin-nav.js` `adm_langHtml/adm_switchLang` — 검색창 아래 KO|EN → `window.toggleLang()` + `adm_renderSidebar()` 재렌더(+활성·문의 배지 복원, aside/document 리스너 1회 가드) ③ `admin-core.js` `admInitLang()` — 저장값 en/ko 없으면 `archinode-lang`=ko 저장 + `<html lang>` 즉시 동기화(lang.js 무수정, IP 감지 안 탐) ④ `admin.css` `.adm-lang` + `.lang-switcher-floating` 숨김 ⑤ 스탬프 20260912d 26개 | 09-12 한울님 발견 · 지휘 창 등록 · 09-12 화이트 구현 → 확인대기(토글·유지·별도 페이지 진입은 로그인 실측 — 한울님 눈 또는 브론즈) |

| A-036 | 2026-09-12 | 공개 페이지(어드민을 연 브라우저) | 어드민을 한 번 열고 나서 홈을 연다 | 접속 나라에 따라 언어가 정해진다 | 항상 한국어로 시작한다 | 어드민 본인 브라우저에만 해당. 해외 방문자 시점 테스트(VPN)가 왜곡됨. 규정집 1-9 첫 문장과 미세하게 어긋남 | Low | 검토대기 | `admin/js/admin-core.js:36-46 admInitLang`이 저장값 없을 때 `archinode-lang=ko` 기록 → `lang.js:50` IP 조회 영구 스킵. 근본 해법: lang.js에 페이지 단위 IP 감지 opt-out 속성 1줄(공개 파일 → 승인 후). 부수: `search.js:143` 기본값 en vs lang.js 기본 ko 불일치(기존) | 09-12 블랙(ef420ac 검수) |

| A-037 | 2026-09-12 | 로그인·가입·프로필·비밀번호 재설정 페이지 | 검색엔진이 페이지를 읽는다 | 검색에 안 잡힌다 | 로봇 파일로만 막고 페이지 자체 표시는 없다 | 새 착륙 페이지만 noindex라 형제 4페이지와 불일치. 실해 낮음 | Low | 확인대기 | `auth/login·signup·profile·reset-password.html`에 `<meta name="robots" content="noindex,nofollow">` 없음(robots.txt는 `/auth/` Disallow). 공개 페이지 → 승인 후. **구현(09-14 화이트)**: 4파일 head에 `<meta name="robots" content="noindex,nofollow">` 한 줄씩 추가(token-entry.html과 동일 패턴). `git diff` 라인 수 파일당 +1로 다른 변경 없음 확인. 커밋 `95f9e3b`. node-black CRITICAL 0 | 09-12 블랙(9222947 검수) · 09-14 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 |

| A-038 | 2026-09-12 | 어드민 상단바(좁은 화면 322px) | 창을 좁혀 어드민을 본다 | 단추가 한 줄로 정돈된다 | "브랜드 로그인"·"로그아웃" 글자가 세로로 끊겨 화면 위로 삐져나간다 | 휴대폰 폭에서 어드민 상단이 깨짐. A-014(공개 페이지)와 같은 원인 가능성 | Medium | 확인완료 | 탭 A-012. `getBoundingClientRect` width 12~52px·top 음수. `.topbar .right` 요소 `white-space:nowrap`·모바일 미디어쿼리 없음. **구현(09-14 화이트)**: `admin/dashboard.html` 기존 768px 미디어쿼리 블록(다른 작업자가 만지는 636줄 근처 Referral 모달과는 무관한 144줄 부근)에 `.topbar`/`.topbar .right`/`.topbar .right > *`/`.topbar .admin-email` nowrap+flex-wrap+ellipsis 7줄 추가. 로그인 화면이라 화이트는 마크업 주입(정적 파일 `_qa_topbar_test.html`에 topbar 마크업+admin.css+같은 미디어쿼리 복제)으로 322px 렌더만 확인 — 실측(주입). 커밋 `95f9e3b`. node-black CRITICAL 0(636줄 비침범 diff로 확인). **R13 실브라우저 실측(09-14 브론즈)**: 어드민 열쇠 로그인 상태에서 322px(스크린샷 644×1400 = 2×322×2×700 확인)로 축소. "브랜드 로그인"·"Log Out"이 완전한 단어 단위로 줄바꿈(3줄)되어 표시, 한 글자씩 쪼개지지 않고 화면 오른쪽 밖으로도 안 나감(Log Out 우측 끝 ≈305px < 322px) | 09-12 브론즈 R11(탭 등록) · 09-14 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 · 09-14 브론즈 R13 실제 로그인 화면 322px 실측 통과 → 확인완료 |
| A-039 | 2026-09-12 | 브랜드 포털 탭바(좁은 화면) | 창을 좁혀 포털 탭을 누른다 | 탭 글자가 읽힌다 | "제품"·"브랜드 매거진" 글자가 세로로 쪼개진다 | 정확히 누르기 어렵다 | Medium | 확인완료 | 탭 A-013. 포털 `.tabs` 탭이 `flex` 고정 폭, 좁은 폭 규칙 없음. **구현(09-14 화이트)**: `brand-portal/dashboard.html` 기존 768px 미디어쿼리 블록에 `.tabs`(overflow-x:auto)·`.tab`(white-space:nowrap) 추가. 로그인 화면이라 마크업 주입으로 375px 렌더 확인 — 실측(주입). 커밋 `95f9e3b`. node-black CRITICAL 0. **R13 실브라우저 실측(09-14 브론즈)**: 브랜드 열쇠(qa-brand@archinodekr.com) 로그인, 322px에서 탭 6개(브랜드 정보·제품·브랜드 매거진·인박스·문의·설정) 전부 `white-space:nowrap`, `.tabs` 내부만 `overflow-x:auto`(scrollWidth 529>clientWidth 274)로 가로 스크롤, 페이지 자체는 가로 스크롤 없음(scrollWidth===innerWidth). **단, 같은 화면 상단의 `.topbar`("Brand Guide"·"로그아웃")는 이 수정 대상이 아니라서 여전히 글자가 세로로 쪼개짐 — 별도로 A-049 새로 등록(탭 A-016)** | 09-12 브론즈 R11 · 09-14 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 · 09-14 브론즈 R13 실제 로그인 화면 322px 실측 통과(탭바만) → 확인완료, 상단바 잔여 결함은 A-049로 분리 등록 |
| A-041 | 2026-09-12 | 어드민 통합 검색 | "테스트"를 검색한다 | 공지·문의도 나온다 | 브랜드·리드·회원·검수만 나온다 | 6단계 검색 범위가 5단계 공지·문의를 안 담음(5단계 화이트가 범위 밖으로 남긴 것) | Low | 확인완료 | 탭 A-015. `admin-core.js ADM_SEARCH_SOURCES`에 notices·inquiries 추가 + `admRegisterCache` 호출. **구현(09-12 화이트)**: `ADM_SEARCH_SOURCES` 에 notices·inquiries 추가. **재수정(09-12 화이트, R12)**: 문의 선로드(`inq_ensureLoaded()`)를 `view-dashboard.js DASH_CARDS` 문의 카드 loader 에 연결해 로그인 시 1회 읽음. **R13 재확인(09-14 브론즈)**: 어드민 열쇠로 로그인한 뒤 1:1 문의 탭을 **열지 않고** 검색창에 "테스트" 입력 → `admSearchResults`에 "문의 [테스트-브론즈] R11 문의" 결과가 정상 표시됨(8건 중 하나, 하단에 "아직 안 연 화면: 리드, 공지"라는 안내가 별도로 있음 — 의도된 동작). 대시보드 "열린 문의" 카드 0건은 실제 문의 목록(1건, 상태 "답변됨")과 일치 확인 | 09-12 브론즈 R11 · 09-12 화이트 구현 → 확인대기 · 09-12 브론즈 R12 재수정요청(문의 미검색) · 09-12 화이트 선로드 재수정 → 확인대기 · 09-14 브론즈 R13 재확인 통과(문의 탭 열지 않고 검색됨, 배지=목록 수 일치) → 확인완료 |

| A-043 | 2026-09-12 | 어드민 대시보드(콘솔) | 대시보드를 연다 | 뒤에서 오류가 없다 | 색인 오류 4건이 상시로 뜬다(브론즈 R12 관찰) | 어떤 조회인지 미확인. 어드민 코드에는 where+orderBy 복합 조회가 없어(지휘 창 grep) 포털 탭 잔류 오류일 가능성 있음 | Medium | 검토대기 | 다음 브론즈 라운드에서 **오류 메시지 안의 색인 생성 링크(필드 목록)를 그대로 기록**할 것. 어드민 단독 새 탭에서 재현되는지. 재현되면 `firestore.indexes.json`에 추가·배포. **R13 시도(09-14 브론즈)**: 어드민 열쇠로 새로 로그인해 `admin/dashboard.html`을 여러 번 재로드하고 대시보드·리드 인박스·1:1 문의·전문가 회원·검수관리 탭을 차례로 열었으나 콘솔에 어떤 오류도 안 떴다(직접 넣은 테스트용 오류 문구만 보임). `admin/consultations.html`·`trend-submissions.html`·`newsletter-subscribers.html`·`notify-subscribers.html` 별도 페이지도 각각 열어 확인했으나 마찬가지로 오류 없음. **이번 라운드에서는 재현 안 됨** — R12 관찰 당시의 특정 상태(다른 탭 잔류, 최초 로그인 직후 등)에서만 뜨는 것일 수 있어 완전히 해소됐다고 단정하지 않는다 | 09-12 브론즈 R12 관찰 · 지휘 창 등록 · 09-14 브론즈 R13 재현 시도 — 여러 화면에서 재현 안 됨(상태 변경 안 함) |

| A-044 | 2026-09-12 | 어드민 1:1 문의 배지·카드 | 문의 화면에서 「새로고침」을 누른다 | 숫자가 그대로 있다가 갱신된다 | 읽는 동안 잠깐 0으로 깜빡인다 | 데이터 손실 없음, 깜빡임뿐 | Low | 검토대기 | `view-inquiries.js:90·115` 새로고침이 `items=[]`·`openCount=null`로 비운 뒤 `loaded`가 true라 `inq_ensureLoaded`가 빈 배열을 셈. `loading` 중엔 push 건너뛰기 한 줄 | 09-12 블랙(93bcf5f 검수) |
| A-045 | 2026-09-14 | 카테고리 서브페이지(예: `categories/furniture/sofas-armchairs.html`) | 페이지를 연다 | 검색·즐겨찾기 등 하단 스크립트가 정상 동작한다 | 콘솔에 `Uncaught SyntaxError: Identifier 'header' has already been declared` | 같은 페이지 안에 `header`가 두 번(245줄 `const`, 283줄 `var`로 추정) 선언돼 두 번째 인라인 `<script>` 블록 전체(토글·즐겨찾기 핸들러 포함)가 파싱조차 안 되고 죽는다 | Medium | 검토대기 | BUG-1(A-001) 수정 중 node-white·node-black이 로컬 실측(정적 서버+브라우저 콘솔)으로 발견. 이번 수정과 무관한 기존 결함, 고치지 않음. 다른 카테고리 서브페이지에도 같은 패턴이 있는지는 확인 못 함(이 1개 파일만 실측) | 09-14 node-white·node-black 발견(BUG-1 검증 중), 지휘 창 등록 |
| A-046 | 2026-09-14 | 카테고리 서브페이지(예: `categories/furniture/sofas-armchairs.html`) | 페이지를 연다 | 그 카테고리 제품 목록이 뜬다 | 콘솔에 `FirebaseError: The query requires an index`(failed-precondition), 제품 목록이 비어 보일 수 있음 | `category-products.js`로 추정되는 공유 스크립트의 products 조회(카테고리 필드 + `orderBy createdAt`으로 추정)에 복합 색인이 없음. A-040(브랜드 포털 제품·글 목록)과 같은 패턴이지만 다른 조회 — A-040 색인 배포로는 안 풀림 | High | 검토대기 | BUG-1(A-001) 수정 중 node-black이 로컬 실측으로 발견. 정확한 조회 코드 위치(`category-products.js` 내부)는 열어보지 않음 — 확인 못 함. 재현되면 `firestore.indexes.json`에 products(category ASC + createdAt DESC 추정) 추가 후 `deploy --only firestore:indexes` | 09-14 node-black 발견(BUG-1 검증 중), 지휘 창 등록 |
| A-047 | 2026-09-14 | 공개 페이지 약 70개(카테고리 서브페이지 다수 — `categories/bathroom/*`·`construction/*`·`kitchen/*`·`lighting/*`·`furniture/*` 등) | 로그인한 뒤 그 페이지를 연다 | 헤더 우측에 로그인 상태(대시보드/마이페이지/Admin 버튼)가 뜬다 | 로그인 여부와 무관하게 헤더가 항상 비로그인 그대로다(콘솔 오류는 없음 — 조용한 실패) | 해당 페이지들이 `auth-ui.js`를 `firebase-config.js`보다 먼저 로드한다(auth-ui.js 자신의 5줄 주석과 반대 순서). auth-ui.js의 IIFE가 실행되는 시점엔 `auth`가 아직 어디서도 선언되지 않아 `typeof auth === 'undefined'` 가드가 항상 참이 되어 조용히 반환 — BUG-1(A-001)과 무관하게 이전부터 있던 순서 결함, 이번 수정이 만든 게 아님 | Medium | 검토대기 | node-black 코드검토(스크립트 태그 줄번호 비교, 총 80개 동시 로드 페이지 중 70개가 역순 — CLAUDE.md 4절 "스크립트 로드 순서" 위반) + `categories/furniture/sofas-armchairs.html` 실측(`.header-actions`에 `.auth-link` 미생성) 확인. 대조군 `brands.html`(정상 순서, auth-compat 있음)은 "Log In" 정상 렌더. 70개 목록 전수는 아직 확인 못 함(대표 1개만 실측), 고치지 않음 | 09-14 node-black 발견(BUG-1 검증 중), 지휘 창 등록 |
| A-048 | 2026-09-14 | `privacy-en.html`(휴대폰 375px) | 휴대폰으로 연다 | style.css 모바일 헤더 수정(A-014)이 다른 공개 페이지처럼 적용돼 상단이 한 줄로 정돈된다 | 코드검토 결과 이 페이지도 index.html처럼 자체 인라인 `<style>`에 별도 미디어쿼리를 갖고 있어 style.css 수정이 안 먹힐 가능성이 높다(index.html이 그래서 따로 고쳐야 했던 것과 같은 패턴) | A-014를 고치면서 같이 안 잡혔을 수 있는 페이지 — 실측은 안 함 | Low | 검토대기 | node-black이 코드검토만으로 지적(실측 안 함). index.html 사례(인라인 style 별도 미디어쿼리)와 동일 원인 추정. 375px 실측 후 필요하면 index.html과 같은 패턴으로 수정 | 09-14 node-black 발견(모바일 수정 검증 중), 지휘 창 등록 |
| A-050 | 2026-09-14 | 브랜드 포털 상단바 「Brand Guide」 링크 | 새 탭으로 브랜드 가이드를 연다 | 새 탭이 원래 탭과 분리되어 열린다 | 새로 연 탭(브랜드 가이드)이 `window.opener`로 원래 탭(브랜드 포털)을 조작할 수 있는 상태로 열린다 | 실해는 낮음(같은 사이트 내부 링크), 다만 보안 모범사례 위반. 방문자가 알아채는 문제는 아님 | Low | 검토대기 | `brand-portal/dashboard.html:199` `<a href="../brand-guide.html" target="_blank" ...>`에 `rel="noopener"` 없음. A-049 수정과 무관한 기존 결함, 이번엔 고치지 않음 | 09-14 node-black 발견(A-049 검증 중), 지휘 창 등록 |
| A-051 | 2026-09-15 | 어드민 사이드바 KO\|EN 언어 토글(첫 화면) | 어드민에 로그인해 처음 화면을 본다 | 검색창 아래 "KO \| EN" 두 글자 토글이 보인다 | 처음엔 "Korean \| English"로 풀어져 보이다가, 토글을 한 번 누르면 그제서야 "KO \| EN"으로 정상으로 바뀐다 | 첫인상이 어색하고, 누르기 전까지 버튼처럼 안 보여 헷갈릴 수 있다. 기능(언어 전환) 자체는 정상 | Low | 확인완료 | `admin/js/admin-nav.js:225,227` `adm_langHtml()`이 KO/EN 버튼에 `tAttr('Korean','한국어')`·`tAttr('English','영어')`를 붙였는데, `tAttr()`(`admin-core.js:29`)는 `data-en`/`data-ko` 속성을 반환한다 — 이 속성은 사이트 공통 `lang.js`의 `applyLanguage()`(161줄)가 "번역해야 할 글자"로 인식해 요소의 텍스트를 통째로 덮어쓰는 바로 그 표시라, 버튼 고정 글자 "KO"/"EN"이 `lang.js` 전역 스캔에 걸려 "Korean"/"English"로 바뀌는 것. 토글 클릭 시 `adm_switchLang()`이 `adm_renderSidebar()`로 사이드바를 통째로 다시 그려 고정 텍스트가 복원되므로 그 뒤엔 정상으로 보인다. 해결: 이 두 버튼엔 `data-en`/`data-ko`를 쓰지 말고(고정 두 글자라 번역 대상이 아님) 필요하면 `title=` 등 별도 속성으로. **구현(09-15 화이트)**: `admin/js/admin-nav.js` `adm_langHtml()`(222~231줄)에서 두 `tAttr(...)` 호출을 제거하고 `title="한국어"`/`title="English"`로 대체(+6/-2줄, `<span class="adm-lang-sep">` 그대로). 로그인 화면이라 화이트는 `lang.js`·`admin-core.js` 관련 함수를 정적 파일로 복제해 로컬 서버(포트 8100)에 올려 **실측(주입)**: 수정 전 코드(`tAttr` 그대로)는 `window.toggleLang()` 1회차부터 버튼 텍스트가 `["Korean","English"]`로 깨짐 재현 확인, 수정 후 코드는 `toggleLang()` 4회 연속 호출해도 매 회차 `["KO","EN"]` 유지 확인(재현 시나리오로 원인·해결 둘 다 검증). node-black 검수 CRITICAL 0(`adm_initLangToggle`·`adm_switchLang`의 `data-lang`·클릭 핸들러·active 토글 로직 무영향 확인, 같은 오적용 패턴이 다른 admin/js 파일에 없는지 전수 grep도 재발 없음 확인). 커밋 `7a5a27b`. 실브라우저 로그인 상태에서 첫 화면 표시·호버 툴팁 확인은 브론즈 몫(못 함) | 09-15 지휘 창(한울님 스크린샷 제보) 발견·등록 → 09-15 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 → 09-15 노드-브론즈 R14 실측: 커밋 `7a5a27b` 라이브 배포 확인(파일 diff 0) · 열쇠 로그인(wool21wool@gmail.com) 첫 화면 "KO \| EN" 정상 · 새로고침 4회 연속 유지(재현 안 됨) · 토글 클릭 시 활성 표시 정상 전환 · 호버 title 속성 "한국어"/"English" 확인 → **확인완료** |
| A-052 | 2026-09-15 | 어드민 규정집/설정 등 본문 화면(넓은 모니터) | 규정집 화면을 넓은 화면에서 연다 | 화면 폭을 알맞게 채워서 보인다 | 본문 영역이 1200px에서 멈추고 양옆에 넓은 빈 회색 여백이 남는다 | 넓은 모니터일수록 어색하고 좁아 보인다. 방문자 화면 아님(어드민 전용) | Low | 검토대기 | `admin/dashboard.html:42` `.admin { max-width: 1200px; margin: 0 auto; }` — 공개 사이트(`style.css`)는 1440px을 쓰는데 어드민만 1200px로 좁게 잡혀 있음. 1440px로 맞추면 해결 | 09-15 한울님 스크린샷 제보·지휘 창 등록 |
| A-053 | 2026-09-15 | 어드민 사이드바(내용 긴 화면에서 아래로 스크롤) | 오른쪽 본문이 긴 화면(예: 규정집)에서 아래로 스크롤한다 | 사이드바도 같이 스크롤되거나, 화면에 붙어 있어도 메뉴 전체가 다 보인다 | 사이드바는 화면에 붙어(sticky) 있는데 사이드바 자신은 스크롤이 안 돼서, 메뉴가 길면 아래쪽 항목(예: 설정·앱으로 설치 버튼)이 화면 밖으로 잘려 영영 안 보인다 | 사이드바 아래쪽 메뉴를 못 누른다 | Medium | 검토대기 | `admin/admin.css:12` `#adminSidebar { position: sticky; top: 72px; }`에 자체 높이 제한·스크롤이 없음. 같은 파일 154줄 `.adm-reg-toc`(규정집 목차)는 이미 `max-height: calc(100vh - 100px); overflow-y: auto;`로 같은 문제를 제대로 처리해뒀음 — 같은 처방을 `#adminSidebar`에도 추가하면 됨 | 09-15 한울님 제보·지휘 창 등록 |
| A-054 | 2026-09-15 | 검수관리 목록의 "상태" 칸(전 화면 공통 — 브랜드·제품·아티클·검수관리) | 검수관리(또는 브랜드) 목록을 본다 | "검토대기"처럼 4~5글자짜리 상태 표시가 알약 모양 배지 안에 가로로 한 줄로 보인다 | 배지가 좁아서 "검", "토", "대", "기"가 한 글자씩 세로로 쌓여 기둥처럼 보인다 | 상태를 한눈에 읽기 어렵고 화면이 지저분해 보인다 | Medium | 검토대기 | `admin/dashboard.html:68` `.status { display: inline-block; padding: 3px 10px; ... }`에 `white-space: nowrap`이 없음. 영어 단어(PENDING 등) 기준으로 만들어져 있어 짧은 글자에선 안 드러났지만, 한글 검수 상태("검토대기"·"확인완료"·"재수정요청")처럼 긴 글자가 좁은 표 칸 안에 들어가면 글자 단위로 줄바꿈된다. `.status`에 `white-space: nowrap` 한 줄만 추가하면 브랜드·제품·아티클·검수관리 배지 전부 한 번에 해결 | 09-15 한울님 스크린샷 제보·지휘 창 등록 |
| A-049 | 2026-09-14 | 브랜드 포털 상단바(좁은 화면 322px) | 브랜드 계정으로 로그인한 뒤 화면을 휴대폰 정도로 좁혔다 | 화면 위쪽 검은 띠에 "Brand Guide"·"로그아웃" 글자가 작아지더라도 한 줄로는 보일 것 | "Brand Guide"·"로그아웃" 글자가 한 글자씩 세로로 쌓여 좁은 기둥처럼 보였고, 위쪽 절반은 화면 위로 잘려서 로고와 겹쳐 보이기까지 했다 | 휴대폰으로 브랜드 포털에 들어온 담당자가 Brand Guide 링크나 로그아웃 버튼을 찾기 어렵고 정확히 누르기도 어렵다. 어드민 상단바(A-038, 확인완료)와 같은 종류의 문제인데 브랜드 포털 쪽은 이번 수정(95f9e3b) 대상이 아니어서 아직 안 고쳐진 상태 | Medium | 확인대기 | 탭 A-016. `brand-portal/dashboard.html:25` `.topbar{display:flex;justify-content:space-between}` flex-wrap 없음, `:28 .user-info{display:flex;gap:16px}`도 wrap 없음, `:199` Brand Guide 링크·`:201` 로그아웃 버튼에 `white-space:nowrap` 없음. `:183` 768px 미디어쿼리는 `.tabs`/`.tab`만 다루고(A-039 수정분) `.topbar`는 대상 밖. 실측(322px): "Brand Guide" rect width 40.8/height 143.5(정상은 20여px 높이), top -43.75(화면 위로 넘침) — 로그아웃도 동일 패턴. A-038 수정과 같은 방식(nowrap+flex-wrap 추가)으로 고치면 됨. 콘솔 오류 없음(레이아웃만 깨짐). **구현(09-14 화이트, 한울님 부재중 지휘 창 직접 호출)**: `brand-portal/dashboard.html` 기존 768px 미디어쿼리 블록(A-039가 쓴 자리)에 `.topbar`/`.topbar .user-info`/`.topbar .user-info > *`/`.topbar .user-email` 4규칙 6줄 추가(admin/dashboard.html A-038 처방을 `.right`→`.user-info`, `.admin-email`→`.user-email`로 이 파일 클래스명에 맞게 이식). diff는 그 6줄만(`git diff --stat` +6/-0), 파일 잘림 없음(1489→1495줄). 로그인 화면이라 화이트는 마크업+CSS를 정적 파일로 복제해 로컬 static 서버(포트 8099)로 322px 렌더 확인 — **실측(주입)**: "Brand Guide" rect width 122/height 30/top 37, "Log Out" width 93/height 29/top 73 — 둘 다 완전한 단어로 한 줄, `right` 306px < `pageW` 322px(화면 밖으로 안 나감), 기존 결함의 height 143.5·top -43.75 패턴 재현 안 됨. node-black 검수 CRITICAL 0(diff 6줄 범위 확인, 인라인 스크립트 파싱 정상, 클래스명 실존 대조로 오탈자 없음 확인 — WARNING·CRITICAL 없음, INFO 1건은 A-049와 무관한 기존 결함이라 별도 등록 권장). 커밋 `c862bac`. 실브라우저 로그인 상태 322px 확인은 브론즈 몫(못 함) | 09-14 브론즈 R13 발견·탭 A-016 등록 · 09-14 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 |

R12 갱신(2026-09-12 밤, 브론즈 로그인 라운드): **A-040 확인완료**(포털 콘솔 오류 0, 제품·글 목록 정상 로드) · **A-042 확인완료**(숨기기 정상, 토스트·배지·새로고침 유지 — R11의 "상태 불변"은 재현 안 됨) · **A-041 재수정요청**(공지는 검색됨, 1:1 문의는 그 화면을 열기 전엔 캐시가 없어 미검색 → 로그인 시 선로드로 보완 예정).

R13 갱신(2026-09-14, 부재중 사다리 「다」 단 브론즈 마라톤): **A-038 확인완료**(어드민 상단바 322px 실측 통과) · **A-039 확인완료**(포털 탭바 322px 실측 통과, 단 같은 화면 상단바는 별도 결함) · **A-041 확인완료**(문의 탭 안 열고 검색해도 결과에 나옴, 배지=목록 수 일치) · **A-043 재현 안 됨**(여러 화면에서 색인 오류 못 찾음, 상태는 검토대기 유지) · **A-049 신규 등록**(브랜드 포털 상단바 322px에서 "Brand Guide"·"로그아웃" 글자 쪼개짐, 탭 A-016) · **7b 유입경로**(`list-your-brand.html?ref=qa-brz-01` → `sessionStorage.archinode_ref` 정상 저장 확인, 실제 제출→Firestore→Referral Source 표시는 확인 못 함, 신청서 제출은 하지 않음).

A-019 보강(블랙 563f415): `search-index.json`도 같은 30건 누락. 복구 41건 중 12건은 sitemap·검색 색인 둘 다 없음 — 자동 발행이 두 파일을 갱신하지 않는 것으로 추정. 우선 Medium으로 올림.

재발견(2026-09-12 브론즈): A-001(전 공개 페이지 콘솔 + `category-products.js` `db is not defined`), A-002, A-012(`/sales/…`·`/firestore.rules`·`/tools/deploy.ps1`·`/docs/*.md` 전부 HTTP 200).

## 닫힌 건

| 번호 | 등록일 | 화면 | 무엇을 눌렀나 | 기대 | 실제 | 문제 | 우선 | 상태 | 개발 메모 | 이력 |
|---|---|---|---|---|---|---|---|---|---|---|
| A-040 | 2026-09-12 | 브랜드 포털 대시보드 | 포털을 연다 | 조용히 뜬다 | 뒤에서 오류 2건이 매번 난다 | 제품·글 목록 조회가 색인을 요구. 목록이 비어 보일 수 있다 | **High** | 확인완료 | 탭 A-014. Firestore `failed-precondition`(복합 색인) — `brand-portal/dashboard.html:1105·1133` `where brandId == uid` + `orderBy createdAt desc`(기존 코드 — **4월부터 실브랜드의 제품·글 목록이 조용히 실패했을 가능성**). `firestore.indexes.json`(products·articles brandId ASC + createdAt DESC) 신설·`firebase.json` 연결·`deploy --only firestore:indexes` 성공(1bff5ab). 색인 빌드 뒤 브론즈가 포털 콘솔 0 확인 | 09-12 브론즈 R11 · 09-12 지휘 창 색인 배포 완료 → 확인대기 · 09-12 브론즈 R12 확인완료(포털 콘솔 오류 0, 제품·글 목록 정상 로드) |
| A-042 | 2026-09-12 | 어드민 공지사항 | 공지의 「숨기기」를 누른다 | 노출이 꺼진다 | 상태가 안 바뀐다 | 노출 끄기가 안 되면 잘못 올린 공지를 못 내린다 | Medium | 확인완료 | 브론즈 R11 "클릭해도 상태 불변". 확인창 없이 인페이지 토글이어야 함 — `view-notices.js` 토글 핸들러 확인 필요(이벤트 위임·data-id). **구현(09-12 화이트)**: 코드검토(위임 `data-ntc-toggle` → `ntc_toggleVisible` → `update({visible,updatedAt,updatedBy})` → `logAdmin` → `notices_draw`, confirm 없음)·vm 주입 실측 모두 정상 → **원인 특정 못 함(재현 안 됨)**. 방어 수정: 저장 중 무시 대신 토스트 · 버튼 잠금 · 성공 시 «지금 목록» 항목을 id 로 갱신해 먼저 다시 그린 뒤 토스트·로그 · `notices_load()` 로 서버 값 재읽기(`ntc_save` 와 동일). | 09-12 브론즈 R11 · 09-12 화이트 방어 수정 → 확인대기 · 09-12 브론즈 R12 확인완료(숨기기 정상, 토스트·배지·새로고침 유지 — R11의 "상태 불변"은 재현 안 됨) |
