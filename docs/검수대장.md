# ARCHINODE 검수 대장

발견한 결함·개선점을 **전부 여기에 쌓는다.** 보고서(`archi-diary/bronze-*.md`)는 그날의 기록이고, "지금 무엇이 열려 있나"는 이 대장이 정본이다.
**2026-09-12부터 새 등록은 어드민 > 「검수관리」 탭(`reviewIssues`)에 한다** (한울님 "A절로 옮겨서 빨리 만들어"). 이 파일의 A-001~A-010은 탭의 [문서 대장 10건 가져오기]로 이관하고, 그 뒤 이 파일은 이관 전 기록으로만 남긴다. 탭이 라이브에 나가고 규칙이 게시되기 전까지는 임시로 여기에 계속 적는다.

## 규칙 (단번 검수판 · 엑사 검수관리 게시판을 문서로 축약)

- **등록은 누구나(브론즈·블랙·지휘 창·한울님), 판정은 사람만.** 에이전트가 올린 건은 `검토대기`로 들어가고, 한울님(또는 한울님이 위임한 지휘 창)이 「진짜 문제」로 올려야 수정 대상이 된다. 아무리 명백해 보여도 이 단계를 건너뛰지 않는다.
- **상태 순환**: `검토대기` → `진행중` / `보류` / `오탐반려` → (수정 뒤) `확인대기` → `확인완료` / `재수정요청`. 상태가 바뀌면 **이력 칸에 한 줄** 추가(날짜·누가·무엇).
- **네 칸(눌렀나·기대·실제·문제)은 비개발자가 읽는 글이다.** 파일 이름·줄 번호·콘솔 원문·함수명은 **개발 메모** 칸에만.
- 우선순위: **High**(방문자·브랜드가 겪는다) / **Medium**(운영이 겪는다, 또는 보안) / **Low**(콘솔·문구·정리).
- 번호는 `A-###`, 재사용 금지. 중복 발견이면 새 번호를 만들지 말고 기존 건 이력에 "재발견"을 적는다.
- 브론즈 보고 형식의 「검수대장 등록: 번호 목록」이 이 파일과 짝이다.

## 대장

| 번호 | 등록일 | 화면 | 무엇을 눌렀나 | 기대 | 실제 | 문제 | 우선 | 상태 | 개발 메모 | 이력 |
|---|---|---|---|---|---|---|---|---|---|---|
| A-001 | 2026-08-14 | 홈 화면, 트렌드 리포트, 카테고리 전체 | 홈에 들어갔다 | 로그인한 브랜드라면 상단에 "대시보드" 단추가 보이고, 트렌드 리포트에는 최근 리포트 목록이 뜬다 | 화면은 뜨지만 로그인 상태가 상단에 반영되지 않고, 트렌드 리포트 목록은 빈 상태 안내로 가려진다 | 홈에 올 때마다 뒤에서 오류가 두 번 나고, 브랜드가 리포트를 발행해도 목록에 안 뜬다. 5월 8일부터 잠복 | High | 재수정요청 | `firebase-config.js:28` `const auth = (typeof firebase.auth === 'function') ? firebase.auth() : null;`(storage와 동일 패턴)로 수정. 연쇄로 `auth-ui.js:16` 가드도 `typeof auth === 'undefined' \|\| !auth`로 함께 수정(auth가 null이면 기존 `typeof undefined` 가드를 통과해 `auth.onAuthStateChanged`에서 새로 throw하는 걸 로컬 실측으로 발견). node-black CRITICAL 0. 커밋 `bc430e0`. 발견된 무관 기존 결함 2건은 별도 등록(카테고리 서브페이지 `header` 중복 선언 SyntaxError, products 쿼리 복합색인 누락) | 08-14 브론즈 등록 · 08-14 한울님 승인 대기(A안/B안) · 09-12 대장 이관 · 09-14 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 · 09-15 브론즈 R17 실측: 새 탭 기준 홈 콘솔 오류 0건은 확인(해소). 그러나 (1) qa-brand 로그인 상태로 홈을 열어도 상단에 "Dashboard" 버튼이 안 뜨고 "브랜드 로그인"만 보임 — `index.html`이 `firebase-auth-compat.js`를 안 실어(A-047과 같은 계열) auth-ui.js가 조용히 무동작, 같은 계정으로 `brands.html`은 정상 표시돼 대조 확인. (2) `trend-report.html` "최근 브랜드 매거진"은 항상 빈 상태 안내만 뜸 — `articles` 컬렉션 status+publishedAt 복합색인 누락으로 쿼리가 `failed-precondition`(console.warn 레벨이라 error 필터로는 안 잡힘, `db.collection().get()` 직접 호출로 재현). 이 두 증상은 bc430e0으로 해결 안 됨 — **검수관리 A-019·A-020으로 신규 등록, "확인완료" 판정 보류 제안** |
| A-002 | 2026-08-14 | 브랜드 온보딩 가이드 | 페이지를 열었다 | 조용히 뜬다 | 뜨긴 하지만 뒤에서 오류가 난다 | 파이어베이스를 안 쓰는 안내 페이지가 파이어베이스 설정을 불러온다. 기능 영향 없음 | Low | 검토대기 | `brand-guide.html:324` `firebase-config.js` 로드 제거. A-001과 함께 처리 권장 | 08-14 브론즈 · 09-12 이관 |
| A-003 | 2026-04-22 | 브랜드 상세 페이지 | 브랜드 소개를 본다 | 브랜드가 적은 글이 글자 그대로 보인다 | 브랜드가 소개란에 HTML을 넣으면 그대로 실행될 수 있다 | 입점 신청 쪽은 막았는데 보여주는 쪽이 아직 열려 있다. 엑사에서는 한 곳에서 시작한 같은 문제가 60곳으로 번졌다 | Medium | 검토대기 | `brands/view.html` innerHTML → textContent 또는 escapeHtml. 미완료업무 #2. 다음 단계 "XSS 출력 측 전수"의 첫 항목 | 04-22 화이트 · 09-12 이관 |
| A-004 | 2026-07-03 | 뉴스레터·입점 신청·자문 요청 폼 | (봇이) 폼을 반복 제출한다 | 막힌다 | 5초 간격만 막고, 그 이상은 무제한 | 봇 하나가 하루면 파이어베이스 무료 한도를 소진시켜 사이트 저장 기능이 멈출 수 있다 | Medium | 보류 | reCAPTCHA v3 무료 도입, 폼 3곳. `pre-launch-audit-2026-07-03-v3.md` P3-신규-1. 다음 단계(제품 기능)로 분류됨 | 07-03 블랙 · 09-12 이관·보류(다음 단계) |
| A-005 | 2026-07-03 | 브랜드 상세 페이지 | 그 브랜드가 쓴 매거진 글을 찾는다 | 브랜드 페이지에 그 브랜드의 글이 보인다 | 제품만 보이고 글은 안 보인다 | 브랜드가 글을 써도 자기 페이지에서 안 보인다 | Low | 보류 | `brands/view.html`에 articles(brandId, approved) 조회 추가, 15~30분. 다음 단계 | 07-03 블랙 · 09-12 이관·보류(다음 단계) |
| A-006 | 2026-07-02 | 검색·SNS 미리보기 | 링크를 카카오톡·슬랙에 붙였다 | 대표 이미지가 뜬다 | 37개 페이지는 벡터 이미지라 미리보기에 안 뜰 수 있다 | 공유했을 때 이미지 없는 카드가 나온다 | Low | 검토대기 | og:image가 SVG인 37페이지 → PNG로. `handoff-2026-07-02-final.md` 부록 B | 07-02 · 09-12 이관 |
| A-007 | 2026-08-14 | 입점 신청(휴대폰 390px) | 단추를 누른다 | 손가락으로 누르기 편하다 | 단추 5개 중 1개가 작다 | 경미 | Low | 검토대기 | 터치 타깃 44px 미만 1개. `bronze-2026-08-14.md` 5절 3항 | 08-14 브론즈 · 09-12 이관 |
| A-008 | 2026-08-14 | 입점 신청 | 언어 단추를 본다 | 영어 전용 페이지라 언어 단추가 없다 | "EN ▾" 드롭다운이 남아 있다 | 결함 아님(의도된 영어 단일 페이지). 숨기면 깔끔 | Low | 보류 | `list-your-brand.html` `data-langs="en"`, 미사용 `data-ko` 72개. brand-guide와 같은 방향으로 스위처 숨김 | 08-14 브론즈 관찰 · 09-12 이관 |
| A-009 | 2026-09-12 | 어드민 대시보드 | 브랜드를 정지했다가 복구한다 | 언제 누가 왜 정지·복구했는지 다 남는다 | 마지막 사유 하나만 남고 이전 것은 덮인다 | 분쟁 시 이력을 못 본다 | Low | 확인대기 | brands 문서에 `history[]` 배열(단번 검수판 패턴). 2단계 구현: `admin/dashboard.html` `updateBrand()` 의 update 에 `history: arrayUnion({status,by,at,note})` 동봉 + `adminLogs` 장부(`logAdmin`) + 브랜드 상세 모달 「이력」 절. 규칙 게시(`adminLogs`) 뒤 확인 | 09-12 지휘 창 · 보류(다음 단계) · 09-12 2단계 구현 — 브론즈 확인 대기 · 09-15 브론즈 R16 실측: `[테스트-브론즈] QA Brand`로 정지→복구 실행, 브랜드 상세 모달 「이력」 절에 SUSPENDED·APPROVED 각각 별도 줄로(누가·언제·왜) 정상 기록 확인. 콘솔 오류 0. R11 때 남긴 이전 이력 4건도 그대로 유지돼 총 5줄 누적 — **실측 확인** |
| A-010 | 2026-09-12 | 폼 4곳(입점·자문·원고·뉴스레터) | 인터넷이 끊긴 채 제출한다 | "연결이 느립니다" 같은 안내가 뜬다 | "접수 중"에서 멈춘 채 아무 안내가 없다 | 고객이 멈춘 건지 되는 중인지 모른다. 탭을 닫으면 접수가 사라진다 | Medium | 보류 | Firestore 오프라인 쓰기는 대기 상태라 실패 갈래가 안 온다. 엑사 9/8 "조용한 실패" 수정 패턴(타임아웃 안내 + 롤백). 다음 단계. 실측 안 함(엑사·단번 사례로 추정) | 09-12 지휘 창 · 보류(다음 단계) |
| A-011 | 2026-09-12 | 어드민 전체 (검수관리·브랜드 상세) | 등록 칸에 큰따옴표가 든 글을 저장하고 목록에서 본다 | 글자 그대로 보인다 | 큰따옴표 뒤 글이 잘리거나 엉뚱한 속성으로 붙는다 | 어드민 본인 화면에서만 나는 문제라 방문자 위험은 없지만, 브론즈·블랙이 붙여넣는 글에 큰따옴표가 흔해 실제로 겪을 가능성이 높다 | Medium | 검토대기 | `admin/dashboard.html:615` `escapeHtml`이 `"`·`'`를 안 바꿈 → 속성 컨텍스트(`title=` 824, `href=` 926·575, `data-ko` 736)에서 속성 주입. 속성용 이스케이프(`&quot;` `&#39;`) 추가 여부는 화이트·골드 판단. 노드-블랙 492fc12 검수 WARNING | 09-12 블랙 등록(탭 로그인 불가로 대장에) |
| A-012 | 2026-09-12 | 사이트 전체 (숨겨진 폴더) | 주소창에 `archinodekr.com/sales/cold-email-templates.md` 를 친다 | 없는 페이지라고 나온다 | 실비아 콜드메일 문안과 브랜드 후보 명단(엑셀)이 그대로 열린다 | 후보 브랜드 담당자 이름·이메일이 누구에게나 공개돼 있다. 개인정보·영업 자료 노출 | High | 검토대기 | GitHub Pages가 리포 전체를 서빙 — `docs/`·`sales/`·`silvia/`가 HTTP 200(노드-골드 실측). 해결: `sales/`·`silvia/`를 저장소에서 빼고(`git rm --cached`, 로컬 보존) `.gitignore` 추가, 또는 7a 단계에서 Firestore `prospects`로 이관. `docs/`는 규정집 뷰어가 fetch하므로 공개 유지 여부 별도 판단. **되돌리기 어렵지 않으나 "무엇을 공개할지"는 한울님 결정** | 09-12 골드 발견·지휘 창 등록 |
| A-013 | 2026-09-12 | 매거진 기사 | 매거진 목록에서 기사 카드를 눌렀다 | 기사 본문이 뜬다 | 제목만 탭에 뜨고 화면이 전부 흰색이다 | **116편 중 41편이 백지**(브론즈 42 중 oak-flooring 1건은 `</style>` 중복만, 렌더 정상). 오늘 자동 발행 3편도 포함이라 앞으로 나오는 기사도 계속 백지가 된다 | **High** | 확인완료 | `magazine/*.html` 41건(블랙 재계수 확정): 16행(tailored만 34행) `<style>` 안 `.article-hero{…url('../assets/magazine/placeholders/inte` 에서 잘린 뒤 바로 공지 바 마크업. `</style>`·`</head>`·`<body>` 없음. 로컬=라이브. 최초 e985d7c(05-06), 최근 1b4f583(09-12). 정상본 `agm-thermroof-…` 38~40행. **자동 발행 파이프라인(Cowork 계정 스킬 mag-*)의 템플릿도 같이 고쳐야 재발 안 함** — 리포 밖이라 한울님. 목록 `archi-diary/bronze-2026-09-12.md` 5-2. 공개 페이지 → 수정 커밋 후 「배포할까요?」 | 09-12 브론즈 R5 등록 · 09-12 화이트 재계수 41건(42번째 oak-flooring은 `</style>` 중복만, 렌더 정상) → 복구 커밋 563f415(41 기사 + `tools/fix-magazine-blank.js`) — 브론즈 라이브 재확인 대기, 파이프라인 템플릿은 한울님 · 09-12 브론즈 R7 라이브 재확인(라이브=17c8000 대조 ✓, 41/41 `</style>`·`</head>`·`<body` 각 1개·md5 로컬=라이브, 브라우저 8건 히어로·제목·본문 렌더 정상·innerText 2,307~3,240자·콘솔 오류 0·깨진 이미지 0) → **확인완료**. 「닫힌 건」 절 이동은 지휘 창. 파이프라인 템플릿(리포 밖) 재발 여부는 다음 자동 발행분에서 다시 확인 |
| A-014 | 2026-09-12 | 사이트 전체(휴대폰 375px) | 휴대폰으로 홈·브랜드·매거진을 열었다 | 상단 메뉴가 한 줄에 정돈돼 보인다 | "브랜드 로그인"·"건축을 잇다" 글자가 세로로 쌓여 안내 띠를 덮고, 브랜드·매거진·자문은 가로 스크롤이 생긴다 | 휴대폰 방문자 첫인상이 깨진다. A-001을 고치면 홈에서도 같이 나타난다 | **High** | 확인완료 | `.header-actions` 인라인 `<a href="brand-portal/login.html">`·`.logo-sub`에 `white-space:nowrap` 없음, `style.css:247` 모바일 쿼리가 `.main-nav`만 숨김. auth-ui.js Log In 삽입 시 451px. 실측 375: scrollWidth 451(brands·request-consultation·magazine). 768↑ 정상. 공개 페이지(style.css). **구현(09-14 화이트)**: `style.css`·`index.html`(자체 인라인 스타일이 같은 선택자를 재정의해 같이 수정) 모바일 미디어쿼리에 `.header .container`/`.logo-sub`/`.header-actions`/`.header-actions a,button` nowrap+flex-wrap, `.main-nav.active` top을 `var(--header-height)`→`100%`. 375px 실측(로컬): index·brands·request-consultation·magazine 4페이지 전부 scrollWidth 375(가로 스크롤 없음), 햄버거 드롭다운 위치 정상. 커밋 `95f9e3b`. node-black CRITICAL 0(492fc12 계열 별도, 이번 검수 세션) | 09-12 브론즈 R6 · 09-14 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 · 09-15 브론즈 R17 실측: `index.html`·`brands.html`·`request-consultation.html`·`magazine.html` 4페이지 375px 전부 scrollWidth=clientWidth=375(가로 스크롤 없음), 헤더 항목 각각 한 줄(세로로 안 쪼개짐), 화면 밖 넘침 없음 → **실측 확인** |
| A-015 | 2026-09-12 | 입점 신청(휴대폰) | 맨 아래로 내렸다 | 폭에 맞는 하단 정보 | 하단 네 칸이 옆으로 늘어나 가로로 밀린다 | 휴대폰에서 좌우로 흔들림 | Medium | 검토대기 | `list-your-brand.html:465` 인라인 `grid-template-columns:2fr 1fr 1fr 1fr` 모바일 규칙 없음. 실측 375 scrollWidth 483 | 09-12 브론즈 R6 |
| A-016 | 2026-09-12 | 세부 카테고리 69페이지 | 세부 카테고리(예: 소파)를 열었다 | 조용히 뜬다 | 뒤에서 오류가 난다 | 같은 코드가 두 번 들어가 뒤쪽 블록이 통째로 안 돈다(앞 블록이 대신) | Medium | 검토대기 | `categories/*/*.html` 69건: 245행 `const header` + 283행 `var header` → `SyntaxError: Identifier 'header' has already been declared`. 두 번째 `<script>`(null 가드판) 미실행. 하나로 통일 | 09-12 브론즈 R5 · 09-15 브론즈 R20 재발견(카테고리 서브페이지 5개 표본 5/5) |
| A-017 | 2026-09-12 | 카테고리 페이지 외 19페이지 | 한국어로 보다가 열었다 | 맨 위 안내 띠도 한국어 | 띠만 영어("2026 Brand Listing — Now Free…") | 한 화면에 두 언어 | Low | 검토대기 | 공지 바 `data-ko` 없이 하드코딩 19페이지(`categories/*.html` 10·brands/view·404·about·brand-guide·for-brands·privacy·privacy-en·submit-trend·terms). index.html 776행 양언어 마크업으로 통일 | 09-12 브론즈 R5 |
| A-018 | 2026-09-12 | 카테고리 페이지 메뉴 18곳 | 한국어로 바꾸고 카테고리 메뉴를 열었다 | "가구", "욕실" 한 번씩 | "가구가구", "욕실욕실" | 메뉴가 어색하다 | Low | 검토대기 | `<span data-ko="가구">Furniture</span><small>가구</small>` KO 전환 시 중복. 18페이지. index.html은 span에 data-ko 없음 — 한 방식으로 통일(미완료업무 #3과 같은 계열) | 09-12 브론즈 R5 · 09-15 브론즈 R20 재발견(`categories/wellness/saunas.html` 드롭다운, KO·EN 둘 다 재현) |
| A-019 | 2026-09-12 | 검색 등록(사이트맵) | 검색엔진이 사이트 지도를 읽는다 | 최신 기사까지 | 7/8 이후 기사 29편·브랜드 가이드가 없다 | 새 기사가 검색에 잘 안 잡힌다 | Low | 검토대기 | `sitemap.xml` lastmod 최신 06-29, 180건, 누락 31+. `scripts/make_sitemap.py` 재실행 필요 — 이 PC Python 없음 → Node 이식 별건 | 09-12 브론즈 R5 · 09-15 브론즈-B R1 재발견: 로컬 매거진 기사 116편 vs 라이브 `sitemap.xml` 매거진 항목 85건(`magazine/view.html` 라우터 제외 실 기사 84건), 누락 30편 전부 실제로는 라이브에서 200 정상 열림(글 자체는 있음, 목록에만 없음) — 아직 안 고쳐짐, 재발견만 기록 · 09-15 브론즈 R20 재발견: 07-03 이후 최근 4편 표본 4/4 전부 sitemap 누락, 격차가 더 벌어짐(07-03 이후 약 27편 이상 추정) |
| A-020 | 2026-09-12 | 브랜드 매거진 글 페이지 | 주소로 열었다 | 하단에 회사 정보 | 저작권 한 줄만 | 사업자 정보 표시가 빠진 화면 | Low | 검토대기 | `trend-report/view.html` 자체 푸터, `.footer-business-info`·auth-ui.js 없음. 표시 의무 여부는 노드-네이비 | 09-12 브론즈 R5 |
| A-021 | 2026-09-12 | 자문 요청(휴대폰) | 동의 체크박스를 누른다 | 누르기 편하다 | 아주 작다 | A-007 같은 유형 | Low | 검토대기 | `request-consultation.html` checkbox 13×13px | 09-12 브론즈 R6 |
| A-022 | 2026-09-12 | 어드민 대시보드 상단바 | (숨겨진) 아티클 링크에 마우스를 올린다 | 색이 바뀐다 | 색이 안 바뀌고 뒤에서 오류가 난다 | 1단계에서 숨겨 지금은 안 보이지만 코드가 남아 있다 | Low | 검토대기 | `admin/dashboard.html:153` `onmouseover="this.style.color=\'#C8A96E\'"` — 속성값 안 `\'`는 JS 문법 오류(도입 9cedb1c). 다른 페이지엔 없음. `\'` → `'` | 09-12 블랙(93e00be 검수) |
| A-023 | 2026-09-12 | 어드민 대시보드 | 목록 중 하나가 못 읽히는 상태(규칙 미게시 등)에서 대시보드를 연다 | 읽힌 것은 보이고 못 읽은 것만 표시된다 | "Loading…" 화면이 영원히 남는다 | 규칙 하나가 늦게 게시되면 어드민 전체가 멈춘 것처럼 보인다 | Medium | 검토대기 | `admin/dashboard.html:322-334` `loadAll()`이 `Promise.all`이라 하나라도 reject면 `loadingScreen` 미해제·`dash_render` 미호출(`loadBrands` try/catch 없음). `Promise.allSettled` + 실패 카드 표시로. 기존 구조 | 09-12 블랙(93e00be 검수) |
| A-024 | 2026-09-12 | 매거진 기사 1편 | 기사(oak-flooring)를 연다 | 조용히 뜬다 | 뜬다(문제 없음) | 정리 대상: 닫는 태그가 두 번 들어가 있다. 지금은 무해 | Low | 검토대기 | `magazine/oak-flooring-return-home-as-comfort-2026.html` 38·39행 `</style>` 중복. 브론즈가 백지 42번째로 잘못 센 파일 | 09-12 블랙(563f415 검수) |
| A-025 | 2026-09-12 | 매거진 기사 전체 | 기사를 다시 방문한다 | 최신 버전을 받는다 | 옛 버전이 남을 수 있다 | 캐시 표시가 빠진 공통 파일 2개 | Low | 검토대기 | 116건에서 `../auth-ui.js`·`../search.js`가 `?v=` 스탬프 없이 로드. 다른 공통 js는 `?v=20260630`. 통일 대상 | 09-12 블랙(563f415 검수) |

| A-026 | 2026-09-12 | 어드민 뉴스레터·디지털 알림 페이지 | 구독자 삭제 단추를 누른다 | 삭제 확인이 뜬다 | 이메일에 작은따옴표가 있으면 단추가 죽는다 | A-011 계열. 형식 검증이 없으면 문자열 탈출 여지 | Medium | 검토대기 | `admin/newsletter-subscribers.html:212`·`notify-subscribers.html:213` `onclick="deleteSubscriber('id','escapeAttr(email)')"` — HTML 엔티티가 파서에서 되돌려진 뒤 JS가 읽음. `data-id`/`data-email` + 이벤트 위임으로 | 09-12 블랙(d1b18fc 검수) |
| A-027 | 2026-09-12 | 브랜드 상세(공개) | 승인된 브랜드 페이지 데이터를 주소로 읽는다 | 공개 정보만 보인다 | 과거 정지·거절 사유와 어드민 이메일이 이력에 남아 누구나 읽을 수 있다 | 복구된 브랜드의 옛 사유가 공개되는 것은 새 노출. 화면엔 안 그리지만 데이터는 열려 있다 | Medium | 보류 | `firestore.rules:57-59` approved 브랜드 공개 read + `brands.history[].note/by`(2단계). 해결: history 를 별도 컬렉션(어드민 전용)으로 빼거나 note 를 공개 read에서 제외 불가 → 컬렉션 분리. **한울님 판단**(계약서 4-7대로 구현됨) | 09-12 블랙(d1b18fc 검수) |

| A-028 | 2026-09-12 | 어드민(비로그인) | 로그인 안 한 채 어드민 주소 두 개를 연다 | 같은 로그인 화면으로 간다 | 대시보드는 브랜드 포털 로그인으로, 자문 목록은 전문가 로그인으로 간다 | 어드민이 어느 로그인 화면을 써야 하는지 헷갈린다 | Low | 검토대기 | `admin/dashboard.html:326` → `brand-portal/login.html`(next 없음) / `admin/consultations.html:283` → `auth/login.html?next=`. 어드민 개편 8단계(흡수) 또는 core 공통 리다이렉트로 통일 | 09-12 브론즈 R7 |

| A-029 | 2026-09-12 | 어드민 활동 로그·설정·규정집 화면 | 언어를 바꾼다 | 안내 문장도 바뀐다 | 표·버튼은 바뀌는데 상태 안내 문장(“0건 표시 중” 등)만 이전 언어로 남는다 | 2·3단계 공통 패턴. 경미 | Low | 검토대기 | `view-logs.js:181`·`view-settings.js:160·168·222·226·240`·`view-regulations.js:74·88·111·115`·`view-users.js:181`·`admin-nav.js:349`(검색 바닥 줄)·`view-leads.js lead_openForm` 안내문 — `textContent`에 `t()` 결과만. `tSpan` 짝으로 일괄 정리 | 09-12 블랙(6cce1d4 검수) · 09-12 블랙(3bb936d) 위치 추가 |
| A-033 | 2026-09-12 | 어드민 전체(배포 운영) | 어드민 코드를 고쳐 올린다 | 방문자가 새 코드를 받는다 | 캐시 표시(`?v=`)를 안 올리면 옛 파일이 남아 새 함수를 못 찾고 화면이 멈출 수 있다 | 6단계에서 실제로 스탬프 없이 두 번 바뀜(5단계에서 20260912b로 갱신). 재발 방지 장치 필요 | Medium | 검토대기 | `tools/deploy.ps1`에 "올릴 커밋에 admin/js·admin.css 변경이 있는데 `?v=` 값이 안 바뀌면 경고" 추가, 또는 노드-블랙 항목 10 강화. 계약서 리스크 6 | 09-12 블랙(3bb936d 검수) |

| A-030 | 2026-09-12 | 브랜드 포털 제품·글 목록 | 브랜드가 자기 제품·글 목록을 본다 | 글자 그대로 보인다 | 제품명·설명에 HTML을 넣으면 그대로 실행된다 | 브랜드 본인 데이터라 자기 화면에만 영향(Low). 포털에 공통 이스케이프 함수가 없다 | Low | 검토대기 | `brand-portal/dashboard.html:1048-1057 loadProducts`·`1076-1084 loadArticles` `${p.name}`·`${p.imageUrl}`·`${a.title}`·`${a.content}` 미이스케이프. 4a에서 `inbox_esc` 신설 → 공용화 후 적용. A-003 계열 | 09-12 블랙(91aa61d 검수) |

| A-031 | 2026-09-12 | 브랜드 포털 로그인(한국어) | 한국어로 로그인 화면을 연다 | 입력칸 안내문도 한국어 | 라벨은 한국어인데 칸 안은 영어("name@brand.com"·"Enter your password") | 한 화면에 두 언어 | Low | 검토대기 | `brand-portal/login.html:92·96·116` placeholder 하드코딩, `data-ko-placeholder` 없음(A-017 계열) | 09-12 브론즈 R8 |
| A-032 | 2026-09-12 | 어드민 대시보드 | 페이지를 연다 | 외부 부품이 바뀌어도 화면이 눈치챈다 | 버전만 고정, 무결성 검증표 없음 | CDN 파일이 바뀌어도 그대로 실행(예방) | Low | 검토대기 | `admin/dashboard.html` cdnjs marked·dompurify·font-awesome에 `integrity`·`crossorigin` 없음. SRI 해시 추가 | 09-12 브론즈 R8 · 블랙 6cce1d4 INFO |

| A-034 | 2026-09-12 | 사이트 전 폼(입점·자문·투고·뉴스레터·포털 문의) | 빈칸 오류를 보고 5초 안에 다시 보내기를 누른다 | 오류 안내가 다시 뜬다 | 아무 일도 안 일어난다 | 재제출 차단이 검증 실패 제출까지 잠그고 안내가 없다. 방문자는 "안 눌리네"만 겪는다 | Medium | 검토대기 | `form-throttle.js:8-19` 캡처 단계 preventDefault+stopPropagation, 검증 실패 제출도 타임스탬프 기록. 검증 통과 후에만 기록하거나 잠긴 동안 안내 문구. 전 폼 공통 | 09-12 블랙(f1a00b2 검수) |

| A-035 | 2026-09-12 | 어드민 접수 > 자문 요청(별도 페이지 5개) | 한국어로 쓰던 어드민에서 사이드바 「자문 요청」을 눌렀다 | 한국어 그대로 이어진다 | 갑자기 영어 페이지로 바뀐다 | 한 어드민 안에서 언어가 오락가락한다. **한울님 요청: 어드민에 한/영 전환 버튼도 두기** | **High**(한울님 직접 발견) | 확인완료 | `admin/consultations.html`·`trend-submissions.html`·`newsletter-subscribers.html`·`notify-subscribers.html`·`articles.html`이 `lang.js`를 안 실어 `html lang="en"` 고정 → 사이드바 `t()`도 영어. 해결: ① 5페이지에 `../lang.js?v=` 로드 + `data-en/ko` 짝 보강 ② 사이드바 상단에 한/영 토글(`window.toggleLang`) ③ 근본은 계약서 8단계(별도 페이지 흡수). 블랙 93e00be INFO에서 예고됐던 건. **구현(09-12 화이트)**: ① 5페이지 `../lang.js?v=20260630` 을 `admin-nav.js` 뒤에 로드 + 상단 nav-link·Log Out·Authenticating 짝(본문 전수는 A-017·A-029 때) ② `admin-nav.js` `adm_langHtml/adm_switchLang` — 검색창 아래 KO|EN → `window.toggleLang()` + `adm_renderSidebar()` 재렌더(+활성·문의 배지 복원, aside/document 리스너 1회 가드) ③ `admin-core.js` `admInitLang()` — 저장값 en/ko 없으면 `archinode-lang`=ko 저장 + `<html lang>` 즉시 동기화(lang.js 무수정, IP 감지 안 탐) ④ `admin.css` `.adm-lang` + `.lang-switcher-floating` 숨김 ⑤ 스탬프 20260912d 26개 | 09-12 한울님 발견 · 지휘 창 등록 · 09-12 화이트 구현 → 확인대기(토글·유지·별도 페이지 진입은 로그인 실측 — 한울님 눈 또는 브론즈) · 09-15 브론즈 R17 실측: 어드민 열쇠 로그인 후 사이드바로 `consultations`→`trend-submissions`→`newsletter-subscribers`→`notify-subscribers`→`articles.html` 5페이지 순회, 매번 `html lang="ko"` 유지 + 사이드바 한국어 유지 확인. KO\|EN 토글 클릭 시 정상 전환(`html lang="en"` + 사이드바 영문화), 버튼 title "한국어"/"English" 확인 → **실측 확인**(단, 본문 영역·레거시 상단 탭바는 여전히 영어 고정 — 사이드바 언어 유지라는 이번 지시 범위는 충족, 화면 전체 완전 다국어화는 A-017·A-029 별건) |

| A-036 | 2026-09-12 | 공개 페이지(어드민을 연 브라우저) | 어드민을 한 번 열고 나서 홈을 연다 | 접속 나라에 따라 언어가 정해진다 | 항상 한국어로 시작한다 | 어드민 본인 브라우저에만 해당. 해외 방문자 시점 테스트(VPN)가 왜곡됨. 규정집 1-9 첫 문장과 미세하게 어긋남 | Low | 검토대기 | `admin/js/admin-core.js:36-46 admInitLang`이 저장값 없을 때 `archinode-lang=ko` 기록 → `lang.js:50` IP 조회 영구 스킵. 근본 해법: lang.js에 페이지 단위 IP 감지 opt-out 속성 1줄(공개 파일 → 승인 후). 부수: `search.js:143` 기본값 en vs lang.js 기본 ko 불일치(기존) | 09-12 블랙(ef420ac 검수) |

| A-037 | 2026-09-12 | 로그인·가입·프로필·비밀번호 재설정 페이지 | 검색엔진이 페이지를 읽는다 | 검색에 안 잡힌다 | 로봇 파일로만 막고 페이지 자체 표시는 없다 | 새 착륙 페이지만 noindex라 형제 4페이지와 불일치. 실해 낮음 | Low | 확인완료 | `auth/login·signup·profile·reset-password.html`에 `<meta name="robots" content="noindex,nofollow">` 없음(robots.txt는 `/auth/` Disallow). 공개 페이지 → 승인 후. **구현(09-14 화이트)**: 4파일 head에 `<meta name="robots" content="noindex,nofollow">` 한 줄씩 추가(token-entry.html과 동일 패턴). `git diff` 라인 수 파일당 +1로 다른 변경 없음 확인. 커밋 `95f9e3b`. node-black CRITICAL 0 | 09-12 블랙(9222947 검수) · 09-14 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 · 09-15 브론즈 R17 실측: `curl`로 라이브 4개 파일 head 확인, `auth/login·signup·profile·reset-password.html` 전부 `<meta name="robots" content="noindex,nofollow">` 존재 → **실측 확인** |

| A-038 | 2026-09-12 | 어드민 상단바(좁은 화면 322px) | 창을 좁혀 어드민을 본다 | 단추가 한 줄로 정돈된다 | "브랜드 로그인"·"로그아웃" 글자가 세로로 끊겨 화면 위로 삐져나간다 | 휴대폰 폭에서 어드민 상단이 깨짐. A-014(공개 페이지)와 같은 원인 가능성 | Medium | 확인완료 | 탭 A-012. `getBoundingClientRect` width 12~52px·top 음수. `.topbar .right` 요소 `white-space:nowrap`·모바일 미디어쿼리 없음. **구현(09-14 화이트)**: `admin/dashboard.html` 기존 768px 미디어쿼리 블록(다른 작업자가 만지는 636줄 근처 Referral 모달과는 무관한 144줄 부근)에 `.topbar`/`.topbar .right`/`.topbar .right > *`/`.topbar .admin-email` nowrap+flex-wrap+ellipsis 7줄 추가. 로그인 화면이라 화이트는 마크업 주입(정적 파일 `_qa_topbar_test.html`에 topbar 마크업+admin.css+같은 미디어쿼리 복제)으로 322px 렌더만 확인 — 실측(주입). 커밋 `95f9e3b`. node-black CRITICAL 0(636줄 비침범 diff로 확인). **R13 실브라우저 실측(09-14 브론즈)**: 어드민 열쇠 로그인 상태에서 322px(스크린샷 644×1400 = 2×322×2×700 확인)로 축소. "브랜드 로그인"·"Log Out"이 완전한 단어 단위로 줄바꿈(3줄)되어 표시, 한 글자씩 쪼개지지 않고 화면 오른쪽 밖으로도 안 나감(Log Out 우측 끝 ≈305px < 322px) | 09-12 브론즈 R11(탭 등록) · 09-14 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 · 09-14 브론즈 R13 실제 로그인 화면 322px 실측 통과 → 확인완료 |
| A-039 | 2026-09-12 | 브랜드 포털 탭바(좁은 화면) | 창을 좁혀 포털 탭을 누른다 | 탭 글자가 읽힌다 | "제품"·"브랜드 매거진" 글자가 세로로 쪼개진다 | 정확히 누르기 어렵다 | Medium | 확인완료 | 탭 A-013. 포털 `.tabs` 탭이 `flex` 고정 폭, 좁은 폭 규칙 없음. **구현(09-14 화이트)**: `brand-portal/dashboard.html` 기존 768px 미디어쿼리 블록에 `.tabs`(overflow-x:auto)·`.tab`(white-space:nowrap) 추가. 로그인 화면이라 마크업 주입으로 375px 렌더 확인 — 실측(주입). 커밋 `95f9e3b`. node-black CRITICAL 0. **R13 실브라우저 실측(09-14 브론즈)**: 브랜드 열쇠(qa-brand@archinodekr.com) 로그인, 322px에서 탭 6개(브랜드 정보·제품·브랜드 매거진·인박스·문의·설정) 전부 `white-space:nowrap`, `.tabs` 내부만 `overflow-x:auto`(scrollWidth 529>clientWidth 274)로 가로 스크롤, 페이지 자체는 가로 스크롤 없음(scrollWidth===innerWidth). **단, 같은 화면 상단의 `.topbar`("Brand Guide"·"로그아웃")는 이 수정 대상이 아니라서 여전히 글자가 세로로 쪼개짐 — 별도로 A-049 새로 등록(탭 A-016)** | 09-12 브론즈 R11 · 09-14 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 · 09-14 브론즈 R13 실제 로그인 화면 322px 실측 통과(탭바만) → 확인완료, 상단바 잔여 결함은 A-049로 분리 등록 |
| A-041 | 2026-09-12 | 어드민 통합 검색 | "테스트"를 검색한다 | 공지·문의도 나온다 | 브랜드·리드·회원·검수만 나온다 | 6단계 검색 범위가 5단계 공지·문의를 안 담음(5단계 화이트가 범위 밖으로 남긴 것) | Low | 확인완료 | 탭 A-015. `admin-core.js ADM_SEARCH_SOURCES`에 notices·inquiries 추가 + `admRegisterCache` 호출. **구현(09-12 화이트)**: `ADM_SEARCH_SOURCES` 에 notices·inquiries 추가. **재수정(09-12 화이트, R12)**: 문의 선로드(`inq_ensureLoaded()`)를 `view-dashboard.js DASH_CARDS` 문의 카드 loader 에 연결해 로그인 시 1회 읽음. **R13 재확인(09-14 브론즈)**: 어드민 열쇠로 로그인한 뒤 1:1 문의 탭을 **열지 않고** 검색창에 "테스트" 입력 → `admSearchResults`에 "문의 [테스트-브론즈] R11 문의" 결과가 정상 표시됨(8건 중 하나, 하단에 "아직 안 연 화면: 리드, 공지"라는 안내가 별도로 있음 — 의도된 동작). 대시보드 "열린 문의" 카드 0건은 실제 문의 목록(1건, 상태 "답변됨")과 일치 확인 | 09-12 브론즈 R11 · 09-12 화이트 구현 → 확인대기 · 09-12 브론즈 R12 재수정요청(문의 미검색) · 09-12 화이트 선로드 재수정 → 확인대기 · 09-14 브론즈 R13 재확인 통과(문의 탭 열지 않고 검색됨, 배지=목록 수 일치) → 확인완료 |

| A-043 | 2026-09-12 | 어드민 대시보드(콘솔) | 대시보드를 연다 | 뒤에서 오류가 없다 | 색인 오류 4건이 상시로 뜬다(브론즈 R12 관찰) | 어떤 조회인지 미확인. 어드민 코드에는 where+orderBy 복합 조회가 없어(지휘 창 grep) 포털 탭 잔류 오류일 가능성 있음 | Medium | 검토대기 | 다음 브론즈 라운드에서 **오류 메시지 안의 색인 생성 링크(필드 목록)를 그대로 기록**할 것. 어드민 단독 새 탭에서 재현되는지. 재현되면 `firestore.indexes.json`에 추가·배포. **R13 시도(09-14 브론즈)**: 어드민 열쇠로 새로 로그인해 `admin/dashboard.html`을 여러 번 재로드하고 대시보드·리드 인박스·1:1 문의·전문가 회원·검수관리 탭을 차례로 열었으나 콘솔에 어떤 오류도 안 떴다(직접 넣은 테스트용 오류 문구만 보임). `admin/consultations.html`·`trend-submissions.html`·`newsletter-subscribers.html`·`notify-subscribers.html` 별도 페이지도 각각 열어 확인했으나 마찬가지로 오류 없음. **이번 라운드에서는 재현 안 됨** — R12 관찰 당시의 특정 상태(다른 탭 잔류, 최초 로그인 직후 등)에서만 뜨는 것일 수 있어 완전히 해소됐다고 단정하지 않는다 | 09-12 브론즈 R12 관찰 · 지휘 창 등록 · 09-14 브론즈 R13 재현 시도 — 여러 화면에서 재현 안 됨(상태 변경 안 함) · 09-15 브론즈 R15 재시도 — 대시보드·리드 인박스·1:1 문의·전문가 회원·검수관리 탭 + 별도 페이지 4종(consultations·trend-submissions·newsletter-subscribers·notify-subscribers) 전부 콘솔 오류 0건, 2연속 재현 안 됨(상태 변경 안 함) |

| A-044 | 2026-09-12 | 어드민 1:1 문의 배지·카드 | 문의 화면에서 「새로고침」을 누른다 | 숫자가 그대로 있다가 갱신된다 | 읽는 동안 잠깐 0으로 깜빡인다 | 데이터 손실 없음, 깜빡임뿐 | Low | 검토대기 | `view-inquiries.js:90·115` 새로고침이 `items=[]`·`openCount=null`로 비운 뒤 `loaded`가 true라 `inq_ensureLoaded`가 빈 배열을 셈. `loading` 중엔 push 건너뛰기 한 줄 | 09-12 블랙(93bcf5f 검수) · 09-15 브론즈 R15 실측 — 브랜드 테스트 계정으로 미답변 문의 1건을 새로 만들어 열린 문의 카운트를 1로 만든 뒤 「새로고침」 2회를 10ms·40ms 간격으로 총 709회 폴링, `dashNum-inquiries` 값이 "1"에서 "0"·빈 값으로 바뀌는 순간 관측 안 됨 — 이번 라운드에서는 재현 안 됨(초고속 새로고침 시 한 프레임짜리 찰나의 깜빡임은 폴링 간격상 못 잡았을 가능성 있음, 상태 변경 안 함) |
| A-045 | 2026-09-14 | 카테고리 서브페이지(예: `categories/furniture/sofas-armchairs.html`) | 페이지를 연다 | 검색·즐겨찾기 등 하단 스크립트가 정상 동작한다 | 콘솔에 `Uncaught SyntaxError: Identifier 'header' has already been declared` | 같은 페이지 안에 `header`가 두 번(245줄 `const`, 283줄 `var`로 추정) 선언돼 두 번째 인라인 `<script>` 블록 전체(토글·즐겨찾기 핸들러 포함)가 파싱조차 안 되고 죽는다 | Medium | 검토대기 | BUG-1(A-001) 수정 중 node-white·node-black이 로컬 실측(정적 서버+브라우저 콘솔)으로 발견. 이번 수정과 무관한 기존 결함, 고치지 않음. 다른 카테고리 서브페이지에도 같은 패턴이 있는지는 확인 못 함(이 1개 파일만 실측) | 09-14 node-white·node-black 발견(BUG-1 검증 중), 지휘 창 등록 · 09-15 노드-브론즈 재확인: `categories/*/*.html` 69개 서브페이지 전부(sitemap.xml 기준) 정적 코드조회 — 245줄 `const header =` · 283줄 `var header =` 중복 선언 패턴이 69/69 전부에서 확인됨(1개 파일만의 문제 아님, 전체 카테고리 서브페이지 공통 결함). `categories/bathroom/bathroom-accessories.html` 실브라우저 로드로 콘솔 오류 재현도 확인 |
| A-046 | 2026-09-14 | 카테고리 서브페이지(예: `categories/furniture/sofas-armchairs.html`) | 페이지를 연다 | 그 카테고리 제품 목록이 뜬다 | 콘솔에 `FirebaseError: The query requires an index`(failed-precondition), 제품 목록이 비어 보일 수 있음 | `category-products.js`로 추정되는 공유 스크립트의 products 조회(카테고리 필드 + `orderBy createdAt`으로 추정)에 복합 색인이 없음. A-040(브랜드 포털 제품·글 목록)과 같은 패턴이지만 다른 조회 — A-040 색인 배포로는 안 풀림 | High | 진행중 | BUG-1(A-001) 수정 중 node-black이 로컬 실측으로 발견. 정확한 조회 코드 위치(`category-products.js` 내부)는 열어보지 않음 — 확인 못 함. 재현되면 `firestore.indexes.json`에 products(category ASC + createdAt DESC 추정) 추가 후 `deploy --only firestore:indexes` | 09-14 node-black 발견(BUG-1 검증 중), 지휘 창 등록 · 09-15 브론즈 R16 실측: `sofas-armchairs.html`·`pendant-lights.html` 2개 서브페이지 콘솔에서 동일 `failed-precondition` 재현. 소스 확인(`category-products.js:41-49`) — 실제 조회는 `products.where(status=='approved').where(category==...).[where(subcategory==...)].orderBy(createdAt,desc)`. sofas-armchairs.html은 `data-subcategory="sofas-armchairs"`를 갖고 있어 4필드 복합색인(status ASC, category ASC, subcategory ASC, createdAt DESC) 필요로 추정 — 기존 개발 메모의 "category+createdAt만" 추정보다 한 단계 더 필요. **재현 확인**. 09-15 지휘 창이 `firestore.indexes.json`에 4필드 복합색인(status ASC, category ASC, subcategory ASC, createdAt DESC) 추가 후 `firebase deploy --only firestore:indexes` 게시 완료(office@archinode.org CLI 재인증 필요했음, 한울님이 재인증). 게시 직후 실측(`sofas-armchairs.html`, Browser pane 직접 쿼리)에서는 아직 "index is currently building" — Firestore 색인 빌드 대기 중, 자동 완료 예정 | 09-14 node-black 발견(BUG-1 검증 중), 지휘 창 등록 · 09-15 브론즈 R16 재현 확인(위) · 09-15 브론즈 R20 재발견(카테고리 서브페이지 5개 표본 5/5) · 09-15 지휘 창이 색인 배포(진행중 — 빌드 완료 후 재확인 필요) · 09-15 브론즈-B2 R2 재실측: `bathroom-accessories.html` 콘솔에서 `FirebaseError: requires an index` **더 이상 안 남**(색인 빌드 완료로 보임), 화면 문구도 "Unable to load products."(오류)에서 "No products listed yet..."(정상 빈 상태 안내)로 바뀜 — **색인 문제 자체는 해소된 것으로 보임(실측 1개 페이지, 나머지 68개는 코드 동일이라 추정, 전수 재확인은 다음 라운드 권장)**. 다만 그 빈 상태 안내 문구가 영어로만 뜨는 별도 문제를 발견해 A-062로 새로 등록(이 번호와는 다른 결함) · 09-15 브론즈-B2 R4~R5 표본 대폭 확대: 10개 대분류(bathroom·furniture·outdoor·lighting·kitchen·office·finishes·wellness·decor·construction) 전부에서 각 1개, 이어서 각 대분류 2개씩 추가로 20개 더 — **총 30개 서브페이지 전수 실측, 30/30 전부 색인 오류 없음**(header 중복선언 A-045는 30/30 전부 그대로 재현 — 별개 결함이라 정상). **판정 제안: 색인 문제는 해소로 봐도 될 신뢰도(69개 중 30개 무작위성 없는 각 대분류 균등 표본, 실패 0건)** — 최종 확인완료 판정은 한울님/지휘 창 몫으로 남김 · 09-15 브론즈-B2 R6: 나머지 39개까지 마저 실측해 **69개 카테고리 서브페이지 전수 완료 — 69/69 전부 색인 오류 없음**. **확인완료로 판정해도 되는 수준의 전수 검증 완료** — 최종 판정은 한울님/지휘 창 몫 |
| A-047 | 2026-09-14 | 공개 페이지 약 70개(카테고리 서브페이지 다수 — `categories/bathroom/*`·`construction/*`·`kitchen/*`·`lighting/*`·`furniture/*` 등) | 로그인한 뒤 그 페이지를 연다 | 헤더 우측에 로그인 상태(대시보드/마이페이지/Admin 버튼)가 뜬다 | 로그인 여부와 무관하게 헤더가 항상 비로그인 그대로다(콘솔 오류는 없음 — 조용한 실패) | 해당 페이지들이 `auth-ui.js`를 `firebase-config.js`보다 먼저 로드한다(auth-ui.js 자신의 5줄 주석과 반대 순서). auth-ui.js의 IIFE가 실행되는 시점엔 `auth`가 아직 어디서도 선언되지 않아 `typeof auth === 'undefined'` 가드가 항상 참이 되어 조용히 반환 — BUG-1(A-001)과 무관하게 이전부터 있던 순서 결함, 이번 수정이 만든 게 아님 | Medium | 검토대기 | node-black 코드검토(스크립트 태그 줄번호 비교, 총 80개 동시 로드 페이지 중 70개가 역순 — CLAUDE.md 4절 "스크립트 로드 순서" 위반) + `categories/furniture/sofas-armchairs.html` 실측(`.header-actions`에 `.auth-link` 미생성) 확인. 대조군 `brands.html`(정상 순서, auth-compat 있음)은 "Log In" 정상 렌더. 70개 목록 전수는 아직 확인 못 함(대표 1개만 실측), 고치지 않음 | 09-14 node-black 발견(BUG-1 검증 중), 지휘 창 등록 · 09-15 브론즈 R16 실측: `qa-pro@archinodekr.com`(전문가 테스트 계정) 로그인 상태로 `categories/furniture/sofas-armchairs.html`·`categories/lighting/pendant-lights.html` 2개 서브페이지 확인, 둘 다 "브랜드 로그인"만 뜨고 로그인 반영 안 됨(재현). 대조군 `brands.html`은 같은 로그인 세션으로 "My Page" 정상 렌더. 추가 원인 확인: 카테고리 서브페이지는 순서 문제 이전에 **`firebase-auth-compat.js` 스크립트 자체가 누락**돼 있음(`<script>` 목록에 app-compat·firestore-compat만 있고 auth-compat 없음 — `window.firebase`에 `auth` 키 자체가 없어 `firebase.auth`가 함수가 아님, CLAUDE.md 8절 "auth-compat 없는 페이지" 함정과 동일 계열). brands.html은 auth-compat 포함 + 순서도 정상. 즉 순서만 고쳐서는 안 풀리고 auth-compat 스크립트 추가가 먼저 필요 — **재현 확인, 원인 한 가지 추가** | 09-14 node-black 발견(BUG-1 검증 중), 지휘 창 등록 · 09-15 브론즈-B R2: 정적 코드조회로 표본을 10개(카테고리별 1개씩, 전 10개 대분류 커버)로 넓혀 두 원인 모두 재확인, 이어서 sitemap.xml 기준 카테고리 서브페이지 69개 **전수** 정적 스캔 — `firebase-auth-compat.js` 누락 69/69, `auth-ui.js`가 `firebase-config.js`보다 먼저 로드 69/69, 둘 다 예외 없이 전부. "약 70개"였던 범위가 카테고리 서브페이지 한정으로는 **69개 확정**. 별도로 상위 카테고리 페이지 10개(`categories/furniture.html` 등 서브폴더 아닌 것)도 점검 — 이쪽은 아예 Firebase 스크립트 자체가 하나도 없어(app-compat도 없음) 로그인 상태가 절대 안 뜨는 건 같지만, auth-ui.js 4번째 줄 가드(`typeof auth === 'undefined'`)가 조용히 막아줘서 콘솔 오류는 없음 — 같은 증상(로그인 헤더 미반영)의 별도 발생 지점으로 참고 기록만, 이 결함 범위(69개)에는 안 넣음 |
| A-048 | 2026-09-14 | `privacy-en.html`(휴대폰 375px) | 휴대폰으로 연다 | style.css 모바일 헤더 수정(A-014)이 다른 공개 페이지처럼 적용돼 상단이 한 줄로 정돈된다 | 코드검토 결과 이 페이지도 index.html처럼 자체 인라인 `<style>`에 별도 미디어쿼리를 갖고 있어 style.css 수정이 안 먹힐 가능성이 높다(index.html이 그래서 따로 고쳐야 했던 것과 같은 패턴) | A-014를 고치면서 같이 안 잡혔을 수 있는 페이지 — 실측은 안 함 | Low | 오탐반려 | node-black이 코드검토만으로 지적(실측 안 함). index.html 사례(인라인 style 별도 미디어쿼리)와 동일 원인 추정. 375px 실측 후 필요하면 index.html과 같은 패턴으로 수정 | 09-14 node-black 발견(모바일 수정 검증 중), 지휘 창 등록 · 09-15 브론즈 R16 실측: 375px에서 `privacy-en.html` 실측, `document.documentElement.scrollWidth`=375=`clientWidth`(가로 스크롤 없음), 헤더 로고·검색·북마크·KR/EN·햄버거 한 줄 정돈 확인. 코드 확인 결과 이 페이지 인라인 `@media (max-width:768px)`(399행)는 `.header-actions`·`.logo-sub`를 건드리지 않아 style.css의 A-014 수정과 충돌하지 않음 — 우려했던 index.html과 같은 충돌 패턴은 이 페이지에는 없음 — **재현 안 됨(정상)** |
| A-050 | 2026-09-14 | 브랜드 포털 상단바 「Brand Guide」 링크 | 새 탭으로 브랜드 가이드를 연다 | 새 탭이 원래 탭과 분리되어 열린다 | 새로 연 탭(브랜드 가이드)이 `window.opener`로 원래 탭(브랜드 포털)을 조작할 수 있는 상태로 열린다 | 실해는 낮음(같은 사이트 내부 링크), 다만 보안 모범사례 위반. 방문자가 알아채는 문제는 아님 | Low | 검토대기 | `brand-portal/dashboard.html:199` `<a href="../brand-guide.html" target="_blank" ...>`에 `rel="noopener"` 없음. A-049 수정과 무관한 기존 결함, 이번엔 고치지 않음 | 09-14 node-black 발견(A-049 검증 중), 지휘 창 등록 |
| A-051 | 2026-09-15 | 어드민 사이드바 KO\|EN 언어 토글(첫 화면) | 어드민에 로그인해 처음 화면을 본다 | 검색창 아래 "KO \| EN" 두 글자 토글이 보인다 | 처음엔 "Korean \| English"로 풀어져 보이다가, 토글을 한 번 누르면 그제서야 "KO \| EN"으로 정상으로 바뀐다 | 첫인상이 어색하고, 누르기 전까지 버튼처럼 안 보여 헷갈릴 수 있다. 기능(언어 전환) 자체는 정상 | Low | 확인완료 | `admin/js/admin-nav.js:225,227` `adm_langHtml()`이 KO/EN 버튼에 `tAttr('Korean','한국어')`·`tAttr('English','영어')`를 붙였는데, `tAttr()`(`admin-core.js:29`)는 `data-en`/`data-ko` 속성을 반환한다 — 이 속성은 사이트 공통 `lang.js`의 `applyLanguage()`(161줄)가 "번역해야 할 글자"로 인식해 요소의 텍스트를 통째로 덮어쓰는 바로 그 표시라, 버튼 고정 글자 "KO"/"EN"이 `lang.js` 전역 스캔에 걸려 "Korean"/"English"로 바뀌는 것. 토글 클릭 시 `adm_switchLang()`이 `adm_renderSidebar()`로 사이드바를 통째로 다시 그려 고정 텍스트가 복원되므로 그 뒤엔 정상으로 보인다. 해결: 이 두 버튼엔 `data-en`/`data-ko`를 쓰지 말고(고정 두 글자라 번역 대상이 아님) 필요하면 `title=` 등 별도 속성으로. **구현(09-15 화이트)**: `admin/js/admin-nav.js` `adm_langHtml()`(222~231줄)에서 두 `tAttr(...)` 호출을 제거하고 `title="한국어"`/`title="English"`로 대체(+6/-2줄, `<span class="adm-lang-sep">` 그대로). 로그인 화면이라 화이트는 `lang.js`·`admin-core.js` 관련 함수를 정적 파일로 복제해 로컬 서버(포트 8100)에 올려 **실측(주입)**: 수정 전 코드(`tAttr` 그대로)는 `window.toggleLang()` 1회차부터 버튼 텍스트가 `["Korean","English"]`로 깨짐 재현 확인, 수정 후 코드는 `toggleLang()` 4회 연속 호출해도 매 회차 `["KO","EN"]` 유지 확인(재현 시나리오로 원인·해결 둘 다 검증). node-black 검수 CRITICAL 0(`adm_initLangToggle`·`adm_switchLang`의 `data-lang`·클릭 핸들러·active 토글 로직 무영향 확인, 같은 오적용 패턴이 다른 admin/js 파일에 없는지 전수 grep도 재발 없음 확인). 커밋 `7a5a27b`. 실브라우저 로그인 상태에서 첫 화면 표시·호버 툴팁 확인은 브론즈 몫(못 함) | 09-15 지휘 창(한울님 스크린샷 제보) 발견·등록 → 09-15 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 → 09-15 노드-브론즈 R14 실측: 커밋 `7a5a27b` 라이브 배포 확인(파일 diff 0) · 열쇠 로그인(wool21wool@gmail.com) 첫 화면 "KO \| EN" 정상 · 새로고침 4회 연속 유지(재현 안 됨) · 토글 클릭 시 활성 표시 정상 전환 · 호버 title 속성 "한국어"/"English" 확인 → **확인완료** |
| A-052 | 2026-09-15 | 어드민 규정집/설정 등 본문 화면(넓은 모니터) | 규정집 화면을 넓은 화면에서 연다 | 화면 폭을 알맞게 채워서 보인다 | 본문 영역이 1200px에서 멈추고 양옆에 넓은 빈 회색 여백이 남는다 | 넓은 모니터일수록 어색하고 좁아 보인다. 방문자 화면 아님(어드민 전용) | Low | 확인완료 | `admin/dashboard.html:42` `.admin { max-width: 1200px; margin: 0 auto; }` — 공개 사이트(`style.css`)는 1440px을 쓰는데 어드민만 1200px로 좁게 잡혀 있음. **구현(09-15 화이트)**: `1440px`로 변경(margin/padding 불변). node-black CRITICAL 0(변경 범위 diff 정확 일치 확인) | 09-15 한울님 스크린샷 제보·지휘 창 등록 → 09-15 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 → 09-15 노드-브론즈 R15 실측: 커밋 `e47f4b4` 라이브 확인, 1920px 뷰포트에서 `.admin` 실측 폭 1440px(계산값 일치) — **정상 확인, 확인완료 판정 가능** |
| A-053 | 2026-09-15 | 어드민 사이드바(내용 긴 화면에서 아래로 스크롤) | 오른쪽 본문이 긴 화면(예: 규정집)에서 아래로 스크롤한다 | 사이드바도 같이 스크롤되거나, 화면에 붙어 있어도 메뉴 전체가 다 보인다 | 사이드바는 화면에 붙어(sticky) 있는데 사이드바 자신은 스크롤이 안 돼서, 메뉴가 길면 아래쪽 항목(예: 설정·앱으로 설치 버튼)이 화면 밖으로 잘려 영영 안 보인다 | 사이드바 아래쪽 메뉴를 못 누른다 | Medium | 확인대기 | `admin/admin.css:12` `#adminSidebar { position: sticky; top: 72px; }`에 자체 높이 제한·스크롤이 없음. 같은 파일 154줄 `.adm-reg-toc`(규정집 목차)는 이미 `max-height: calc(100vh - 100px); overflow-y: auto;`로 같은 문제를 제대로 처리해뒀음. **구현(09-15 화이트)**: `#adminSidebar`에 같은 `max-height: calc(100vh - 100px); overflow-y: auto;` 추가(`position: sticky; top: 72px;`는 유지). node-black CRITICAL 0 — WARNING 1건(모바일 768px 미디어쿼리에서 `max-height` 리셋 누락, `.adm-reg-toc`는 `max-height: none` 처리돼 있는데 이건 안 됨 — 현재 메뉴 수로는 발생 가능성 낮다고 판단, A-055로 별도 등록) | 09-15 한울님 제보·지휘 창 등록 → 09-15 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0(WARNING 1건 A-055 분리) 확인 → 확인대기 → 09-15 노드-브론즈 R15 실측(부분 확인): `sidebar.scrollTop`을 JS로 직접 바꾸면 정상 스크롤됨(scrollHeight 910/clientHeight 500, 아래쪽 메뉴 정상 노출) — CSS 자체는 라이브에 정상 반영. 그러나 실제 마우스 휠로 사이드바 위를 스크롤하면 사이드바가 아니라 페이지 전체가 스크롤되고 사이드바는 안 움직임(자동화 도구로 재현 2회, 좌표 정확도는 별도 검증됨) — 자동화 도구 한계인지 실제 브라우저 동작인지 구분 못 해 **확인완료 판정 보류 권장**, 검수관리 탭에 A-017로 별도 등록(사람 손 재확인 요청) · 09-15 브론즈 R E2 참고: 이번 라운드는 A-053(데스크톱 세로 축소·마우스 휠) 자체는 재검증하지 않았으나, 별개 증상인 A-055(모바일 폭 펼침 메뉴)를 실측해 "실제로 막혀 있는 결함"으로 확인함(검수관리 A-040) — 둘 다 사이드바 스크롤 처리 미비라는 같은 계열이지만 뷰포트·메커니즘이 달라 별도 항목 유지 |
| A-054 | 2026-09-15 | 검수관리 목록의 "상태" 칸(전 화면 공통 — 브랜드·제품·아티클·검수관리) | 검수관리(또는 브랜드) 목록을 본다 | "검토대기"처럼 4~5글자짜리 상태 표시가 알약 모양 배지 안에 가로로 한 줄로 보인다 | 배지가 좁아서 "검", "토", "대", "기"가 한 글자씩 세로로 쌓여 기둥처럼 보인다 | 상태를 한눈에 읽기 어렵고 화면이 지저분해 보인다 | Medium | 확인완료 | `admin/dashboard.html:68` `.status { display: inline-block; padding: 3px 10px; ... }`에 `white-space: nowrap`이 없음. 영어 단어(PENDING 등) 기준으로 만들어져 있어 짧은 글자에선 안 드러났지만, 한글 검수 상태("검토대기"·"확인완료"·"재수정요청")처럼 긴 글자가 좁은 표 칸 안에 들어가면 글자 단위로 줄바꿈된다. **구현(09-15 화이트)**: `.status`에 `white-space: nowrap;` 한 줄 추가 — 브랜드·제품·아티클·검수관리 배지 전부 공유하는 클래스라 한 번에 해결. node-black CRITICAL 0(표 셀·목록 다른 요소 침범 없음 코드검토 확인) | 09-15 한울님 스크린샷 제보·지휘 창 등록 → 09-15 지휘 창 직접 호출 node-white 수정 + node-black CRITICAL 0 확인 → 확인대기 → 09-15 노드-브론즈 R15 실측: 검수관리 탭 "검토대기"·"보류" 배지 24건 전부 height 20~22px 한 줄, 세로 쌓임 없음 — **정상 확인, 확인완료 판정 가능** |
| A-055 | 2026-09-15 | 어드민 사이드바(휴대폰 폭, 메뉴 펼침 상태) | A-053 수정 뒤 휴대폰 폭에서 사이드바를 펼친다 | 메뉴가 많아져도 스크롤로 다 볼 수 있다 | 모바일 전용 규칙(768px 이하)이 `max-height`를 다시 풀어주지 않아서, 지금 당장은 메뉴 수가 적어 안 보이지만 나중에 메뉴가 늘면 아래쪽이 또 잘릴 수 있다 | 지금은 실해 없음(메뉴 수 기준), 잠재적 재발 위험만 → **09-15 R E2 재검증 결과 평가 상향: "잠재적 위험"이 아니라 지금 당장 실제로 막혀 있는 결함**(아래 이력 참고) | Low → High(실측 근거) | 검토대기 | `admin/admin.css` 71~76줄 모바일 미디어쿼리가 `#adminSidebar`를 `position: static`으로 바꾸면서 A-053에서 추가한 `max-height: calc(100vh - 100px)`를 안 풂. 같은 파일 `.adm-reg-toc`는 모바일 블록에서 `max-height: none;`으로 명시 리셋함 — 같은 처방을 `#adminSidebar` 모바일 블록에도 추가하면 됨 | 09-15 node-black 발견(A-052~054 검수 중), 지휘 창 등록 · 09-15 브론즈 R E2 실측(375×700 뷰포트, 실사이트, 어드민 로그인 상태): 메뉴 펼친 뒤 `sidebar.scrollHeight`=867 / `clientHeight`=600(=100vh-100px) / `overflow`=hidden → 267px 분량이 스크롤도 페이지 확대도 안 통하고 영구히 가려짐. 각 메뉴 항목 좌표를 JS로 측정해 "Articles(Cards)"·"Notices"·"Activity Log"·"Review Board"·"Regulations"·"Settings" 6개 항목이 전부 `visible:false` 확인 — 즉 휴대폰으로 어드민에 들어오면 공지사항·활동로그·검수관리·규정집·설정에 아예 진입할 방법이 없음(뷰포트가 800px로 커도 max-height=700 vs 필요 867이라 대부분의 휴대폰에서 상시 재현). "메뉴 수가 적어 안 보인다"던 원래 평가는 낡은 것으로 판단, 검수관리 탭에 A-040으로 신규 등록(실측 근거 포함, 사람 판정 요청) |
| A-049 | 2026-09-14 | 브랜드 포털 상단바(좁은 화면 322px) | 브랜드 계정으로 로그인한 뒤 화면을 휴대폰 정도로 좁혔다 | 화면 위쪽 검은 띠에 "Brand Guide"·"로그아웃" 글자가 작아지더라도 한 줄로는 보일 것 | "Brand Guide"·"로그아웃" 글자가 한 글자씩 세로로 쌓여 좁은 기둥처럼 보였고, 위쪽 절반은 화면 위로 잘려서 로고와 겹쳐 보이기까지 했다 | 휴대폰으로 브랜드 포털에 들어온 담당자가 Brand Guide 링크나 로그아웃 버튼을 찾기 어렵고 정확히 누르기도 어렵다. 어드민 상단바(A-038, 확인완료)와 같은 종류의 문제인데 브랜드 포털 쪽은 이번 수정(95f9e3b) 대상이 아니어서 아직 안 고쳐진 상태 | Medium | 확인완료 | 탭 A-016. `brand-portal/dashboard.html:25` `.topbar{display:flex;justify-content:space-between}` flex-wrap 없음, `:28 .user-info{display:flex;gap:16px}`도 wrap 없음, `:199` Brand Guide 링크·`:201` 로그아웃 버튼에 `white-space:nowrap` 없음. `:183` 768px 미디어쿼리는 `.tabs`/`.tab`만 다루고(A-039 수정분) `.topbar`는 대상 밖. 실측(322px): "Brand Guide" rect width 40.8/height 143.5(정상은 20여px 높이), top -43.75(화면 위로 넘침) — 로그아웃도 동일 패턴. A-038 수정과 같은 방식(nowrap+flex-wrap 추가)으로 고치면 됨. 콘솔 오류 없음(레이아웃만 깨짐). **구현(09-14 화이트, 한울님 부재중 지휘 창 직접 호출)**: `brand-portal/dashboard.html` 기존 768px 미디어쿼리 블록(A-039가 쓴 자리)에 `.topbar`/`.topbar .user-info`/`.topbar .user-info > *`/`.topbar .user-email` 4규칙 6줄 추가(admin/dashboard.html A-038 처방을 `.right`→`.user-info`, `.admin-email`→`.user-email`로 이 파일 클래스명에 맞게 이식). diff는 그 6줄만(`git diff --stat` +6/-0), 파일 잘림 없음(1489→1495줄). 로그인 화면이라 화이트는 마크업+CSS를 정적 파일로 복제해 로컬 static 서버(포트 8099)로 322px 렌더 확인 — **실측(주입)**: "Brand Guide" rect width 122/height 30/top 37, "Log Out" width 93/height 29/top 73 — 둘 다 완전한 단어로 한 줄, `right` 306px < `pageW` 322px(화면 밖으로 안 나감), 기존 결함의 height 143.5·top -43.75 패턴 재현 안 됨. node-black 검수 CRITICAL 0(diff 6줄 범위 확인, 인라인 스크립트 파싱 정상, 클래스명 실존 대조로 오탈자 없음 확인 — WARNING·CRITICAL 없음, INFO 1건은 A-049와 무관한 기존 결함이라 별도 등록 권장). 커밋 `c862bac`. 실브라우저 로그인 상태 322px 확인은 브론즈 몫(못 함) | 09-14 브론즈 R13 발견·탭 A-016 등록 · 09-14 지휘 창 직접 호출 node-white 수정(실측 주입) + node-black CRITICAL 0 확인 → 확인대기 · 09-15 브론즈 R17 실측: qa-brand 로그인 상태에서 322px로 축소, "브랜드 가이드"(w116.5·h33·top37·right306)·"로그아웃"(w91.9·h32·top76·right306) 모두 완전한 단어로 한 줄, 화면 위로 안 잘리고 우측도 페이지 폭(322px) 안, 페이지 자체 가로 스크롤 없음(scrollWidth=clientWidth=322) → **실측 확인** |
| A-056 | 2026-09-15 | 비밀번호 재설정 화면(`auth/reset-password.html`), 브랜드 포털 로그인 화면(`brand-portal/login.html`에서도 순간적으로 같은 오류 관측, 재확인은 못 함) | 비밀번호 재설정 링크를 눌러 이 화면을 열었다 | 조용히 뜬다 | 화면은 정상으로 뜨고("링크가 만료되었습니다" 안내까지 정확히 나옴) 실제로 막히는 기능은 없어 보이지만, 화면을 열 때마다 뒤에서 빨간 오류가 하나 난다 | 지금 당장 사용자가 겪는 건 없음(링크 재설정 흐름 자체는 살아있는 것으로 보임). 다만 이 화면에 나중에 다른 기능이 추가되면 그 기능은 조용히 먹통이 될 수 있고, 오류 모니터링(Sentry)에 매번 잡음이 쌓인다 | Low | 검토대기 | `auth/reset-password.html` 16~17줄이 `firebase-app-compat.js`·`firebase-auth-compat.js`만 싣고 `firebase-firestore-compat.js`를 안 싣는데, `firebase-config.js:30` `const db = firebase.firestore();`는 A-001(auth) 때와 달리 가드 없이 무조건 호출 — `Uncaught TypeError: firebase.firestore is not a function` 매번 발생. 다만 `db` 선언 직전 29줄 `const auth = ...`는 이미 끝난 뒤라 `auth` 변수는 살아있고, 이 페이지 로직(`auth.verifyPasswordResetCode`·`auth.confirmPasswordReset`, 224·269줄)은 `auth`만 써서 기능은 안 죽음 — 30줄 이후에 정의되는 `storage`·`ADMIN_EMAILS`(31~38줄)는 이 페이지에서 절대 안 만들어짐. 실브라우저 새 탭 기준 재현(캐시·이전 탭 오염 배제 확인함), oobCode 없이 열어도 "Link Expired" 화면 정상 표시로 기능 영향 없음을 교차 확인. 같은 패턴(firebase-config.js는 쓰는데 firestore-compat 미탑재)을 전체 HTML에서 찾으면 `brand-guide.html` 1곳 더 있는데 그건 이미 A-002(다른 처방: firebase-config.js 자체를 빼라)로 등록돼 있어 중복 등록 안 함. 처방 후보: `firebase-config.js:30`도 A-001 패턴처럼 `(typeof firebase.firestore === 'function') ? firebase.firestore() : null`로 가드하거나, `reset-password.html`에 firestore-compat 스크립트 한 줄 추가 | 09-15 브론즈-B R2 등록. `brand-portal/login.html`에서도 이동 직후 순간적으로 같은 콘솔 문구를 봤으나 그 페이지는 firestore-compat을 정상 탑재하고 있고(17줄), 곧바로 로그인된 관리자 계정(우연히 이 브라우저에 이미 로그인돼 있던 실 어드민 세션, 내가 로그인한 것 아님)으로 `admin/dashboard.html`로 자동 리디렉션돼 새로고침 잔상일 가능성이 높음 — 이쪽은 "확인 못 함"으로 남기고 `auth/reset-password.html` 단독 재현만 이 번호에 반영 · 09-15 브론즈 R E2 재확인: 어드민 로그인 성공 후 `?mode=resetPassword&oobCode=TESTINVALIDCODE12345`로 새 탭 실측, 콘솔에 정확히 "TypeError: firebase.firestore is not a function ... firebase-config.js:30:21" 재현, 화면은 "Link Expired" 정상 표시로 기능 영향 없음 재확인 — 검수관리 탭에 A-039로 신규 등록 |
| A-057 | 2026-09-15 | 소개 화면(about.html) "창업자 소개" 문단 2곳(이한울 소개, 실비아 반돈 소개) | 소개 화면에 처음 들어갔다(별도 클릭 없음, 진입 즉시 재현) | 문단 전체가 한국어 한 문장으로 자연스럽게 읽힌다 | 문장 앞부분은 한국어인데, 사람 이름("Hanul Lee"·"Silvia Vandone (실비아 반돈)") 뒤부터 갑자기 영어 원문이 이어 붙어 한 문단 안에 두 언어가 섞여 나온다(예: "…한국인 창업자입니다.**Hanul Lee** — a Korean entrepreneur with 10+ years of hands-on experience…") | 방문자가 대표 소개를 읽다가 문장 중간부터 영어로 바뀌어 당황한다. 회사를 처음 알게 되는 화면(소개)에서 나는 문제라 신뢰도에 영향 | Medium | 검토대기 | `about.html:161`(이한울)·`226`(실비아) — 두 `<p>` 모두 `data-en`/`data-ko` 속성과 함께, 태그 안쪽에 `<strong>이름</strong>` 자식 요소를 이미 품은 채로 작성돼 있음(예: `<p data-en="..." data-ko="...">ARCHINODE is built by <strong>Hanul Lee</strong> — a Korean entrepreneur...</p>`). `lang.js:160` `applyLanguage()`는 요소에 자식 태그가 있으면(`el.children.length > 0`) data 속성 전체를 쓰지 않고 **첫 번째 텍스트 노드 하나만** 교체(166~179줄) — 이 경우 첫 텍스트 노드("ARCHINODE is built by ")만 한국어 전체 문장으로 바뀌고, 뒤따르는 `<strong>Hanul Lee</strong>`와 그 뒤 영어 꼬리 텍스트(" — a Korean entrepreneur...")는 그대로 남아 두 언어가 이어 붙어 보임. 실브라우저 새 탭 기준 재현(기본 언어 ko로 뜨자마자 바로 보임, 사용자 조작 불필요). 처방 후보: 두 `<p>`를 단순 텍스트(자식 태그 없이 data-en/ko만 갖는 구조)로 바꾸거나, `data-en-html`/`data-ko-html`(23~24줄, HTML 통째 교체용으로 이미 존재하는 속성)로 바꿔 `<strong>`까지 포함한 전체 문장을 언어별로 온전히 교체 | 09-15 브론즈-B2 R1 등록(공개 콘텐츠 전수 라운드, 실브라우저 재현) · 09-15 브론즈-B2 R6 양방향 재확인: 헤더 언어 스위처로 실제로 KR→EN 클릭 전환해보니 **영어 모드에서도 증상 재현**(방향만 바뀜) — "ARCHINODE is built by **Hanul Lee** — a Korean entrepreneur...Hanul Lee — a Korean entrepreneur..."처럼 이번엔 영어 문장 전체가 통째로 중복(한/영 혼재가 아니라 같은 영어 문장 두 번). 실비아 문단도 동일. 원인은 동일(A-057 본문 참고), 언어 방향과 무관하게 재현된다는 것만 추가 확인 |
| A-058 | 2026-09-15 | 브랜드 입점 안내 화면(for-brands.html) 헤더 검색 아이콘 | 헤더 오른쪽 돋보기(검색) 아이콘을 눌렀다 | 다른 화면과 같은 검색창 오버레이가 뜬다 | 아무 반응이 없다. 뒤에서는 "toggleSearch is not defined"라는 오류가 조용히 남는다 | 이 화면에서 방문자가 브랜드·제품을 검색하려 하면 버튼이 죽어 있어 아무것도 못 한다(다른 공개 화면은 전부 정상 동작) | Medium | 검토대기 | `for-brands.html`의 검색 아이콘 버튼(약 484줄)이 `onclick="toggleSearch(event)"`를 호출하는데, 이 페이지엔 `toggleSearch` 함수를 정의하는 인라인 `<script>` 블록 자체가 빠져 있음(다른 208개 페이지는 페이지마다 이 함수를 인라인으로 각자 정의함 — 모듈 없는 순수 HTML/JS 구조라 공유 파일이 아니라 페이지마다 복붙돼 있는데 이 페이지만 그 블록이 누락됨). 실브라우저 재현(콘솔에 `ReferenceError: toggleSearch is not defined`, `for-brands.html:484:81`). **같은 결함이 의심되는 다른 화면**(정적 코드 대조만, 실브라우저 재현 안 함): `digital-products.html`(실브라우저로 재현 완료, 이번 라운드 담당 범위 밖이라 별도 등록 안 하고 여기 참고만 남김) · `submit-trend.html` · `request-consultation.html`(둘 다 이번 라운드 범위 밖, 코드상 같은 패턴만 확인). `list-your-brand.html`도 정적으로는 같은 함수가 없지만 그 화면은 검색 아이콘 자체가 없는 영어 단일 레이아웃이라 실사용자 영향 없음(확인 완료, 결함 아님). 처방 후보: 다른 페이지의 `toggleSearch` 인라인 스크립트 블록을 그대로 복사해 넣기 | 09-15 브론즈-B2 R1 등록(공개 콘텐츠 전수 라운드, 실브라우저 재현) |
| A-059 | 2026-09-15 | 브랜드 입점 안내 화면(for-brands.html) 맨 위 큰 제목 | 화면에 처음 들어갔다(별도 클릭 없음) | 제목이 한국어 한 문장으로 보인다 | "한국의 USD 480억 디자인 시장으로의 관문Korea's USD 48B Design Market"처럼 한국어 문장 뒤에 영어 원문이 띄어쓰기도 없이 바로 붙어 나온다(영어 부분은 골드색 강조) | 화면에 들어가자마자 보이는 첫 줄 제목이 두 언어가 붙어서 나와 어색하고, 안 그래도 브랜드 유치를 설득해야 하는 화면이라 첫인상에 영향 | Medium | 검토대기 | A-057과 같은 원인·같은 파일이지만 다른 요소라 별도 등록. `for-brands.html:497` `<h1 data-en="Your Gateway to Korea's USD 48B Design Market" data-ko="한국의 USD 480억 디자인 시장으로의 관문">Your Gateway to <em>Korea's USD 48B</em> Design Market</h1>` — `<em>` 자식 요소가 있어 `lang.js`의 첫-텍스트-노드만-교체 규칙(A-057 참고)에 걸려 앞부분만 한국어로 바뀌고 `<em>Korea's USD 48B</em> Design Market`은 영어로 남음. 실브라우저 재현. 처방은 A-057과 동일한 방식(자식 태그 없는 구조로 정리, 또는 data-en-html/data-ko-html 사용) — 같이 고치면 한 번에 끝남 | 09-15 브론즈-B2 R1 등록(공개 콘텐츠 전수 라운드, 실브라우저 재현) · 09-15 브론즈-B2 R3 추가 실측(휴대폰 폭 375px, 별도 브라우저 세션): 기본 언어가 영어로 뜨는 세션에서는 증상이 더 심함 — "Your Gateway to Korea's USD 48B Design MarketKorea's USD 48B Design Market"처럼 제목 뒷부분이 통째로 두 번 겹쳐 보임(영어 data-en 전체 문장이 첫 텍스트 노드에 다시 끼워지면서 기존 `<em>` 자식 + 꼬리 텍스트와 겹침 — 같은 원인, 언어에 따라 "두 언어 혼재" 또는 "같은 문장 중복"으로 다르게 보일 뿐). 휴대폰 폭에서도 가로 스크롤 등 레이아웃 붕괴는 없음(문구 겹침만) |
| A-060 | 2026-09-15 | 문의 화면(contact.html)·브랜드 입점 안내 화면(for-brands.html) — 화면에는 안 보이고 검색엔진용 구조화 데이터(JSON-LD)에만 있음 | 화면을 열었다(사용자 눈엔 안 보임, 페이지 소스로 확인) | 회사 정보 구조화 데이터의 "url"이 우리 사이트 주소(archinodekr.com)를 가리킨다 | `"url": "https://archinode.com"`으로, 마침표(kr) 없는 다른 도메인을 가리키고 있다. 그런데 그 주소를 직접 열어보니 ARCHINODE와 전혀 무관한 제3자(해외 건축가 개인 홈페이지)가 이미 쓰고 있는 사이트였다 | 방문자 눈에는 안 보이지만, 구글 등 검색엔진이 이 구조화 데이터를 읽고 ARCHINODE의 공식 주소를 엉뚱한 남의 사이트로 잘못 연결할 위험이 있다(브랜드 신뢰도·검색 노출에 영향 가능) | **High**(09-16 상향, 근거는 이력 참고) | 검토대기 | `contact.html:167~168`, `for-brands.html:28`의 JSON-LD `"url"`·`"logo"` 값이 `https://archinode.com`(우리 도메인 아님, 302 리다이렉트로 www.archinode.com → 뉴욕 건축가 "Mitchell Joachim / Terreform" 개인 사이트로 확인됨)로 박혀 있음. `archinodekr.com`으로 고쳐야 함. 같은 패턴이 이번 라운드 범위 밖인 `privacy.html:152~153`·`terms.html:155~156`에도 있어 한 번에 같이 고치는 게 효율적(코드 대조만, 실브라우저 확인은 두 파일만) | 09-15 브론즈-B2 R1 등록(공개 콘텐츠 전수 라운드, curl로 실제 도메인 소유자 확인) · **09-16 브론즈-B2 R9 범위 대폭 확대**: `grep`으로 사이트 전체를 다시 훑어보니 이 결함이 2개 파일이 아니라 **404.html·about.html·brand-guide.html·brands.html·categories.html·contact.html·digital-products.html·for-brands.html·index.html·list-your-brand.html·magazine.html·privacy-en.html·privacy.html·request-consultation.html·submit-trend.html·terms.html·trend-report.html — 17개 파일**에 걸쳐 있고(파일당 4~9번씩 반복, 예: privacy.html 7회·privacy-en.html 9회) 잘못된 도메인이 두 종류임을 확인: (1) `archinode.com` — 여전히 남의(뉴욕 건축가) 사이트로 연결(A-060 원래 발견과 동일) (2) **`archinode.org`** — 이건 회사가 실제로 소유한 도메인(이메일 `office@archinode.org`는 정상 사용 중)이지만 웹사이트로는 전혀 안 쓰고 있고 **Squarespace "공사 중입니다" 임시 페이지**만 떠 있음(`curl`로 직접 확인, `<title>곧 출시 예정</title>`) — privacy-en.html 등에 "Website: archinode.org"라고 명시돼 있어 방문자가 그대로 클릭하면 완성 안 된 페이지로 감. 두 경우 다 사이트가 "관리 안 되는 곳"으로 보이게 만듦. **우선순위를 Medium→High로 재평가 제안**(영향 범위가 2파일이 아니라 사이트 전체, 그중 하나는 실제로 남의 사이트로 보냄). 처방 후보: 17개 파일에서 `archinode\.com`·`archinode\.org`를 전부 `archinodekr.com`으로 찾아바꾸기(이메일 주소 `@archinode.org`는 실제 쓰는 이메일이라 제외해야 함 — 도메인 단독 언급만 대상) |
| A-061 | 2026-09-15 | 제품 상세(products/view.html)·브랜드 상세(brands/view.html) "찾을 수 없습니다" 화면 | 존재하지 않는 제품·브랜드 주소로 직접 들어갔다 | 안내 문구와 돌아가기 버튼이 전부 한국어로 보인다 | 안내 문구("제품을 찾을 수 없습니다" 등)는 한국어인데 바로 아래 돌아가기 버튼만 "Browse Categories"(제품)·"All Brands"(브랜드)로 영어 그대로 나온다 | 화면 전체 톤은 한국어인데 버튼 하나만 영어라 어색하고, 다국어 처리가 빠진 부분이 있다는 인상을 준다 | Low | 검토대기 | `products/view.html:398` `showError()`가 만드는 HTML에 `<a href="../categories.html">...Browse Categories</a>`가 하드코딩(다른 문구는 함수 호출 시 한국어로 전달되는데 이 버튼 글자만 고정 영어). `brands/view.html:599~605` `showError()`도 같은 패턴으로 `All Brands`가 하드코딩. 실브라우저로 두 화면 다 재현(존재하지 않는 id로 직접 접근). 처방 후보: 두 함수 모두 현재 언어(`localStorage.getItem('archinode-lang')` 등)를 보고 버튼 글자를 한국어/영어로 갈라주거나, 최소한 기본값을 한국어로 바꾸기 | 09-15 브론즈-B2 R2 등록(공개 콘텐츠 전수 라운드, 실브라우저 재현) |
| A-062 | 2026-09-15 | 카테고리 서브페이지(69개 전체 공통) 제품 목록 자리 — "아직 등록된 제품이 없습니다" 빈 상태 안내 | 제품이 하나도 없는 카테고리 서브페이지를 열었다(A-046 색인 수정 후의 정상 빈 상태) | 안내 문구와 "브랜드 입점" 버튼이 한국어로 뜬다(페이지 전체가 한국어 기준) | "No products listed yet. Brands can register their products through the brand portal."와 버튼 "List Your Brand"가 **항상 영어로만** 뜬다(한국어로 안 바뀜) | 방문자가 한국어로 사이트를 보고 있어도 제품이 비어 있는 카테고리마다 영어 안내문이 튀어나와 어색하고, 자동 번역이 덜 된 사이트처럼 보인다. A-046(색인 오류) 수정으로 대부분의 카테고리 서브페이지가 이제 이 상태를 보여주게 돼 영향 범위가 넓어짐 | Medium | 검토대기 | `category-products.js:55~56`은 `data-en`/`data-ko` 속성을 제대로 갖고 있지만(문구 자체는 이미 번역돼 있음), 이 HTML이 상품 목록을 비동기로 불러온 **뒤에** `innerHTML`로 나중에 끼워 넣어지는 조각이라 문제가 생김 — `lang.js`의 `applyLanguage()`는 페이지가 처음 열릴 때(`DOMContentLoaded`) 딱 한 번, 그리고 사용자가 언어 버튼을 직접 누를 때만 돈다. 이 빈 상태 문구는 그 첫 실행이 끝난 **이후에** DOM에 나타나므로 번역 손길이 한 번도 안 닿음(사용자가 언어 버튼을 한 번이라도 눌렀다 놓으면 그제서야 정상적으로 바뀜 — 데이터 자체는 정상이라 버그가 은근히 숨어 있었음). 같은 파일 83줄의 예전 오류 문구("Unable to load products.")는 아예 data-en/ko조차 없어 더 심함(다만 A-046 수정 뒤로는 이 오류 상태 자체가 거의 안 뜨게 됨). 처방 후보: 제품 목록을 그려 넣은 직후 `category-products.js`가 직접 `applyLanguage(currentLang)`을 한 번 더 호출(또는 `lang.js`가 전역 함수로 재적용을 노출)하거나, 아예 빈 상태 문구를 현재 언어에 맞는 텍스트로 직접 생성 | 09-15 브론즈-B2 R2 등록(A-046 색인 수정 직후 재실측 중 발견, 공개 콘텐츠 전수 라운드) · 09-15 브론즈-B2 R6 관찰 추가: 같은 원인(동적으로 나중에 삽입되는 내용은 언어 재적용을 못 받음)의 다른 사례를 brands.html에서도 목격 — 언어를 영→한으로 바꿨을 때 브랜드 카드의 "PARTNER" 배지·"2 brands" 건수 라벨이 한국어로 안 바뀜(브랜드 카드 자체는 Firestore에서 비동기로 그려짐). 영향은 작아 별도 번호는 안 만들고 이 결함이 카테고리 페이지 하나만의 문제가 아니라 사이트 전반의 패턴이라는 참고로만 남김 |
| A-063 | 2026-09-15 | 브랜드 입점 안내 화면(for-brands.html) 안의 "일반 디렉토리 vs ARCHINODE" 비교표, 휴대폰 폭(375px) | 휴대폰 정도로 좁은 화면에서 이 화면을 아래로 읽어 내려갔다 | 표가 화면 폭에 맞게 줄어들거나 옆으로만 따로 스크롤된다 | 표(Feature/Generic Directory/ARCHINODE 비교표)가 화면보다 넓어서, **표 하나 때문에 페이지 전체가 좌우로 스크롤**된다(페이지 폭 375px인데 실제로는 517px까지 밀림) | 휴대폰으로 들어온 방문자가 무심코 화면을 옆으로 밀면 페이지 전체가 밀리는 낯선 경험을 하게 되고, 표의 오른쪽 칸(ARCHINODE 열)이 화면 밖으로 잘려 안 보일 수 있다 | Medium | 검토대기 | `for-brands.html`의 "Generic Directory vs ARCHINODE" 비교 `<table>`이 375px 폭에서 약 396px로 렌더돼(`overflow-x:auto`를 주는 감싸는 컨테이너 없이 `<table>`이 그대로 본문에 박혀 있음) 표 하나가 `document.documentElement.scrollWidth`를 517px까지 밀어 올림(정상은 375px) — 페이지 전체가 가로 스크롤 대상이 됨. 실측(자바스크립트로 `scrollWidth`/`clientWidth` 직접 비교, 375×812 에뮬레이션). 처방 후보: 표를 `<div style="overflow-x:auto">`로 감싸 표만 옆으로 스크롤되게 하거나, 좁은 화면에서 표를 세로 카드형으로 바꾸기(다른 화면에 이미 있는 반응형 표 패턴이 있으면 그걸 재사용) | 09-15 브론즈-B2 R3 등록(반응형 표본 확인, 별도 브라우저 세션·375px 에뮬레이션 실측) |
| A-064 | 2026-09-15 | 자문 요청 화면(request-consultation.html) | 자문 요청 폼을 정상적으로 채워 제출했다 | 요청이 접수되고, 운영자에게도 알림이 가서 빠르게 확인할 수 있다 | 화면엔 "요청 접수 완료"가 뜨고 요청 자체는 정상 저장되지만, 운영자에게 가는 알림 메일은 **매번 조용히 실패**한다(뒤에서 코드 오류가 나서 알림 메일 자체가 아예 안 만들어짐) | "2영업일 이내 회신"을 약속하는 화면인데, 운영자가 어드민 화면에 직접 들어가서 목록을 보지 않는 한 새 요청이 왔는지 이메일로는 전혀 알 수 없다. 응답이 늦어질 위험 | High | 검토대기 | `request-consultation.html` `submitConsult()` 483~505줄. consultations 저장 성공 후 별도 try 블록에서 관리자 알림용 mail 문서를 추가하는데, 그 안에서 `data.companyName` 등 `data.*`로 필드를 읽지만 이 함수엔 `data`라는 변수가 애초에 없음(실제 변수명은 companyName·contactName 등 개별 변수) → `ReferenceError: data is not defined`가 나고 `catch(mailErr){console.warn('Admin notification skipped', ...)}`로 조용히 삼켜짐. 실측: 정상 값으로 제출 → 콘솔에 매번 동일 오류 재현 → Firestore엔 consultations 문서 정상 생성(직접 조회 확인) → 같은 제목 패턴(`[ARCHINODE] 컨설팅 요청 - `)의 mail 문서는 0건. 처방: 483~505줄의 `data.x`를 위에서 이미 만든 실제 변수명(companyName, contactName, email, phone, country, category, timing, budget, description)으로 바꾸면 됨. 대조군: 같은 페이지의 consultations 저장 자체는 정상, index.html 뉴스레터 구독의 동일 패턴 관리자 알림 메일은 변수명을 올바로 써서 정상 동작 | 09-15 브론즈-E R1 등록(공개 폼 5종 E2E 라운드, 실브라우저+콘솔 재현, Firestore 직접 조회로 확인) · 09-15 브론즈 R E2: 검수관리 탭 등록 시도 중 오늘 먼저 등록된 A-021이 완전히 같은 결함(동일 원인·동일 파일)임을 발견 — 등록 순서를 몰랐던 채로 A-033으로 중복 등록했고, 즉시 A-033에 "A-021과 중복, 판정은 A-021 기준" 메모 남김. **판정·수정은 A-021 기준으로 진행할 것**, A-033은 참고용 |
| A-065 | 2026-09-15 | 문의 화면(contact.html) "Get in Touch / Send a Message" 영역, 휴대폰 폭(322px·375px) | 휴대폰 폭으로 이 화면을 열었다 | "문의하기 정보"와 "메시지 보내기" 폼이 세로로 쌓여 한 줄씩 보인다 | 두 영역이 계속 나란히(2칸) 배치돼 화면 폭보다 넓어지고, 오른쪽 "메시지 보내기" 입력칸들이 화면 밖으로 잘리며 페이지 전체가 좌우로 스크롤된다(322px 화면에서 실제 폭 393px, 375px 화면에서도 393px) | Contact는 방문자가 회사에 직접 연락하는 핵심 창구인데, 휴대폰에서는 입력칸을 옆으로 밀지 않으면 온전히 못 봐서 사실상 쓰기 어렵다 | High | 검토대기 | `contact.html` 96행 `<div style="display:grid;grid-template-columns:1fr 1fr;gap:60px;">`가 인라인 스타일로 두 칼럼을 고정하고, 좁은 화면에서 1열로 바꾸는 `@media` 규칙이 전혀 없음(같은 사이트 request-consultation.html의 `.form-row`는 `@media (max-width:768px){grid-template-columns:1fr}`가 있어 정상 — 대조군). 실측: 322px→scrollWidth 393(71px 초과) · 375px→393(18px 초과) · 768px→753(정상, 초과 없음). 즉 대략 600px대 이하(대다수 스마트폰)에서 재현. 처방 후보: 인라인 스타일을 클래스로 빼서 `@media (max-width:768px){grid-template-columns:1fr}` 추가(request-consultation.html `.form-row` 패턴 재사용) | 09-15 브론즈-E R1 등록(공개 폼 5종 E2E 라운드, 322·375·768px 실측) · 09-15 브론즈-B2 R4 재확인: 별도 브라우저 세션·375px에서 동일 재현(scrollWidth 393, offending 요소 좌표 left:260~right:393의 "Send a Message" 폼 칼럼), 스크린샷으로 시각 확인(입력칸이 133px 폭으로 짓눌려 사실상 못 씀) — 같은 결함, 새 번호 안 만듦 · 09-15 브론즈 R E2: 검수관리 탭에 A-034로 신규 등록 |
| A-066 | 2026-09-15 | 브랜드 매거진 글 발행 안내 화면(submit-trend.html) "How to Publish" 4단계, 휴대폰 폭(322px·375px) | 휴대폰 폭으로 이 화면을 열고 4단계 안내까지 내려봤다 | 4개 단계(①입점 신청 ②승인 대기 ③포털 로그인 ④글 작성)가 세로로 하나씩 쌓여 전부 보인다 | 4단계가 계속 가로 4칸으로 배치돼 화면보다 넓어지고, 마지막 4번째 단계("글 작성 후 발행" — 가장 중요한 단계)가 화면 오른쪽 밖으로 잘려 스크롤해야만 보인다. 페이지 전체도 좌우로 스크롤됨(322px에서 실제 폭 386px, 375px에서도 386px) | 브랜드 담당자가 휴대폰으로 이 화면을 보면 실제로 글을 쓰는 마지막 단계를 놓치기 쉽다 | Medium | 검토대기 | `submit-trend.html` 76행 `<div style="display:grid; grid-template-columns:repeat(4,1fr); gap:24px;">`가 인라인 스타일로 4칼럼 고정, 반응형 `@media` 규칙 없음(A-065와 같은 유형, 다른 파일). 실측: 322px→scrollWidth 386(64px 초과) · 375px→386(11px 초과) · 768px→753(정상). 처방 후보: A-065와 동일한 방식(좁은 화면에서 1~2열로) | 09-15 브론즈-E R1 등록(공개 폼 5종 E2E 라운드, 322·375·768px 실측) · 09-15 브론즈 R E2: 검수관리 탭에 A-035로 신규 등록 |
| A-067 | 2026-09-15 | 홈 화면(index.html) 하단 "Stay Updated" 뉴스레터 구독 폼, 매우 좁은 휴대폰 폭(322px) | 322px 폭으로 이 화면을 열고 구독 영역까지 내려봤다 | 이메일 입력칸과 "Subscribe" 버튼이 화면 폭 안에 온전히 들어온다 | 입력칸과 버튼이 한 줄에 나란히 배치된 채 줄어들지 않아 "Subscribe" 버튼 글자가 화면 오른쪽으로 잘려 나가고, 페이지 전체가 좌우로 스크롤된다(322px 화면에서 실제 폭 363px) | 아주 좁은 휴대폰 화면(구형 기종 등)에서 구독 버튼이 잘려 보이고 페이지가 옆으로 밀린다. 단 375px 이상에서는 문제없이 정상적으로 보임 | Medium | 검토대기 | `index.html` 1037행 `.newsletter-form`(입력칸+버튼, `display:flex` 추정)이 폭 약 360px 미만에서 줄바꿈 없이 한 줄을 유지. 자바스크립트로 overflow 요소를 직접 특정: 뷰포트를 넘는 유일한 실질 요소가 "Subscribe" 버튼(부모 클래스 `newsletter-form fade-in`), 우측 끝이 뷰포트보다 41px 밖(322px 기준). 322px→scrollWidth 363(41px 초과) / 375px→scrollWidth 375(정상). 처방 후보: `.newsletter-form`에 좁은 폭에서 `flex-wrap:wrap` 또는 `flex-direction:column` 미디어쿼리 추가 | 09-15 브론즈-E R1 등록(공개 폼 5종 E2E 라운드, 322px 실측 + JS로 overflow 요소 특정) · 09-15 브론즈 R E2: 검수관리 탭에 A-036으로 신규 등록 |
| A-068 | 2026-09-15 | 문의 화면(contact.html) "메시지 보내기" 폼 제출 이후 화면 | 이름·이메일·메시지를 정상적으로 채우고 "Send Message"를 눌렀다 | 접수됐다는 안내가 ARCHINODE 화면 안에서(최소한 ARCHINODE 로고가 보이는 화면으로) 나온다 | 제출은 성공하지만 브라우저가 archinodekr.com을 완전히 떠나 "formspree.io"라는 전혀 다른 회사의 기본 "Thanks!" 페이지로 이동한다. ARCHINODE 로고·문구가 전혀 없고, 사이트로 돌아가는 링크도 없이 "Go back"(브라우저 뒤로가기)뿐 | 문의를 보낸 방문자가 낯선 외부 사이트로 순간이동한 것처럼 느낄 수 있고(신뢰도 우려), ARCHINODE로 자연스럽게 돌아오는 길이 없어 그대로 이탈할 가능성이 있다 | Low | 검토대기 | `contact.html` 130행 `<form action="https://formspree.io/f/xgodynvd" method="POST">`에 Formspree의 `_next`(제출 후 리디렉션 대상) 히든 필드가 없어 Formspree 기본 감사 페이지로 이동함. 실측: 정상 제출 → `https://formspree.io/...`의 "Thanks! The form was submitted successfully." 화면 전환 확인. 처방 후보: `<input type="hidden" name="_next" value="https://archinodekr.com/contact.html?sent=1">` 추가 + contact.html에 `?sent=1`일 때 보여줄 감사 메시지 섹션 추가(request-consultation.html `#formSuccess` 패턴 참고) | 09-15 브론즈-E R1 등록(공개 폼 5종 E2E 라운드, 실제 Formspree 제출로 재현) · 09-15 브론즈 R E2: 검수관리 탭에 A-037로 신규 등록 |
| A-069 | 2026-09-15 | 전 공개 페이지 공통 헤더 "List Your Brand" 버튼, 좁은 데스크톱·태블릿 폭(약 1024px, 요청 범위 322~768px 밖에서 우연히 발견) | 로그인 여부와 무관하게 이 폭으로 아무 공개 페이지나 열었다(request-consultation.html·digital-products.html 등에서 확인) | "List Your Brand" 버튼이 다른 메뉴들처럼 한 줄로 얌전히 보인다 | 버튼 글자가 "List / Your / Brand"로 세로 4줄에 걸쳐 줄바꿈되면서 검은 배경 박스가 세로로 길게 늘어나(가로 77px·세로 77px) 옆의 "Admin/Log In"·"Brand Login" 버튼 위로 겹쳐 보인다 | 로그인 상태와 무관하게(admin 세션·비로그인 둘 다 재현) 태블릿이나 작은 노트북 화면에서 헤더가 지저분해 보이고, 다른 버튼과 겹쳐 클릭 실수를 유발할 수 있다 | **Medium**(09-16 R18 상향, 근거는 이력 참고 — 단순 시각적 흠이 아니라 영어 모드에서 검색·북마크·언어전환·로그인 버튼이 통째로 클릭 불가 상태가 되는 넓은 폭 구간이 확인됨) | 검토대기 | `.nav-cta`("List Your Brand")가 1024px 폭에서 계산된 실제 폭이 77px밖에 안 돼(`getBoundingClientRect().width`) 텍스트가 강제 줄바꿈됨 — `.main-nav`의 항목들이 좁아진 데스크톱 폭에서 유연하게 줄어들다가 `.nav-cta`만 유독 심하게 눌리는 것으로 추정(정확한 원인은 CSS 폭 계산 미확인, 후속 조사 필요). 768px 이하(모바일 메뉴로 전환)와 넓은 데스크톱(약 1200px 이상 추정)에서는 재현 안 됨 — 그 사이 구간만의 문제. 요청받은 확인 범위(322~768px) 밖이라 이번 라운드에선 정밀 조사는 안 함, 발견만 기록 | 09-15 브론즈-E R1 등록(공개 폼 5종 E2E 라운드 중 기본 뷰포트 1024px에서 우연히 발견) · 09-15 브론즈 R E2: 검수관리 탭에 A-038로 신규 등록 · 09-15 브론즈-B2 R8 추가 실측(125% 배율 등가 1152px, for-brands.html): 같은 버튼이 126×57px(2줄 "List Your"/"Brand")로 재현 — 1024px보다 훨씬 덜 심하지만(4줄 세로 쌓임이 아니라 2줄), 기존 메모의 "약 1200px 이상에서는 재현 안 됨" 추정과 달리 1152px에서도 일부 재현됨을 확인 — 안전 구간 상한을 1200px보다 좁게 잡아야 할 수 있음(정밀 경계값은 여전히 미조사) · 09-16 브론즈-B2 R17 추가 실측(175% 배율 등가 823px, list-your-brand.html): 이 페이지는 자기 전용 헤더(영어 단일, 지금 보고 있는 메뉴를 굵게 강조하는 "active-nav" 상태)를 따로 쓰는데, 823px에서 "Brand Login" 링크와 검은 "List Your Brand" 버튼이 통째로 화면 오른쪽 밖으로 밀려나 글자가 "Li.../You.../Bra..."로 잘림(`scrollWidth` 885 vs `clientWidth` 808, 77px 초과) — 정확히 같은 823px에서 다른 페이지(index.html·categories/furniture.html 등 표준 헤더)는 재현 안 됨(정상), 이 페이지가 유독 더 좁은 구간(768px 초과~900px 안팎 추정, 768px 이하는 모바일 메뉴로 정상 전환됨을 확인)에서 겹침. 표준 헤더보다 이 페이지 전용 헤더가 좁은 폭에 더 취약하다는 새 단서 — 원인·정밀 경계값은 여전히 미조사 · **09-16 브론즈-B2 R18 핵심 추가 실측 — 진짜 원인은 폭이 아니라 "영어 모드"였음**: 위 R17 메모에서 "list-your-brand.html만 재현, 표준 헤더는 정상"이라고 적었던 건 **그때 표준 헤더 페이지들이 우연히 한국어 상태였기 때문**임을 뒤늦게 확인. 같은 categories/furniture.html을 언어만 영어로 명시적으로 바꾸고(`localStorage` `archinode-lang`='en') 다시 재보니 823px·960px·1024px **전부 재현**(헤더 오른쪽(검색·북마크·언어전환·Brand Login)이 화면 밖으로 통째로 밀려남, `scrollWidth` 1047 고정 vs 폭에 따라 808/945/1009로 각각 239/102/38px 초과) — 반대로 한국어로 되돌리면 823px에서도 정상(808=808). 즉 **이 결함의 진짜 조건은 "폭이 좁다"가 아니라 "영어 모드 + 폭이 약 1047px보다 좁다"**이고, 그 폭 조건 자체는 꽤 넓어서 768px(모바일 메뉴 전환점) 바로 위부터 대략 1046px까지 — **일반 노트북 해상도인 1024px도 포함**됨(1152px·1280px에서는 정상 확인). categories.html(1044)·brands.html(1044)·magazine.html(1045)·categories/furniture.html(1047) 등 표준 헤더를 쓰는 페이지 전부 같은 폭에서 영어 모드일 때 동일하게 재현 — **거의 전 페이지 공통 결함**으로 범위가 크게 넓어짐. 실사용 영향도 재평가 필요: 원래 메모는 "List Your Brand 버튼이 줄바꿈되어 지저분해 보인다"(Low)였지만, 실제로는 **검색·북마크·언어 전환·브랜드 로그인 버튼 4개가 전부 화면 밖으로 밀려나 보이지 않게 됨**(페이지 자체는 가로 스크롤이 생겨 이론적으로 오른쪽으로 밀면 닿기는 하지만, 일반 방문자가 헤더를 옆으로 밀어볼 이유가 없어 사실상 못 찾는 상태) — 영어로 사이트를 보는 방문자(정확히 EU 브랜드 담당자 등 타겟 고객)가 일반 노트북 폭에서 언어를 되돌리거나 로그인하는 길이 막히는 것과 같아, 단순 시각적 흠보다 심각할 수 있음. **우선순위 Low→Medium 이상 재검토를 제안**(사람 판단 필요, 임의로 바꾸지 않음) · **09-16 브론즈-B2 R19 범위 정량화(900px 고정, 영어 모드)**: 대표 화면 8종을 같은 폭에서 재검사 — index.html·privacy-en.html **2개만 정상**(885=885), 나머지 **6개(categories/bathroom/mirrors.html·contact.html·for-brands.html·매거진 기사 1편·products/view.html·brands/view.html?id=oska(실제 승인 브랜드))는 전부 재현**. 특히 `products/view.html`(초과 261px)·`brands/view.html`(초과 260px, 실제 공개된 oska 브랜드 페이지)은 다른 화면(약 59px 초과)보다 훨씬 심하게 깨짐 — 제품·브랜드 상세 화면이 브레드크럼 등으로 헤더 폭 요구치가 더 커서로 추정(정밀 원인 미조사). **거의 전 화면 공통(요청·상세·목록·정적 페이지 가리지 않음) + 실제 공개 브랜드 페이지에서도 재현**되는 것까지 확인돼, 영향 범위가 애초 생각보다 훨씬 넓음 — **우선순위 제안을 Medium에서 Medium~High로 상향**(최종 등급은 사람 판단) |
| A-070 | 2026-09-15 | 브랜드 입점 신청 화면(list-your-brand.html) 맨 아래 푸터, 휴대폰 폭(375px) | 휴대폰 폭으로 이 화면을 맨 아래까지 내려봤다 | 푸터의 "회사 소개·카테고리·회사 정보·법적 고지" 같은 항목들이 세로로 한 칸씩 쌓여 전부 읽힌다 | 푸터가 4칸짜리 가로 표처럼 그대로 유지돼, "COMPANY"·"For Brands" 같은 오른쪽 칸 글자들이 화면 밖으로 잘려 나가고 페이지 전체가 좌우로 스크롤된다(375px 화면에서 실제 폭 483px, 108px 초과) | 브랜드가 입점을 신청하러 들어온 화면인데 맨 아래 회사 정보·법적 고지(약관·개인정보처리방침) 링크가 휴대폰에서 잘려 안 보이거나 누르기 어렵다 | Medium | 검토대기 | `list-your-brand.html` 푸터가 다른 공유 `style.css` 기반 페이지들과 달리 이 페이지 전용으로 별도 마크업/스타일을 쓰는 것으로 추정(다른 페이지 푸터는 375px에서 정상 1열로 쌓임 — 이 라운드에서 index·about·brands·magazine·contact·카테고리 서브페이지 전부 정상 확인, list-your-brand.html만 재현). 실측: `document.documentElement.scrollWidth` 483 vs `clientWidth` 375, 초과 요소는 좌표상 "CATEGORIES"(left 382~)·"COMPANY"(left 436~484) 칼럼 — 4칼럼 그리드가 좁은 화면에서 1열로 안 바뀜. 스크린샷으로 "COMPA[NY]"·"For Bran[ds]" 글자가 화면 오른쪽에서 잘린 것 시각 확인. 처방 후보: 이 페이지 푸터에도 A-065와 같은 처방(좁은 화면 `@media`에서 grid-template-columns를 1fr로) 적용 | 09-15 브론즈-B2 R4 등록(반응형 매트릭스 375px 표본, 별도 브라우저 세션 실측) |
| A-071 | 2026-09-15 | 소개 화면(about.html) 맨 위 큰 제목("건축을 잇다") | 언어를 영어(EN)로 바꾼 상태에서 소개 화면에 들어갔다 | 화면 맨 위 큰 제목도 영어로 바뀐다 | 다른 글자는 전부 영어로 바뀌는데 이 제목 한 줄("건축을 잇다")만 한국어로 그대로 남는다 | 언어를 영어로 바꿔서 보는 외국 방문자(EU 브랜드 담당자 등)가 화면 맨 위 가장 큰 글자를 못 읽는다 — 소개 화면 첫인상에 영향 | Medium | 검토대기 | `about.html:89` `<h1>건축을 잇다</h1>`가 `data-en`/`data-ko` 속성 자체가 아예 없이 한국어 텍스트만 하드코딩돼 있음(로고 옆 작은 태그라인은 의도적으로 한국어 고정인 게 맞아 보이지만, 이 페이지 h1은 "About Us" 소제목 바로 아래 가장 크게 보이는 제목이라 성격이 다름). 실측: 헤더 언어 스위처로 KR→EN 전환 후 재확인, 다른 모든 섹션 제목(Meet the Founder·Our Mission 등)은 정상 영어 전환됨 — 이 h1만 예외. 처방 후보: `data-en="Bridging Architecture"`(또는 적절한 영문 태그라인) 추가 | 09-15 브론즈-B2 R6 등록(언어 토글 양방향 실클릭 테스트 중 발견) |
| A-072 | 2026-09-16 | 전 공개 페이지 공통 헤더 검색창 | 헤더 검색창에 실제 입점 브랜드 이름("oska")을 입력했다 | oska 브랜드 결과가 뜨고 누르면 브랜드 페이지로 간다 | "검색 결과가 없습니다" — 브랜드 이름으로는 아무것도 안 뜬다(같은 검색창이 카테고리·서브카테고리·매거진 기사는 잘 찾음) | 검색창 placeholder와 빈 검색창 안내 문구가 둘 다 "브랜드, 제품, 디자이너 검색…"이라고 약속하는데 정작 브랜드는 하나도 못 찾는다 — 방문자가 아는 브랜드 이름을 검색해도 항상 "결과 없음"만 보게 된다 | High | 검토대기 | `search.js`는 Firestore를 전혀 안 쓰고(`grep` 확인, firebase 참조 0건) 전적으로 정적 파일 `search-index.json`(175건)만 검색하는데, 이 파일엔 **`brand` 타입 항목이 0건**(`page` 10·`category` 10·`subcategory` 69·`article` 85·`trend-report` 1 뿐). 그런데 `search.js` 자체는 `brand` 타입 처리 코드(아이콘·라벨·국가+카테고리 부제 조립)를 이미 갖추고 있어 브랜드 검색을 지원하도록 만들어진 게 분명함 — 데이터만 안 채워짐. 원인 특정: `scripts/make_search_index.py:85-86`이 `brands/*.html` **정적 파일**을 스캔해서 brand 항목을 만드는데, 사이트가 정적 브랜드 페이지에서 Firestore 동적 브랜드(`brands/view.html?id=slug`)로 전환된 뒤 이 스캔 로직이 갱신되지 않아 브랜드를 하나도 못 찾음(이 PC엔 Python도 없어 스크립트 실행 자체가 안 됨 — CLAUDE.md 4절). 처방 후보: 검색 인덱스 생성 시 Firestore `brands`(status==approved)도 함께 읽어 합치거나, 클라이언트에서 브랜드만 별도로 Firestore 실시간 검색 | 09-16 브론즈-B2 R9 등록(search.js 실측 라운드) · 09-16 브론즈-B2 R11 범위 확인: 힌트 문구가 "브랜드, **제품**, 디자이너 검색…"이라고 약속하는 "제품"도 같은 이유로 전멸 확인 — `search-index.json`에 `product` 타입도 0건(같은 원인: 제품도 Firestore 동적 데이터라 정적 스캔 스크립트가 못 잡음). 제품은 현재 승인된 것이 0건이라 실검색으로 "제품이 있는데 안 뜨는지"까지는 확인 못 했지만, 데이터 구조상 있어도 못 뜰 것이 확실함 — 처방은 브랜드와 동일하게 함께 해결 가능 |
| A-073 | 2026-09-16 | 전 공개 페이지 공통 헤더 검색창, 매거진 기사·트렌드 리포트 결과 | 매거진 기사나 트렌드 리포트가 검색 결과에 뜨는 검색어를 입력했다("가구"·"살로네") | 결과 줄 오른쪽에 "매거진"(또는 그에 준하는 한국어 종류 표시)이 뜬다 | 카테고리·서브카테고리 결과는 "카테고리"·"서브카테고리"로 한글 표시되는데, 매거진 기사·트렌드 리포트 결과만 영어 원문 그대로 "ARTICLE"·"TREND-REPORT"(하이픈 포함, 대문자)로 뜬다 | 같은 화면 같은 줄에서 어떤 결과는 한글, 어떤 결과는 영어 내부 코드명이 그대로 노출돼 마무리가 덜 된 느낌을 준다 | Low | 검토대기 | `search.js:116` `getTypeLabel()`의 라벨 사전에 `category`·`subcategory`·`brand`·`magazine`·`page` 5종만 있고 실제 인덱스에 쓰이는 타입명 `article`·`trend-report`는 없음 — `labels[type] \|\| { en: type, ko: type }` 폴백이 걸려 원본 타입 문자열이 그대로(대문자는 CSS `text-transform`) 노출됨. 실측 스크린샷으로 "TREND-REPORT" 배지 확인. 처방 후보: `labels`에 `article: {en:'Article', ko:'매거진'}`·`'trend-report': {en:'Trend Report', ko:'트렌드 리포트'}` 추가 | 09-16 브론즈-B2 R9 등록(search.js 실측 라운드) |
| A-074 | 2026-09-16 | 전 공개 페이지 공통 헤더 검색창 입력칸(빈 상태) | 한국어로 사이트를 보다가 검색창을 열었다 | 아무것도 안 친 입력칸 안내 문구가 "브랜드, 제품, 디자이너 검색…"처럼 한국어로 보인다 | 안내 문구가 언제나 "Search brands, products, designers..."로 영어 고정 — 한국어 모드에서도 안 바뀜 | 사소하지만 화면 전체가 한국어인데 검색창 입력 전 안내 문구만 영어라 눈에 띄고, 사이트 거의 전부(표본 확인만 206개 파일)에서 똑같이 나타남 | Low | 검토대기 | 각 페이지 HTML에 `<input type="text" placeholder="Search brands, products, designers...">`가 `data-en-placeholder`/`data-ko-placeholder` 없이 하드코딩(`index.html:825` 등). `lang.js`는 `data-{lang}-placeholder` 속성이 있는 요소만 처리하므로(23절) 이 입력칸은 손을 안 댐. `grep`으로 `placeholder="Search brands`가 site 전역에 박혀 있는 파일 206개 확인(카테고리·매거진 다수 포함) — 사실상 검색 기능이 있는 거의 모든 페이지. 처방 후보: `data-en-placeholder="Search brands, products, designers..."` `data-ko-placeholder="브랜드, 제품, 디자이너 검색..."` 두 속성을 모든 검색 input에 일괄 추가(찾아바꾸기로 가능, 페이지 수는 많지만 기계적 작업) | 09-16 브론즈-B2 R9 등록(search.js 실측 라운드 중 발견) |
| A-075 | 2026-09-16 | 이용약관 화면(terms.html) 본문 | 영어로 언어를 바꾼 뒤 "브랜드 입점 신청 시 동의해야 하는" 이용약관 본문(제1조~제8조)을 읽으려 했다 | 제목·안내문은 영어로 바뀌는데 본문 조항도 영어로 읽힌다 | 페이지 제목("Terms of Service")과 헤더·푸터 메뉴만 영어로 바뀌고, 실제 계약 내용인 제1조~제8조 조항 전체는 언어를 바꿔도 항상 한국어 그대로다 — 애초에 영어 버전 문구 자체가 없다 | ARCHINODE가 유치하려는 대상이 유럽 브랜드인데, 그 브랜드들이 입점 시 "동의"해야 하는 약관을 한국어로만 읽을 수 있다 — 실제로 뭘 동의하는지 모른 채 서명하게 되는 상황 | High | 검토대기 | `terms.html`에 `data-en`이 14곳뿐이고 전부 헤더·푸터 공용 요소 — 제1조~제8조 조항(`<h2>제1조 (목적)</h2>` 등, 98줄~)은 `data-en` 속성 자체가 없는 순수 한국어 하드코딩. `privacy.html`은 별도 파일 `privacy-en.html`이라도 존재하는데(별개로 A-076에서 그 파일이 어디서도 안 걸린다는 문제를 다룸) **terms.html은 영어 버전 파일 자체가 아예 없음**(`terms-en.html` 없음, `ls`로 확인). 처방 후보: 최소한 `privacy-en.html`과 같은 방식으로 `terms-en.html` 신설, 이왕이면 A-076과 함께 언어 전환 시 실제로 해당 언어 문서로 가도록 연결 | 09-16 브론즈-B2 R9 등록(terms.html·privacy.html 점검 중 발견) |
| A-076 | 2026-09-16 | 개인정보처리방침 화면(privacy.html), 전 공개 페이지 공통 푸터의 "개인정보처리방침"/"Privacy Policy" 링크 | 언어를 영어로 바꾼 상태에서 푸터의 "Privacy Policy" 링크를 눌렀다(또는 privacy.html에서 헤더 언어 스위처로 영어를 눌렀다) | 영어로 된 개인정보처리방침(`privacy-en.html`)으로 연결되거나, 최소한 본문이 영어로 바뀐다 | 어느 경우든 한국어 전용 `privacy.html`만 보인다 — 본문 조항(제1조~)은 영어 모드에서도 안 바뀌고, 실제 영어 번역 파일 `privacy-en.html`은 사이트 어디에서도(헤더·푸터·사이트맵 전부) 링크가 안 걸려 있어 URL을 직접 알고 쳐야만 열림 | 영어로 사이트를 보는 방문자가 개인정보처리방침을 찾으면 항상 한국어 문서로 가고, 실제로 존재하는 영어 번역본은 찾을 방법이 없다(검색엔진도 sitemap.xml에 없어 못 찾음) — 유럽 브랜드 대상 GDPR 관련 고지 의무 측면에서도 걸리는 부분일 수 있음(법률 검토 필요) | High | 검토대기 | `index.html:1111` 등 전 페이지 푸터의 `<a href="privacy.html" data-en="Privacy Policy" data-ko="개인정보처리방침">`가 언어 불문 `privacy.html` 고정 — `privacy-en.html`로 분기하는 로직이 어디에도 없음. `grep`으로 사이트 전체에서 `privacy-en.html`을 참조하는 파일이 **자기 자신 1개뿐**임을 확인(어떤 페이지도 링크 안 함), `sitemap.xml`에도 없음. `privacy.html` 자체도 A-075(terms.html)와 같은 패턴으로 본문에 `data-en` 없음(14곳뿐, 전부 공용 요소). 처방 후보: 푸터·헤더의 Privacy Policy 링크를 현재 언어에 따라 `privacy.html`/`privacy-en.html`로 분기하거나, 아예 `privacy.html` 한 파일 안에 data-en/ko로 양 언어를 다 넣고 `privacy-en.html`은 그쪽으로 리다이렉트 | 09-16 브론즈-B2 R9 등록(terms.html·privacy.html 점검 중 발견) |
| A-077 | 2026-09-16 | 매거진 화면(magazine.html)·브랜드 매거진 화면(trend-report.html) 맨 아래 뉴스레터 구독 폼 | 이메일을 넣고 "구독" 버튼을 눌렀다 | 화면 안에 "감사합니다" 같은 안내 문구가 자연스럽게 나타난다(digital-products.html의 "출시 시 알림" 폼과 같은 방식) | 화면이 그대로 멈추고(자동화 도구 스크린샷·클릭이 전부 응답 없음 상태가 됨) 흰 화면만 보이는데, 이건 사실 브라우저 자체의 **구식 팝업창(alert)**이 페이지 전체를 가리고 뜬 것 — 그 팝업을 직접 눌러 닫아야만 페이지로 돌아온다(자동화 키 입력으로는 안 닫히고 다른 페이지로 이동해야 겨우 풀림) | 브라우저 기본 팝업은 사이트 디자인과 전혀 안 어울리고(글꼴도 다르고 브랜드 색도 없음), 화면 전체를 막아버려서 방문자가 당황할 수 있다. 같은 사이트의 다른 구독 폼(digital-products.html)은 화면 안에 자연스러운 문구로 알려주는데 이 두 화면만 옛날식 팝업을 씀 — 같은 사이트 안에서도 화면마다 손맛이 다르다는 인상을 준다 | Medium | 검토대기 | `magazine.html`·`trend-report.html`의 `subscribeNewsletter()` 함수가 성공·실패 안내를 전부 브라우저 기본 `alert()`로 처리(`magazine.html:1991` `alert('Thank you for subscribing!')`, 에러 시에도 `alert('Subscription failed: ...')`). `alert()`는 페이지 렌더링·입력을 전부 멈추는 모달이라(실측: 클릭 직후 스크린샷 요청이 30초 타임아웃, 다른 페이지로 이동해야 풀림) UX상 좋지 않고, 이 사이트의 다른 폼들(digital-products.html `#notifyMsg`, request-consultation.html `#formSuccess`)은 전부 화면 안 인라인 안내로 처리하는 것과도 안 맞음. 실제 구독 자체는 성공한 것으로 보임(제출 후 콘솔에 오류 없음 — 오류가 났다면 catch 블록의 다른 alert 문구와 console.error가 함께 남았을 것). 처방 후보: `alert()` 두 곳을 digital-products.html 패턴처럼 폼을 숨기고 인라인 "감사합니다" 문구를 보여주는 방식으로 교체 | 09-16 브론즈-B2 R10 등록(공개 폼 실제 제출 라운드, magazine.html·trend-report.html 둘 다 재현) · 09-16 브론즈-B2 R13 범위 확대: **홈 화면(index.html) 맨 아래 "소식 받기" 뉴스레터 폼도 완전히 같은 `alert()` 패턴**임을 실제 제출(`qa-bronze+homenews@archinodekr.com`)로 확인 — 제출 직후 화면이 흐릿하게 멈추고(스크린샷상 버튼·입력칸이 반투명 상태로 굳음) 다른 페이지로 이동해야 풀림, 3번째 재현 화면. 부수 발견: 같은 index.html 폼의 이메일 입력칸 placeholder("Your email address")만 `data-en-placeholder`/`data-ko-placeholder`가 없어 한국어 모드에서도 항상 영어로 뜸(형제 격인 magazine.html·trend-report.html의 같은 폼은 이 속성이 정상적으로 있어서 "이메일 주소를 입력하세요"로 잘 나옴 — index.html 폼만 복사할 때 이 부분이 빠진 것으로 보임, A-074의 검색창 placeholder와 같은 패턴이지만 다른 입력칸이라 여기 메모로만 남김) |
| A-078 | 2026-09-16 | 전 공개 페이지 공통 헤더 검색창 — "lighting" 검색 결과 | 헤더 검색창에 "lighting"을 입력했다 | 서브카테고리 결과들이 각각 어느 대분류(카테고리) 소속인지 알 수 있게 나온다 | "아웃도어 조명"이라는 **완전히 똑같은 이름의 결과가 두 줄 나오는데, 어느 것이 조명 카테고리 것이고 어느 것이 아웃도어 카테고리 것인지 화면에서 구분할 방법이 전혀 없다**(부제·설명 없이 이름과 "서브카테고리" 표시만 동일하게 반복) | 방문자가 원하는 걸 찾으려고 검색했는데 똑같아 보이는 결과 두 개 중 아무거나 눌러야 하는 상황 — 실제로는 서로 다른 페이지(조명 카테고리의 아웃도어 조명 vs 아웃도어 카테고리의 아웃도어 조명)라 엉뚱한 곳으로 갈 수 있다. "스팀룸"도 같은 문제(욕실 카테고리·웰니스 카테고리에 각각 있음) | Medium | 검토대기 | 근본 원인 두 가지가 겹침: (1) `search-index.json`의 서브카테고리 항목에 `parent_en`/`parent_ko` 필드가 **아예 없음**(69개 서브카테고리 전수 확인) — `search.js:167` `sub = (lang==='ko'?item.parent_ko:item.parent_en)||''`가 항상 빈 문자열이 돼 소속 대분류를 보여주는 기능 자체가 죽어 있음(이 항목 자체는 69개 서브카테고리 전부에 해당하지만, 이름이 겹치는 경우에만 실제로 혼란을 일으킴). (2) 이름이 겹치는 서브카테고리가 실제로 2쌍 존재: "아웃도어 조명"(`categories/lighting/outdoor-lighting.html` · `categories/outdoor/outdoor-lighting.html`, 설명 문구도 서로 다름 — 의도적으로 두 카테고리에 각각 만든 것으로 보임)과 "스팀룸"(`categories/bathroom/steam-rooms.html` · `categories/wellness/steam-rooms.html`). 실브라우저 스크린샷·zoom으로 두 결과 줄이 픽셀 단위로 동일하게 보이는 것 확인. 처방 후보: 검색 인덱스 생성 스크립트가 각 서브카테고리 항목에 소속 대분류(`parent_en`/`parent_ko`)를 채워 넣도록 수정(이미 `search.js` 쪽 렌더링 코드는 준비돼 있어 데이터만 채우면 바로 해결) | 09-16 브론즈-B2 R11 등록(검색 다국어·카테고리명 검색 확인 중 발견) |
| A-079 | 2026-09-16 | 404 화면(404.html) — 사이트 최상위가 아니라 하위 경로(예: `categories/furniture/` 아래)에서 없는 페이지에 들어갔을 때 | 카테고리·매거진 등 깊은 경로에서 오타가 있는 주소나 삭제된 글 주소로 들어갔다(예: `categories/furniture/nonexistent-subcategory-xyz.html`) | 최상위에서 없는 주소로 들어갔을 때와 똑같이, 디자인이 그대로 살아있는 404 안내 화면이 뜬다 | 화면이 완전히 **꾸밈 없는 맨 HTML**로 뜬다 — 글꼴도 안 먹고, 검은 헤더 띠도 없고, 메뉴가 밑줄 친 파란 글씨 목록으로 줄줄이 나열되고, 골드 포인트 색도 전혀 없다. 안내 문구도 이번엔 한국어가 아니라 영어("Page Not Found")로만 뜸(사이트는 기본이 한국어인데) | 방문자가 사이트 안에서 링크를 따라가다 없어진 글이나 오타 주소를 만나는 게 가장 흔한 404 상황인데, 바로 그 경우에 ARCHINODE답지 않은 완전히 깨진 화면을 보게 된다. 루트 주소(`archinodekr.com/없는주소`)로 바로 들어갔을 때만 정상으로 보이는데, 실제로는 카테고리·매거진 등 깊은 경로 어디서든 같은 문제가 재현될 것으로 추정 | **High** | 검토대기 | `404.html`이 `style.css`·`favicon.ico`·`lang.js`·`search.js`·`auth-ui.js`·`cookie-consent.js`·`analytics-loader.js`·`sentry-loader.js` 등 모든 리소스를 **상대경로**(`href="style.css"` 등, 앞에 `/` 없음)로 불러옴. GitHub Pages는 없는 주소를 만나면 실제 위치와 무관하게 이 파일 하나를 그대로 돌려주는데, 브라우저는 상대경로를 "요청했던 그 주소"(`categories/furniture/`) 기준으로 풀기 때문에 `categories/furniture/style.css` 같은 존재하지 않는 경로를 요청하게 됨. 실측(네트워크 탭 직접 확인): `style.css`·`search.js`·`lang.js`·`auth-ui.js`·`cookie-consent.js`·`analytics-loader.js`·`sentry-loader.js` 전부 503, `favicon.ico`·`favicon-32x32.png`·`favicon-16x16.png` 전부 404 — 11개 리소스 전멸. `lang.js`가 안 불려서 한국어 전환도 같이 죽음(영어 하드코딩 상태로 보임). 반대로 사이트 루트 바로 아래에서 없는 주소로 들어가면(`archinodekr.com/없는주소.html`) 상대경로가 우연히 맞아떨어져 정상으로 보임 — 이래서 이전 라운드들이 못 잡았던 것으로 추정. 처방 후보: `404.html`의 모든 리소스 경로를 절대경로(`/style.css`, `/lang.js` 등, 맨 앞에 `/`)로 바꾸기 — GitHub Pages 커스텀 404 페이지의 잘 알려진 함정 | 09-16 브론즈-B2 R11 등록(sitemap 전수 스캔 이후 404 패턴 확인 중 발견, 네트워크 탭 증거 확보) · 09-16 브론즈-B2 R12 추가 조사: 같은 "상대경로 함정"이 다른 화면에도 있는지 확인 — **없음, 404.html에만 있는 문제로 범위 확정**. 실제 페이지들(카테고리 서브페이지·매거진 기사 등, 최대 2단계 깊이까지 실존)은 전부 자기 깊이에 맞는 상대경로(`../../style.css`·`../style.css` 등)를 정확히 쓰고 있어 정상 — 이건 파일이 항상 "자기 실제 위치"에서만 서비스되기 때문에 문제가 안 생김. 404.html만 특수한 이유: GitHub Pages가 **어느 주소에서 404가 나든 이 파일 하나를 그대로 돌려주는데, 요청 주소는 매번 다름**(실제 파일 위치는 고정인데 서비스되는 URL은 가변) — 이 "고정 파일 vs 가변 URL" 불일치가 있는 곳이 사이트에서 404.html이 유일함(다른 대체/리다이렉트용 페이지 없음, `grep`으로 확인). 실측: `categories/wellness/wellness-showers.html`(실제로 존재하는 2단계 깊이 페이지)을 직접 새로고침 2회 — `style.css` 등 전부 정상 200, 콘솔엔 기존 A-045(header 변수 중복)만 재현되고 새 문제 없음 |
| A-080 | 2026-09-16 | 제품 상세 화면(products/view.html)·브랜드 상세 화면(brands/view.html) — 화면 소스(스크린리더·검색엔진이 자바스크립트 실행 전에 보는 것) | 제품·브랜드 상세 화면을 열었다(화면 자체는 정상 확인됨, 이번엔 "JS가 실행되기 전엔 뭐가 있나"만 점검) | 제목(h1)이 페이지 뼈대에 이미 있고, 브랜드·제품 데이터가 로드되면 그 안 글자만 바뀐다 | 두 화면 다 h1 태그 자체가 **정적 HTML 어디에도 없고**, Firestore에서 데이터를 받아온 뒤 자바스크립트가 템플릿 문자열로 그 순간에 만들어 넣음 — 자바스크립트가 실행되기 전(또는 실행이 실패한 상황)에는 이 페이지에 제목이라는 게 아예 존재하지 않음 | 화면으로 직접 보는 사람에게는 문제가 없지만(자바스크립트가 정상 실행되고 나면 제목이 잘 보임), 자바스크립트를 늦게 실행하거나 아예 못 읽는 일부 환경(오래된 스크린리더, 일부 검색엔진 크롤러의 첫 패스)에서는 이 페이지에 제목이 하나도 없는 것처럼 보일 수 있음 | Low | 검토대기 | `products/view.html:363` `<h1 class="pp-name">${p.name}</h1>`, `brands/view.html:505` `<h1>${brand.brandName}</h1>` — 둘 다 Firestore 데이터 로드 콜백 안 템플릿 문자열에만 존재, 정적 HTML(`<body>` 바로 아래 등)에는 h1이 전혀 없음(다른 정적 페이지 7종 표본은 전부 h1 정확히 1개씩 정상 확인 — 이 두 동적 페이지만의 특징). alt 텍스트 누락은 표본 7개 페이지(index·categories/furniture·products/view·magazine 1편·brands·categories/bathroom/mirrors·about) 전부 0건으로 깨끗함, 헤딩 레벨 건너뛰기도 없음 — 접근성 전반은 양호하고 이 항목만 예외. 처방 후보: 정적 `<h1>` 자리를 미리 만들어두고("제품 정보를 불러오는 중입니다" 등 기본 문구) 데이터가 오면 textContent만 바꾸는 방식으로 전환 | 09-16 브론즈-B2 R12 등록(대표 템플릿 접근성 점검 중 발견) |
| A-081 | 2026-09-16 | 카테고리 대분류 화면(예: `categories/furniture.html`) 상단 메뉴 | 카테고리 대분류 화면(가구·욕실 등 10개 중 아무거나)에 들어가 상단 메뉴를 봤다 | "브랜드 매거진" 메뉴가 한 번만 보인다(다른 화면들과 동일하게) | 상단 메뉴에 "브랜드 매거진"이 **완전히 똑같은 링크로 두 번** 나온다(둘 다 `trend-report.html`로 감) — 메뉴가 "카테고리 · 브랜드 · 브랜드 매거진 · 매거진 · 브랜드 매거진 · 소개 · …" 순서로 어색하게 이어짐 | 방문자 입장에선 같은 메뉴가 왜 두 번 있는지 의아하고, 안 그래도 메뉴가 많은 상단바가 더 복잡해 보임(A-069처럼 좁은 화면에서 버튼이 눌리는 문제와도 겹쳐 상황을 더 악화시킬 수 있음) | Medium | 검토대기 | `categories/furniture.html:49`와 `:51` 두 줄에 `<a href="../trend-report.html" data-en="Brand Magazine" data-ko="브랜드 매거진">Brand Magazine</a>`가 그대로 중복 삽입돼 있음 — **10개 대분류 페이지 전부**(furniture·bathroom·outdoor·lighting·kitchen·office·finishes·wellness·decor·construction) 동일하게 2개씩 확인(`grep`으로 10/10 전수 확인). 서브카테고리 69개는 이 중복이 없음(1개씩 정상) — 대분류 페이지만의 문제. 실브라우저(`read_page`)로도 메뉴에 "브랜드 매거진"이 두 줄 뜨는 것 확인. 처방 후보: 10개 대분류 페이지에서 두 번째 "Brand Magazine" 링크 줄 삭제(찾아바꾸기 가능한 기계적 작업) | 09-16 브론즈-B2 R13 등록(카테고리 대분류 화면 자율 탐색 중 발견) |
| A-082 | 2026-09-16 | 카테고리 대분류·서브카테고리 화면(카테고리 관련 79개 전부) 푸터 "서비스"·"회사" 영역의 "매거진" 링크 | 카테고리 화면 맨 아래 푸터에서 "브랜드 매거진"이라고 적힌 링크를 눌렀다 | 브랜드 매거진(trend-report.html, 유럽 브랜드가 쓴 트렌드 글 모음)으로 간다 | 실제로는 **일반 매거진(magazine.html)**으로 간다 — 링크 자체(`href`)는 magazine.html인데 화면에 뜨는 한글 글자만 "브랜드 매거진"으로 잘못 붙어 있음. 같은 푸터에 진짜 브랜드 매거진 링크는 아예 없음(1개짜리 링크에 이름표만 잘못 붙임) | 방문자가 "브랜드 매거진"을 보고 눌렀는데 실제로는 일반 매거진이 열려서 원하는 걸 못 찾고, 반대로 카테고리 화면에서는 진짜 브랜드 매거진(trend-report.html)으로 가는 링크가 푸터에 없어서 그쪽을 찾으려면 상단 메뉴로 다시 가야 함 | Medium | 검토대기 | `categories/furniture.html:221`·`:229`(대분류) 및 서브카테고리 전부(`categories/bathroom/mirrors.html:213`·`:221` 등)에서 `<a href="../magazine.html"><span data-en="Brand Magazine" data-ko="브랜드 매거진">Magazine</span></a>`처럼 **링크 주소는 magazine.html인데 이름표(data-en/ko)만 "Brand Magazine"/"브랜드 매거진"**으로 붙어 있음(정상 페이지, 예: index.html 푸터는 "Magazine"→magazine.html과 "Brand Magazine"→trend-report.html 두 링크가 각각 올바르게 따로 있음 — 대조군). `grep` 전수 확인: **카테고리 79개(대분류 10 + 서브카테고리 69) 전부** 이 오표기가 있고, `categories/` 폴더 밖의 다른 페이지 9종(magazine·brands·about·contact·for-brands·trend-report·digital-products·request-consultation·submit-trend)은 전부 정상 — `categories/` 전용 템플릿 문제로 범위 확정. 부수 관찰(같은 조사 중 발견, 별도 번호 안 만듦): 헤더의 "브랜드 입점" 버튼 한글 표기도 페이지마다 다름 — index.html은 "브랜드 입점", categories/furniture.html은 "브랜드 등록"(영어는 둘 다 "List Your Brand"로 동일) — 같은 버튼인데 문구가 안 맞음, Low. 처방 후보: 카테고리 79개 파일의 해당 두 줄을 `data-en="Magazine" data-ko="매거진"`으로 고치거나(간단), 아예 진짜 브랜드 매거진 링크를 하나 더 추가(index.html 패턴과 통일) | 09-16 브론즈-B2 R13 등록(카테고리 화면 자율 탐색 중 발견, 79개 전수 grep 확인) · 09-16 브론즈-B2 R14 사이트 전체 재검증: "링크 글자 vs 실제 목적지" 대조 스크립트를 공개 페이지 216개 전체에 돌려 재확인 — "브랜드 매거진"이라는 글자가 걸린 링크가 사이트 전체에서 정확히 516개인데, 그중 357개는 정상(trend-report.html), **158개가 이 항목(magazine.html로 잘못 감)**, 나머지 1개는 A-083(아래)으로 확인 — `categories/` 폴더 밖에는 이 오표기가 전혀 없음을 전수로 재확인 |
| A-083 | 2026-09-16 | 매거진 화면(magazine.html) 맨 위 대표 기사 카드("테일러드 클래식과 어스톤의 시대…") | 매거진 화면에 들어가서 맨 위 큰 기사 카드에 붙은 꼬리표를 봤다 | 이 기사가 무슨 성격의 글인지 알려주는 꼬리표가 뜬다(예: "정책"·"디자인"처럼 목록의 다른 기사들과 같은 방식) | 꼬리표가 "TREND REPORT"(영어)·"브랜드 매거진"(한국어)라고 뜨는데, 이 기사는 실제로는 일반 매거진 글(`magazine/tailored-classic-earth-tone-2026-korea.html`)이지 브랜드 매거진(trend-report.html) 글이 아님 — 페이지 제목도 "ARCHINODE Magazine"으로 돼 있어 일반 매거진 소속이 맞음 | 방문자가 매거진에 들어가자마자 가장 크게 보이는 대표 기사에 "브랜드 매거진"이라는 잘못된 꼬리표가 붙어 있어, 이 글이 유럽 브랜드가 직접 쓴 글인 줄 오해할 수 있음. 게다가 영어("TREND REPORT")와 한국어("브랜드 매거진")조차 서로 다른 개념처럼 안 보임(하나는 "리포트", 하나는 "매거진") | Low | 검토대기 | `magazine.html:445` `<span class="featured-tag" data-en="TREND REPORT" data-ko="브랜드 매거진">TREND REPORT</span>` — 대표 기사 카드에 하드코딩된 꼬리표가 실제 글 성격과 안 맞음. 사이트 전체 "브랜드 매거진" 텍스트 링크·꼬리표 516개를 스크립트로 전수 대조한 결과 이 1건만 유일하게 예외였음(나머지는 A-082 또는 정상). 처방 후보: 이 대표 기사 카드의 꼬리표를 실제 소속에 맞게 "FEATURED"/"추천" 또는 이 글의 진짜 주제 태그(예: "인테리어"/"트렌드")로 교체 | 09-16 브론즈-B2 R14 등록(사이트 전체 링크-라벨 대조 스크립트로 발견) |
| A-084 | 2026-09-16 | 브랜드 온보딩 가이드 화면(brand-guide.html) 상단 카테고리 드롭다운 메뉴 | 상단 메뉴의 "Categories"를 눌러 하위 카테고리 10개를 봤다 | 각 카테고리 이름 아래 작은 글씨로 한국어 이름이 같이 뜬다(다른 화면들처럼, 예: "Furniture" 아래 작게 "가구") | 작은 글씨 자리에 한국어 대신 **영어 단어가 그대로 또 한 번** 찍혀 있음 — "Furniture" 아래 작게 또 "Furniture", "Bathroom" 아래 또 "Bathroom" 식으로 10개 카테고리 전부 자기 이름이 두 번씩 겹쳐 보임 | 사소하지만 눈에 딱 띄는 복사-붙여넣기 실수처럼 보여서(같은 단어가 큰 글씨·작은 글씨로 두 번) 이 페이지만 유독 덜 다듬어진 인상을 줌. brand-guide.html은 원래 영어 전용 화면이라 굳이 한국어가 필요한 자리는 아니지만, 그렇다고 같은 영어 단어를 두 번 넣는 건 의도된 디자인으로 보기 어려움 | Low | 검토대기 | `brand-guide.html:89~98` 카테고리 드롭다운 10줄 전부 `<span>Furniture</span><small>Furniture</small>` 처럼 `<small>` 안에 한국어("가구") 대신 영어 단어가 그대로 복사돼 들어가 있음(다른 다국어 페이지들의 같은 드롭다운은 `<small>가구</small>`로 정상). brand-guide.html 자체는 `<html lang="en">` 고정·`data-en/ko` 속성 0개인 완전 영어 단일 페이지라 원래 `<small>`에 아무것도 안 넣거나 태그 자체를 빼는 게 맞아 보임 — 지금처럼 같은 단어를 넣은 건 실수로 보임. 콘솔 오류는 없음(화면 자체는 정상 작동). 처방 후보: 10줄의 `<small>Furniture</small>` 등을 지우거나(영어 단일 페이지 취지에 맞게), 다른 페이지처럼 한국어 이름을 채워 넣기 | 09-16 브론즈-B2 R14 등록(사이트 전체 링크-라벨 대조 스크립트로 발견) |
| A-085 | 2026-09-16 | 자문 요청 화면(request-consultation.html) 폼 전체 | 폼을 끝까지 채우되 일부러 "구체적인 상황 설명" 칸을 아주 짧게만 쓰고(100자 미만), 맨 아래 "요청 보내기" 버튼을 눌렀다 | 방금 누른 버튼 근처에서 "설명을 더 자세히 써 달라"는 안내가 바로 눈에 띄게 나타난다 | 버튼을 눌러도 화면이 전혀 움직이지 않고 아무 반응도 안 보인다 — 안내 문구는 실제로 만들어지긴 했지만, 지금 보고 있는 화면(맨 아래 버튼 근처)이 아니라 **폼 맨 위, 화면 밖으로 800px 넘게 벗어난 자리**에 조용히 나타날 뿐이라 스크롤을 올려보지 않는 한 아예 안 보인다. 게다가 그 문구 자체도 페이지의 다른 모든 글자는 한국어인데 이 안내만 "Please provide at least 100 characters..."처럼 **영어로만** 뜬다. 같은 방식으로, 연락 동의 체크박스를 안 누르고 제출했을 때 뜨는 안내("Please confirm the contact consent to submit.")도 영어 전용, 제출 중 버튼 글자("Submitting...")도 영어 전용 | 방문자 입장에서는 "요청 보내기"를 눌렀는데 화면이 그대로라 버튼이 고장 났다고 오해하기 쉽고, 실제로는 제출이 막힌 이유(설명이 짧다)조차 못 보고 포기하고 떠날 위험이 크다. 자문 요청은 회사 매출과 직결되는 핵심 리드 창구라 이탈 영향이 특히 크다 | High | 검토대기 | `request-consultation.html` `submitConsult()`(431행~) 안의 두 커스텀 검증 — `agreeContact` 미동의(449~452행 `errorDiv.textContent='Please confirm the contact consent to submit.'`)·`description.length<100`(454~457행 `errorDiv.textContent='Please provide at least 100 characters...'`) — 이 실행되면 `#formError`의 `display`만 `block`으로 바꿀 뿐 `scrollIntoView` 등 위치 이동이 전혀 없음. 실측: `#formError`의 문서 기준 절대 top≈978px인데 `#submitBtn`은 top≈1809px로 830px 이상 아래(즉 폼 맨 위, 첫 입력칸보다도 위)에 위치 — 버튼을 누른 시점 스크롤 위치에서는 완전히 뷰포트 밖. 스크린샷 실측으로 아무 것도 안 보이는 것 확인, JS로 `errorDivDisplay:"block"`·`errorDivText` 값이 실제로 채워졌음은 별도 확인(콘솔 조회, `confirm/prompt` 아님). 두 메시지 모두 `data-en`/`data-ko` 속성이 아니라 JS 안에 영어 문자열이 그대로 박혀 있어 한국어 모드에서도 안 바뀜(460행 `btn.textContent='Submitting...'`도 동일 패턴). 참고: `agreeContact`·`description` 모두 HTML `required` 속성이 있어 **아예 빈 채로 제출하면** 브라우저 기본 안내가 먼저 뜨고(실측: 크롬이 한국어로 "계속하려면 이 확인란을 선택하세요." 등 정상 출력, 이 부분은 정상) 커스텀 메시지는 안 나옴 — 이번에 찾은 문제는 필드를 채우긴 했지만 기준(100자·동의)을 못 채운 경우에만 재현되는, 좁지만 실사용자가 충분히 만날 수 있는 경로(설명을 짧게 쓰는 사람은 흔함). 처방 후보: `errorDiv.scrollIntoView({behavior:'smooth', block:'center'})`를 두 검증 분기에 추가 + 두 메시지·로딩 버튼 글자에 `data-en`/`data-ko` 방식 또는 최소한 현재 언어를 보고 분기하는 처리 추가. 대조군: `digital-products.html`의 알림 신청 폼은 실패 시 `alert()`(항상 화면 어딘가에 강제로 보임, 다만 A-077과 같은 팝업 문제는 있음)라 이 "안 보임" 문제는 없음 | 09-16 브론즈-B2 R15 등록(폼 유효성 검사 문구·버튼 로딩 상태 라운드, 실브라우저 재현 + 절대좌표 실측). 참고: 같은 라운드에서 콘솔에 `data is not defined`(관리자 알림 메일 누락) 경고가 다시 재현됐으나 이는 기존 A-064(A-021 기준 판정)와 동일 결함 — 재등록 안 함, "09-16 재확인됨"으로만 남김 |
| A-086 | 2026-09-16 | 전 공개 페이지 공통 맨 위 검은 띠 배너("2026 Brand Listing — Now Free \| List your brand..."), 21개 화면(404·소개·브랜드 온보딩 가이드·브랜드 상세·카테고리 대분류 10개 전부·디지털 상품·브랜드 입점 안내·개인정보처리방침(한/영)·자문 요청·브랜드 매거진 제출·이용약관) | 한국어 모드에서 이 21개 화면 중 아무거나 열었다(예: about.html) | 화면의 다른 모든 글자(메뉴·제목·본문)처럼 이 맨 위 띠도 한국어로 보인다 | 화면 전체가 한국어인데 맨 위 띠 배너만 "2026 Brand Listing — Now Free \| List your brand on ARCHINODE at no cost. Learn more →"로 **항상 영어 그대로** 나온다 — 같은 문구가 index.html 등 다른 194개 화면에서는 정상적으로 "2026 브랜드 입점 — 무료 \| ARCHINODE에 무료로 브랜드를 등록하세요. 자세히 보기 →"로 한국어로 잘 뜨는 것과 대조됨 | 방문자가 화면에 들어가면 가장 먼저 보이는 맨 위 줄이 영어라 "한국어 사이트인데 첫 줄부터 안 맞는다"는 인상을 주고, 특히 이 21개 화면에는 방문자가 처음 들어오는 진입 화면(about.html·for-brands.html·request-consultation.html 등)과 카테고리 10개 전부·브랜드 상세 페이지(전체 승인 브랜드 공통)가 포함돼 있어 영향 범위가 넓음 | High | 검토대기 | 맨 위 띠(`<div class="announcement-bar">`)가 `index.html:781~783`에서는 각 조각(`<span>`·`<strong>`·`<a>`)마다 `data-en`/`data-ko`가 정상적으로 달려 있어 `lang.js`가 문제없이 번역하는데, 정확히 같은 문구를 복사해 넣은 나머지 페이지들 중 21개(`grep -rL 'data-en="2026 Brand Listing'` 로 전수 확인)는 `data-en`/`data-ko` 속성 자체가 통째로 빠진 순수 영어 하드코딩(`about.html:35` 등)이라 애초에 `lang.js`가 손댈 자리가 없음 — A-057·A-059·A-071 등(자식 태그 때문에 번역이 "일부만" 되는 유형)과 원인이 다른, "속성 자체가 없어 통째로 안 되는" 유형. 정상 194개·깨짐 21개 파일 목록 전수 확인 완료: 깨짐 21개 = `404.html`·`about.html`·`brand-guide.html`·`brands/view.html`(동적, 즉 승인된 브랜드 상세 페이지 전부)·카테고리 대분류 10개 전부(`categories/bathroom.html` 등, 서브카테고리 69개는 정상이라 대분류 전용 문제)·`digital-products.html`·`for-brands.html`·`privacy.html`·`privacy-en.html`·`request-consultation.html`·`submit-trend.html`·`terms.html`. 처방 후보: 21개 파일의 `<div class="announcement-bar">` 안쪽을 index.html의 `data-en`/`data-ko` 포함 마크업으로 그대로 교체(간단한 찾아바꾸기로 일괄 처리 가능해 보임) | 09-16 브론즈-B2 R17 등록(배율·언어토글 반복 클릭 변형 라운드 중 about.html에서 발견, `grep` 전수 확인) |
| A-087 | 2026-09-16 | 브랜드 상세 화면(brands/view.html — 승인된 모든 브랜드 페이지 공통) 맨 아래 푸터 전체 | 한국어 모드에서 실제 공개 중인 브랜드 상세 페이지(예: oska)를 열고 맨 아래까지 내려봤다 | 푸터의 "카테고리"·"회사"·"브랜드를 위한"·"법적 고지" 같은 항목 제목과 그 아래 링크들(가구·욕실·소개·매거진·문의·브랜드 입점 등)이 한국어로 보인다 | 푸터 전체(4개 섹션 제목 + 그 아래 링크 12개 전부)가 "Categories/Furniture/Bathroom/Lighting/Kitchen"·"Company/About/Magazine/Contact"·"For Brands/List Your Brand/Brand Login"·"Legal/Terms/Privacy"처럼 **처음부터 끝까지 전부 영어로만** 나온다. 같은 화면의 맨 위 태그라인("글로벌 건축·디자인 브랜드를...")과 본문은 정상적으로 한국어인데 푸터만 통째로 예외 | 방문자가 화면을 처음 열 때는 정상적으로 한국어로 보이다가, 아래로 내려가 다른 카테고리·회사 정보를 찾으려는 순간 갑자기 전부 영어로 바뀌어 있어 어색함. 지금은 승인 브랜드가 2개뿐이라 영향이 작아 보이지만, **브랜드가 승인될 때마다 새로 생기는 페이지마다 전부 이 증상이 나타나는 구조적 문제**라 브랜드 수가 늘수록 영향도 같이 커짐 | High | 검토대기 | `brands/view.html` 299~324행의 `<footer>` 블록 전체 — `<h4>Categories</h4>`·`<h4>Company</h4>`·`<h4>For Brands</h4>`·`<h4>Legal</h4>`와 그 아래 `<li><a>` 12개 전부 `data-en`/`data-ko` 속성이 하나도 없는 순수 하드코딩 영어(305~323행). 대조군: 같은 성격의 `products/view.html` 187~216행은 정확히 같은 자리에 `<h4 data-en="Categories" data-ko="카테고리">`처럼 속성이 정상적으로 달려 있어, **브랜드 상세 페이지 템플릿만 이 푸터 블록을 통째로 빠뜨린 것**으로 보임(A-086의 최상단 배너 결함과 같은 "복사 시 data-en/ko 속성 누락" 계열이지만, 그 항목은 21개 파일의 배너 한 줄 문제였고 이건 brands/view.html 한 파일의 푸터 전체(약 15개 텍스트)라는 점에서 범위·파일이 다름 — 별도 등록). 실측: `products/view.html?id=zzz`(없는 id)와 `brands/view.html?id=zzz`를 한국어 모드로 나란히 열어 화면 텍스트 직접 대조, 소스에서 `data-en`/`data-ko` 유무 확인. 처방 후보: `brands/view.html` 305~323행에 `products/view.html` 패턴 그대로 `data-en`/`data-ko` 속성 추가 | 09-16 브론즈-B2 R23 등록(A-061 재현 확인 중 부수 발견, products/view.html과 화면 대조로 확정) · 09-16 브론즈-B2 R24 추가 확인: "찾을 수 없습니다" 화면이 아니라 **실제로 지금 공개돼 있는 정상 브랜드 페이지**(`brands/view.html?id=oska`)에서도 완전히 동일하게 재현됨을 확인("CATEGORIES/COMPANY/FOR BRANDS/LEGAL" 전부 영어) — 오류 화면만의 문제가 아니라 방문자가 실제로 보게 되는 정상 브랜드 상세 화면 전부에 해당하는 결함임을 확정 · 09-16 브론즈-B2 R26 범위 추가: 같은 화면 맨 위 "Home / Brands / (브랜드명)" **브레드크럼도 같은 이유로 영어 고정**(`brands/view.html:279~283`, `<a href="../index.html">Home</a>`·`<a href="../brands.html">Brands</a>` 둘 다 `data-en`/`data-ko` 없음)임을 추가 확인 — 이 화면은 상단 메뉴(카테고리 드롭다운·브랜드 입점 버튼)는 정상 번역되는데 브레드크럼과 푸터 두 군데만 번역이 안 된 상태(대조군 `products/view.html`은 브레드크럼에 `data-en="Home" data-ko="홈"`이 정상적으로 붙어 있음) — 페이지 전체가 아니라 특정 영역만 번역이 누락된 패턴이라는 것을 보여주는 추가 증거 |
| A-088 | 2026-09-16 | 브랜드 입점 안내 화면(for-brands.html) 맨 아래 푸터 · 브랜드 매거진 글 상세 화면(magazine/view.html — 브랜드가 직접 작성한 매거진 글 공통 템플릿) 맨 아래 푸터 | 한국어 모드에서 두 화면을 열고 맨 아래까지 내려봤다 | 다른 글자들처럼 푸터의 "카테고리"·"브랜드를 위해"·"회사"(또는 "법적 고지") 같은 항목 제목과 그 아래 링크들도 한국어로 보인다 | 두 화면 모두 푸터 항목 제목(for-brands.html: "Categories"·"For Brands"·"Company" 3개 / magazine/view.html: "Categories"·"Company"·"For Brands"·"Legal" 4개)과 그 아래 링크 전부(가구·욕실·소개·문의·매거진·이용약관 등 총 13~9개)가 **항상 영어로만** 나온다 | A-087(brands/view.html 푸터)과 같은 증상이 다른 두 화면에서도 나타남 — for-brands.html은 브랜드 유치를 설득하는 핵심 B2B 랜딩 화면이라 첫인상에 영향이 크고, magazine/view.html은 **브랜드가 직접 작성해 올린 매거진 글**마다 뜨는 공통 템플릿이라 브랜드 콘텐츠가 늘수록 영향도 커지는 구조 | High | 검토대기 | 사이트 전체 216개 공개 HTML을 스크립트로 훑어 "이 페이지는 다른 곳엔 `data-en`을 정상적으로 쓰면서 푸터의 `<h4>` 제목만 `data-en` 속성이 빠진 경우"를 찾음(A-087 발견 직후 같은 패턴이 더 있는지 전수 확인) — 영어 단일 페이지(brand-guide.html·list-your-brand.html, `<html lang="en">` 고정이라 애초에 번역 대상이 아님, 제외)를 빼고 나면 **for-brands.html·magazine/view.html 2개가 신규로 걸림**(brands/view.html은 A-087로 이미 등록). `for-brands.html`은 푸터 전체(`<footer>`부터)가 공유 `.footer`/`.footer-col` 클래스가 아니라 인라인 스타일로 완전히 별도 제작된 블록이라(799~832행) `<h4>Categories</h4>` 등 3블록·13개 링크는 물론 **맨 위 태그라인("Connecting global architecture...")까지 `data-en`/`data-ko`가 단 하나도 없음** — brands/view.html·magazine/view.html은 그래도 태그라인 한 줄은 속성이 붙어 있는 것과 달리 이 페이지는 푸터가 통째로 예외. 실측: 한국어 모드로 열어 콘솔로 푸터 텍스트 직접 조회, "CATEGORIES/Furniture/.../COMPANY/About/Terms/Privacy" 전부 영어로 확인. `magazine/view.html:125~143`은 `brands/view.html`과 **글자 하나까지 완전히 동일한 마크업**(`<div class="footer-col"><h4>Categories</h4><ul>...`)이 통째로 복사된 것으로 보이며 역시 속성 전무. magazine/view.html은 `<html lang="ko">`이고 Firestore에서 글을 읽어와 렌더하는 실제 작동하는 동적 페이지(정적 사이트맵엔 안 잡히지만 브랜드가 작성한 매거진 글의 실제 상세 주소로 추정)임을 소스 확인(쿼리스트링으로 글 id를 읽어 Firestore 조회하는 코드 존재). 처방 후보: 두 파일의 해당 블록에 `products/view.html`·index.html 패턴처럼 `data-en`/`data-ko` 속성 추가(3개 파일이 전부 같은 항목이라 함께 처리하면 효율적) | 09-16 브론즈-B2 R25 등록(A-087 발견 직후 같은 패턴이 사이트에 더 있는지 스크립트로 전수 스캔, brand-guide.html·list-your-brand.html은 영어 단일 페이지라 제외 확인) · 09-16 브론즈-B2 R26 범위 추가: `magazine/view.html:234~236`의 "Home / Magazine / (글 제목)" 브레드크럼도 `data-en`/`data-ko` 없이 하드코딩돼 있음을 추가 확인(brands/view.html과 완전히 같은 패턴 — 헤더는 정상, 브레드크럼·푸터만 누락) |

브론즈-E R1 갱신(2026-09-15, 공개 폼 5종 E2E 라운드): 자문 요청(request-consultation.html)·디지털 상품 알림(digital-products.html)·문의(contact.html)·뉴스레터(index.html) 4개 폼은 유효성 검사(필수·이메일 형식)와 정상 제출 모두 실측 확인, **A-064~A-068 신규 등록**(관리자 알림 메일 실패·좁은 화면 레이아웃 3건·Formspree 이탈). **A-069 신규 등록**(요청 범위 밖 1024px 우연 발견). **트렌드 투고는 확인 대상에서 제외** — `submit-trend.html`이 더 이상 제출 폼이 아니라 "글 발행은 브랜드 포털에서"로 안내만 하는 정보 페이지로 바뀌어 있음(리포 전체에 `trend-submissions` 컬렉션에 쓰는 공개 폼 코드가 없음, admin 쪽 읽기/수정 코드만 존재) — 버그 아님, 설계가 바뀐 것으로 판단해 결함 등록 안 함. **로그인 필요한 확인(검수관리 탭 실제 등록, admin 화면으로 mail/digital-products-notify/newsletter-subscribers 문서 생성 재확인)은 브리지 열쇠 발급이 이번 라운드에서 환경 classifier에 막혀 못 함** — 위 A-064~A-069는 어드민 탭이 아니라 이 문서에 직접 기록(절차대로). 병렬 라운드가 이 파일을 실시간으로 같이 쓰고 있는 것을 확인함(git status에 다른 프로세스가 만든 미커밋 변경 발견, 번호 채번 직전 재확인으로 충돌 회피).

브론즈-R E2 갱신(2026-09-15, 이어서 라운드): 0단계에서 라이브=로컬 판 대조 통과(커밋 `7e80b6d`), `tools/bronze-token.js` 어드민 열쇠 발급 정상 작동(직전 라운드의 환경 classifier 차단이 이번엔 재현 안 됨 — `wool21wool@gmail.com` 로그인 성공, `/admin/dashboard.html` 정상 착륙). **A-053/A-055 재검증**: A-053(데스크톱 세로 축소) 자체는 다시 안 봄, 대신 A-055(모바일 폭 펼침 메뉴)를 375×700 뷰포트 실측 — "잠재적 재발 위험"이라던 원래 평가와 달리 **지금 당장 6개 메뉴(Articles Cards·Notices·Activity Log·Review Board·Regulations·Settings)가 완전히 가려져 진입 불가능**함을 확인, 검수관리 탭에 **A-040**으로 등록(평가 상향 반영). **A-056 재확인**: `auth/reset-password.html`에 firestore-compat 스크립트 누락 + `firebase-config.js:30` 무가드 호출 조합이 여전함을 코드로 재확인하고, 실브라우저에서 `TypeError: firebase.firestore is not a function` 재현(기능 영향 없음도 재확인) — 검수관리 탭에 **A-039**로 등록. **직전 라운드(E) 등록 재시도**: 로그인이 이번엔 되어 A-064~A-069를 검수관리 탭에 등록 — A-065→**A-034**, A-066→**A-035**, A-067→**A-036**, A-068→**A-037**, A-069→**A-038**. A-064는 등록 과정에서 오늘 먼저 등록됐던 **A-021과 완전히 같은 결함**임을 뒤늦게 발견 — 이미 A-033으로 중복 등록한 뒤 "A-021과 중복, 판정은 A-021 기준" 메모를 남김(교훈: 다음부턴 새 항목 등록 전에 먼저 화면·문구로 기존 목록을 훑을 것). **R6 반응형 확장**: `magazine.html`·`brands.html`·`categories.html` 322/375/768px 전부 가로 스크롤 없음(정상, 신규 결함 없음). `admin/dashboard.html`은 로그인 상태로 375px 실측해 위 A-055/A-040 발견. `brand-portal/dashboard.html`은 **확인 못 함** — `qa-brand@archinodekr.com` 열쇠 로그인을 2회 시도했으나 매번 "Access denied. Admin only." 얼럿과 함께 실패해 `index.html`로 리다이렉트(로그인 안 된 상태), 브랜드 계정 브리지 자체의 문제인지 계정 상태 문제인지는 미확인 — 무한 재시도하지 않고 중단, 사람 확인 필요.

R12 갱신(2026-09-12 밤, 브론즈 로그인 라운드): **A-040 확인완료**(포털 콘솔 오류 0, 제품·글 목록 정상 로드) · **A-042 확인완료**(숨기기 정상, 토스트·배지·새로고침 유지 — R11의 "상태 불변"은 재현 안 됨) · **A-041 재수정요청**(공지는 검색됨, 1:1 문의는 그 화면을 열기 전엔 캐시가 없어 미검색 → 로그인 시 선로드로 보완 예정).

R13 갱신(2026-09-14, 부재중 사다리 「다」 단 브론즈 마라톤): **A-038 확인완료**(어드민 상단바 322px 실측 통과) · **A-039 확인완료**(포털 탭바 322px 실측 통과, 단 같은 화면 상단바는 별도 결함) · **A-041 확인완료**(문의 탭 안 열고 검색해도 결과에 나옴, 배지=목록 수 일치) · **A-043 재현 안 됨**(여러 화면에서 색인 오류 못 찾음, 상태는 검토대기 유지) · **A-049 신규 등록**(브랜드 포털 상단바 322px에서 "Brand Guide"·"로그아웃" 글자 쪼개짐, 탭 A-016) · **7b 유입경로**(`list-your-brand.html?ref=qa-brz-01` → `sessionStorage.archinode_ref` 정상 저장 확인, 실제 제출→Firestore→Referral Source 표시는 확인 못 함, 신청서 제출은 하지 않음).

A-019 보강(블랙 563f415): `search-index.json`도 같은 30건 누락. 복구 41건 중 12건은 sitemap·검색 색인 둘 다 없음 — 자동 발행이 두 파일을 갱신하지 않는 것으로 추정. 우선 Medium으로 올림.

재발견(2026-09-12 브론즈): A-001(전 공개 페이지 콘솔 + `category-products.js` `db is not defined`), A-002, A-012(`/sales/…`·`/firestore.rules`·`/tools/deploy.ps1`·`/docs/*.md` 전부 HTTP 200).

## 닫힌 건

| 번호 | 등록일 | 화면 | 무엇을 눌렀나 | 기대 | 실제 | 문제 | 우선 | 상태 | 개발 메모 | 이력 |
|---|---|---|---|---|---|---|---|---|---|---|
| A-040 | 2026-09-12 | 브랜드 포털 대시보드 | 포털을 연다 | 조용히 뜬다 | 뒤에서 오류 2건이 매번 난다 | 제품·글 목록 조회가 색인을 요구. 목록이 비어 보일 수 있다 | **High** | 확인완료 | 탭 A-014. Firestore `failed-precondition`(복합 색인) — `brand-portal/dashboard.html:1105·1133` `where brandId == uid` + `orderBy createdAt desc`(기존 코드 — **4월부터 실브랜드의 제품·글 목록이 조용히 실패했을 가능성**). `firestore.indexes.json`(products·articles brandId ASC + createdAt DESC) 신설·`firebase.json` 연결·`deploy --only firestore:indexes` 성공(1bff5ab). 색인 빌드 뒤 브론즈가 포털 콘솔 0 확인 | 09-12 브론즈 R11 · 09-12 지휘 창 색인 배포 완료 → 확인대기 · 09-12 브론즈 R12 확인완료(포털 콘솔 오류 0, 제품·글 목록 정상 로드) |
| A-042 | 2026-09-12 | 어드민 공지사항 | 공지의 「숨기기」를 누른다 | 노출이 꺼진다 | 상태가 안 바뀐다 | 노출 끄기가 안 되면 잘못 올린 공지를 못 내린다 | Medium | 확인완료 | 브론즈 R11 "클릭해도 상태 불변". 확인창 없이 인페이지 토글이어야 함 — `view-notices.js` 토글 핸들러 확인 필요(이벤트 위임·data-id). **구현(09-12 화이트)**: 코드검토(위임 `data-ntc-toggle` → `ntc_toggleVisible` → `update({visible,updatedAt,updatedBy})` → `logAdmin` → `notices_draw`, confirm 없음)·vm 주입 실측 모두 정상 → **원인 특정 못 함(재현 안 됨)**. 방어 수정: 저장 중 무시 대신 토스트 · 버튼 잠금 · 성공 시 «지금 목록» 항목을 id 로 갱신해 먼저 다시 그린 뒤 토스트·로그 · `notices_load()` 로 서버 값 재읽기(`ntc_save` 와 동일). | 09-12 브론즈 R11 · 09-12 화이트 방어 수정 → 확인대기 · 09-12 브론즈 R12 확인완료(숨기기 정상, 토스트·배지·새로고침 유지 — R11의 "상태 불변"은 재현 안 됨) |
