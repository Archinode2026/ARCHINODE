# ARCHINODE 작업일지

세션이 끝날 때마다 맨 위에 날짜별로 한 덩어리씩 쌓는다.
**다음 세션은 이 파일 맨 위 "다음에 할 일"부터 읽고 시작할 것.** ("종료" 절차가 이 절을 갱신한다.)

이 파일이 **"다음에 할 일"의 유일한 창구**다. 2026-09-12까지 다섯 곳(루트 `CLAUDE.md` 12절, `ARCHINODE-미완료업무.md`(4/22), `archi-diary/handoff-2026-07-02-final.md`, `archi-diary/_migration/MIGRATION.md` 6절, `docs/deferred-work-2026-07-02.md`)에 흩어져 있던 할 일을 여기로 합쳤다. 그쪽 문서는 **이력으로만 남기고 더 갱신하지 않는다.**

---

## 다음에 할 일

> 갱신: 2026-09-17 (종료 절차, 9/16 저녁~9/17 아침 마라톤 마감). 브론즈 3트랙(어드민·공개페이지·폼)이 밤새 도는 동안 결함이 A-069→**A-138**까지 급증(신규 약 69건), **검토대기가 34건→113건으로 폭증** — 판정 병목이 지금 최우선 문제. 마지막 push `3cca1a2`. 아래 이력 참조.
> ★ 결함 목록은 이 파일이 아니라 **[검수대장](검수대장.md)** 또는 어드민 > 검수관리 탭이 정본이다. 여기엔 "무엇을 언제 왜"만 적는다.

### A-0. 한울님 결정 필요 (급한 순)

- **🔴🔴 A-126 — 이 프로젝트 내부 문서 전체가 지금도 공개 사이트에 그대로 노출 중** — `archinodekr.com/docs/검수대장.md`(미해결 취약점 목록 포함)·`/docs/firestore-schema.md`(DB 구조)·`/CLAUDE.md`·`/.claude/`·`/tools/`가 전부 HTTP 200. **응급 배관공사는 이미 만들어 push했음**(`.github/workflows/deploy-pages.yml`, 커밋 `e95e7ef` — `docs/tools/scripts/.claude`는 git 추적은 그대로 두고 배포 산출물에서만 제외). **한 단계만 남음, 한울님만 할 수 있음**: GitHub 저장소 `Archinode2026/ARCHINODE` → Settings → Pages → Build and deployment → Source를 **"Deploy from a branch"에서 "GitHub Actions"로 변경**. 바뀌는 순간 적용되고, 이 리포 로컬 작업이나 브론즈 마라톤은 전혀 안 끊긴다.
- **🔴 검토대기 113건 — 판정 병목.** 브론즈가 밤새 워낙 많이 찾아서 사람 판정 속도를 완전히 앞질렀다. 전부 다 보시기보다 검수대장에서 **High만 먼저** 훑는 걸 추천(A-104·A-105·A-107·A-086~088 등 이미 High로 표시된 것 다수).
- **A-058~060 세 결함 번호 충돌** — 라이브 어드민 검수관리 탭(자동 채번)과 이 리포의 `docs/검수대장.md`(수동 채번)가 각각 다른 결함에 같은 번호를 배정했다. 여러 브론즈 세션이 동시에 등록하며 생긴 구조적 문제 — 지휘 창이 다음 세션에서 정리 예정(오늘은 손 안 댐, 더 꼬일 위험).
- **A-046(카테고리 색인)** ✅ 09-16 배포 완료·69/69 전수 재확인 끝남(더 이상 신경 안 써도 됨).
- **A-012(sales/silvia 노출)** ✅ 09-16 저장소에서 제외·push 완료.
- **어제(9/16) 그레이·실버 조사 3건 나옴, 결정 3가지 대기** — `docs/리서치/` 폴더: 글로벌 8개 플랫폼 갭 31건·한국 시장 갭 17건·실버 우선순위 정리. 실버 결론은 "지금은 신규 기능 말고 A절(검수) 계속"이지만, 아래 3가지는 언제든 결정 가능:
  1. GA4 켤지(`GA_ID` 발급 — 계정 작업만, 개발 아님)
  2. 카카오톡 채널 상담을 지금 넣을지(규정집 1-7 "채팅은 나중"과 같은 건지 여쭤야 함)
  3. 이커머스(카트·결제·가격노출) 안건화 여부(규정집 1-1·1-3에 이미 "안 한다"고 돼 있음, 재검토 여부만)
- **매거진 자동 발행 파이프라인(Cowork 계정 스킬 mag-*) 템플릿 수정** — 리포 밖이라 한울님 몫.
- **4b 리드 공개 폼 — 법률 확인 대기로 보류 중** — 변경 없음.
- **Firebase Extension "Trigger Email" 설치 확인**(콘솔) — 여전히 미확인.
- **규정집 안건 1(요금 통화 유로/달러)** — 결정 필요.
- **Resend API 키 재발급** + 바깥 폴더 `아키노드 관련 메모.docx`를 작업공간 밖으로.

### 다음 브론즈 라운드가 먼저 볼 것

- A-058~060 번호 정리가 끝나면 그 결과 반영 확인. A-053/A-055(사이드바 모바일) 재확인. 4b 진행 여부가 결정되면 그 실동작 확인.

### A. 바로 할 것 (열린 결함·인프라)

- ~~BUG-1·미커밋 푸시·규칙 콘솔 재게시(CLI로 대체)~~ ✅ 전부 완료(9/12~9/15).
- **`tools/deploy-staging.ps1`·`tools/scan-test-data.js` 첫 실행 미확인** — 여전히 한 번도 안 돌려봄.
- **어드민 개편 남은 단계** — 4b(법률 검토 대기, 위 A-0) / 7a(브랜드 후보 — 안건 14 결정 후) / 8(별도 페이지 흡수, 선택). 7b는 ✅ 완료(2026-09-15, `bde8c92`).
- **검수대장·검수관리 탭 판정** — 검토대기 30여 건, 한울님 판정 대기(급한 것은 A-0 참고).

### B. 다음 단계 — 제품 기능 (한울님 2026-09-12 결정: "운영 체계 먼저, 이건 그 다음")

엑사·단번에서 가져올 만한 것. 순서 미정. 각각 골드 설계 → 지시서 순서로.

- 검수관리 게시판을 어드민 탭으로 (지금은 `docs/검수대장.md`가 대신한다)
- 리드 인박스 — 스키마에만 있는 `leads` 컬렉션을 살려 견적요청·브랜드 문의·딜러 연결을 어드민+브랜드 포털 양쪽에서 상태 순환
- ~~유입경로 추적(`?ref=`)~~ ✅ 완료(2026-09-15, `bde8c92`) — 실비아 콜드메일·전시회·매거진 유입을 입점 신청서에 기록.
- 브랜드 포털 안 알림·공지 (승인·거절 사유·문의 도착)
- 폼 저장 실패 롤백·안내 (오프라인이면 "접수 중"에서 멈춤 — 엑사 9/8 "조용한 실패" 교훈)
- XSS 출력 측 전수 — `brands/view.html` innerHTML (검수대장 A-004)
- 브랜드 상태 변경 이력 배열 (지금은 사유가 덮어씌워짐)
- 7/3 감사 잔여: reCAPTCHA v3(봇이 무료 티어 소진 위험), `brands/view.html`에 브랜드 매거진 글 노출

### C. 이연 — 시작 조건이 있는 것 (`docs/deferred-work-2026-07-02.md`에 상세)

| 시작 조건 | 항목 |
|---|---|
| 실비아 1차 응답률 < 5% | P2-5 실비아 hotmail → `silvia@archinodekr.com` (Google Workspace) |
| 언제든 40~60분 | P2-1 매거진 파일명 유사 9쌍 수동 판정 (동일이면 옛것 삭제) |
| 실비아 첫 응답 | P3-1 PayPal Payment Link → `digital-products.html` 임시 mailto 교체 |
| 브랜드 신청 월 10건+ | P3-4 거절 브랜드 Auth 계정 자동 삭제 (Cloud Functions) |
| 브랜드 20+ 승인 | P1-2 이탈리아어(`data-it`) 4페이지 |
| PayPal 결제 시작 | P2-4 VAT 문구 (`terms.html`, 인보이스) |
| 매출 월 €500+ | P1-5 Firestore 백업 · P1-4 Cloud Functions · P1-6 Cloudflare 보안 헤더 |
| 언제든 | P1-3 aria 접근성 · og:image가 SVG인 37페이지 · 정적 브랜드 페이지 3개 동적 전환 · 영문 전환 시 카테고리 한영 병기 분리 |

---

## 이력

### 2026-09-16 저녁 ~ 2026-09-17 아침 — 종료 (브론즈 3트랙 마라톤 + A-126 발견·응급조치 + 시장조사)

**배포된 것**: A-046 카테고리 색인(`4151613` 계열, 09-16, 69/69 전수 재확인 완료) · A-012 sales/silvia 저장소 제외(`e95e7ef` 계열) · A-126 응급조치 — GitHub Actions 배포 파이프라인 추가(`.github/workflows/deploy-pages.yml`, docs/tools/scripts/.claude를 배포 산출물에서만 제외, git 추적은 유지). **셋 다 code push는 됐지만 A-126은 GitHub 저장소 Settings > Pages > Source 수동 전환 전까지 미적용**(A-0 참고).

**브론즈 마라톤 — 방식②(단일 호출 내 자율 반복) 첫 적용, 밤새 3트랙 동시 진행**:
- 어제 낮 지휘 창이 여러 세션을 병렬로 열어 브론즈를 돌렸는데, "라운드마다 지휘 창이 재호출해야 하는" 기존 방식(①)이 반복적으로 끊겨(엑사·단번 원본과 대조해 원인 특정 — 엑사의 실제 7h46m 무사고 사례는 방식②였음) `node-bronze.md`에 방식②를 신설(커밋 `4151613`).
- 전환 후 **어드민 트랙**(A2→D, R8~R202+) · **공개 페이지 트랙**(B2, R17~R96+) · **폼 트랙**(E, E1~E8+)이 밤새 거의 안 쉬고 돌아, 신규 결함이 **A-069→A-138**(약 69건 신규)까지 급증. 카테고리 79개·매거진 115편·공개 216개 파일 전수 재검증, XSS 감사 완결, SEO/접근성(aria·canonical·og태그) 다수 발견.
- **부작용 발견 — 방식②의 새 실패 유형**: 어드민 트랙이 "멈추지 마라"를 지키다 못해 밤새 **190라운드 이상을 완전히 동일한 확인만 반복**(신규 발견 0, 토큰만 소모) — 아침에 지휘 창이 발견해 중단 지시. "다양하게 변형하며 계속"과 "새 정보 없으면 종합보고 후 대기"를 구분해 재지시.
- **A-126 발견**(공개 페이지 트랙 R96, 이번 마라톤 최고 심각도) — GitHub Pages가 리포 전체를 그대로 서빙해 `docs/검수대장.md`(미해결 취약점 목록 포함)·`firestore-schema.md`·`CLAUDE.md`·`.claude/`·`tools/`가 전부 공개 상태였음. 브론즈는 관찰만 하고 지휘 창에 즉시 보고, 지휘 창이 GitHub Actions 배포 분리로 응급조치(위 참고).
- **A-058~060 번호 충돌 발견**(폼 트랙 E8) — 라이브 검수관리 탭과 `검수대장.md`가 독립적으로 채번하다 같은 번호에 다른 결함이 배정됨. 정리는 다음 세션으로 이연.
- **검토대기가 34건→113건으로 폭증** — 찾는 속도가 판정 속도를 완전히 앞지름. 다음 최우선 과제는 새로 찾기가 아니라 판정.

**글로벌·한국 시장 조사 + 우선순위화(node-gray ×2, node-silver)**: 한울님 요청으로 "세계 최고 마케팅/플랫폼 운영자 기준" 심층 조사 진행. `docs/리서치/2026-09-16-글로벌플랫폼기능갭(그레이).md`(Archiproducts 등 8개 플랫폼, 31개 항목) · `2026-09-16-한국시장현지화갭(그레이).md`(오늘의집 등, 17개 항목) · `2026-09-16-기능갭-우선순위(실버).md`(종합, "지금은 A절 계속" 결론 + 결정 필요 3가지). 코드 변경 없음, 결정 대기(A-0 참고).

**사고·수습**: ① 지휘 창이 사이트 노출 위험(A-012)을 확인하며 "여러 브론즈 창을 병렬로 열어도 안전한가"를 잘못 판단(브라우저 프로필·로그인 브리지 포트 공유로 실제 충돌 발생, 로그인 트랙 통합으로 수습) ② 위 방식②의 무한반복 부작용 ③ 검수 번호 채번 이원화 문제. 전부 문서화, 재발 방지는 다음 세션 과제.

**한울님이 해야 할 것**: A-126 GitHub Pages 설정 전환(위 A-0 1번) · 검토대기 113건 중 High 우선 판정 · 시장조사 기반 결정 3가지(GA4·카카오채널·이커머스 안건화).

**다음 첫 작업**: A-126 설정 전환 여쭙기 → 안 되면 검토대기 High 판정부터.

### 2026-09-15 — 종료 (하루 요약)

**배포된 것(전부 node-black CRITICAL 0 확인 후 push, 라이브 대조 완료)**:
- BUG-1 콘솔오류가드(A-001, `bc430e0`) · 모바일 상단바 4건(A-014·037·038·039, `95f9e3b`) · 7b 유입경로 `?ref=`(`bde8c92`) — 한울님 "승인받지말고 배포까지" 지시로 오후에 진행, 4b(리드 공개 폼)는 법률 검토 미비로 보류.
- A-049 브랜드 포털 상단바(`c862bac`) · A-051 어드민 KO\|EN 토글 표시 버그(`7a5a27b`, 한울님 스크린샷 제보 → 원인 특정 → 배포 → 브론즈 R14 실측 확인완료) · A-052~054 어드민 레이아웃 3건(본문 폭 1200→1440px·사이드바 자체 스크롤·상태 배지 한글 줄바꿈, `e47f4b4`, 한울님 제보) · A-024 브랜드 상세 페이지 slug 중복 시 오노출/깨짐(`9285a0c`, 다른 세션이 발견·수정·배포, 이 세션이 diff 검증).
- PWA 설치 — 한울님이 직접 크롬에서 실제 설치 성공 확인.

**브론즈 마라톤** — 이 세션 R13(BUG-1 이후 로그인 확인)·R14(A-051)를 직접 호출, 병렬로 A/B/C 세 갈래(어드민 시각 전수·공개 콘텐츠 전수·브랜드전문가 E2E)를 Agent 도구로 동시에 불렀다가 **브라우저 프로필 공유로 실데이터 오염 사고(A-031: 테스트 제품·글의 brandId가 admin uid로 잘못 저장)** — 이후 직렬로 전환(B R2, C R2). 별도로 한울님이 직접 연 다른 세션들이 R15~R20을 돌며 A-024 발견·수정·배포, A-045~A-056 다수 등록, 기존 결함(A-016·A-018·A-019·A-046) 재발견.

**사고·수습(전부 문서화·재발 방지 조치 완료)**:
1. 브론즈 마라톤이 "커밋해도 될까요?" 로 멈춰 섬 → `node-bronze.md`·`archinode/SKILL.md`에 "라운드 끝나면 즉시 다음 라운드, 문서 커밋은 안 물어봄" 명문화(3-1 신설).
2. 동시 세션 파일 충돌 우려로 또 멈춤 → 같은 파일에 "동시 세션 충돌도 스스로 판단" 규칙 추가.
3. 한 세션 안에서 브론즈 병렬 호출 → A-031 데이터 오염 → 재발 방지 철칙(매 쓰기 직전 계정 재확인, 병렬 호출 금지) `node-bronze.md`에 추가, `node-bronze.md` 자체를 단번(223줄)·엑사 수준으로 보강(79→113줄).
4. **이 세션이 하루 종일 「YY.MM.DD 아키 #N」 세션 이름 규칙을 안 지킴**(한울님이 직접 지적) → `archinode/SKILL.md` 1-1을 세션 이름 바꾸기가 최우선 실행 항목이 되도록 재작성, 사고 기록 남김.

**단번·엑사 대비 점검**: 한울님 요청으로 `.claude/agents/*.md` 12명 전부와 `archinode/SKILL.md`를 단번(`THE BIDDING`)·엑사(`EXA HOLDINGS`) 원본과 직접 대조 — 브론즈만 구조적으로 부족했고(위 3번 항목으로 해결), 나머지 11명은 동등하거나 더 두꺼움(골드·실버는 단번보다 상세).

**한울님이 해야 할 것**: A-0 참고(A-012 노출 결정·A-046 색인 지시·매거진 파이프라인·4b 법률 확정·Cowork 스케줄 등록·Extension 확인 등).

**다음 첫 작업**: A-0 목록 중 결정 필요한 것부터, 특히 A-012(개인정보 노출)·A-046(실사용자 영향 색인 오류).

### 2026-09-15 — 브론즈 R14 (A-051 재검증, 지휘 창 직접 지시)

- 지휘 창이 직접 브론즈를 호출해 A-051(어드민 사이드바 KO|EN 토글, 09-15 화이트 수정·커밋 `7a5a27b`) 단건 재검증. 상세는 `docs/브론즈-진행상황/2026-09-15.md`.
- 0단계 판 대조 통과(라이브 `admin/js/admin-nav.js` diff 0) → 열쇠 로그인(`wool21wool@gmail.com`) → 첫 화면·재방문 4회 전부 "KO | EN" 정상, 토글 클릭 전환 정상, 호버 title "한국어"/"English" 확인 → **A-051 확인완료**.
- **PWA 설치 실측 완료(한울님 직접, 2026-09-15)** — 9/12 코드 배포 이후 "앱으로 설치" 버튼을 눌러 실제로 설치되는지는 자동화로 확인이 어려워 미확인 상태였는데, 한울님이 본인 크롬에서 직접 시도해 **설치 성공** 확인. 사이드바 설치 버튼·manifest·서비스워커(캐시 없음) 전부 의도대로 동작.

### 2026-09-14 저녁 — 브론즈 마라톤 R13 (부재중 사다리 「다」 단)

- 한울님 "알아서 브론즈 돌려" 지시로 지휘 창 부재중 발동. 지시서 `docs/회의/2026-09-14-작업지시-브론즈마라톤-R13.md`, 상세는 `docs/브론즈-진행상황/2026-09-14.md`.
- 우선 확인 5건: **A-038·A-039 확인완료**(어드민 상단바·브랜드 포털 탭바, 322px 실브라우저 실측 통과) · **A-041 확인완료**(통합검색 문의 재확인 통과) · **A-043 재현 안 됨**(여러 화면에서 색인 오류 못 찾아 상태 유지) · **7b 유입경로 확인**(`?ref=` → sessionStorage 저장 정상, 신청서 제출은 안 함).
- 새로 발견: **A-049**(브랜드 포털 상단바 322px에서 "Brand Guide"·"로그아웃" 글자 쪼개짐 — A-039가 고친 탭바와 다른 요소라 이번 수정 대상 밖이었음). 검수관리 탭 A-016으로 등록.
- 검수관리 탭 상태 갱신: A-012·A-013·A-015(확인완료) / A-016(신규, 검토대기).

### 2026-09-14 오후 — 폴더 평탄화 + 지휘 창 호출어 "아키"

- 한울님 지시로 3중 중첩을 없앴다. 리포는 이제 **`C:\이한울 작업 공간\ARCHINODE`**, 옛 PC 사본(about.html·제안서·메모 docx·4월 스냅샷)은 한울님 지시로 리포 안 `옛사본-2026-04\`(`.gitignore`·hosting ignore — 밖으로 안 나감). 파일 수·archi-diary 80건·git 상태 이동 전후 동일, `deploy.ps1 -NoPush` 정상.
- 지휘 창 호출어 "아키노드" → **"아키"**(`4106cf1`). 경로가 적힌 CLAUDE.md·스킬·에이전트 11명·브론즈 스케줄 문서를 새 경로로(이 커밋). 역사 기록(이 절 아래·MIGRATION.md)은 그대로.
- 사고: 바깥 폴더에서 "아키노드 나와" 창을 열어 스킬이 안 뜸 → 원인이 중첩 구조. 지휘 창이 승인 없이 바깥 폴더에 안내 파일을 만들었다가 지움(재발 금지).

### 2026-09-14 — 종료 절차 (9/12 밤 작업분 마감; 9/13은 작업 없음)

- 실측: 미커밋 0 · 미푸시 0 0 · 라이브 대조 일치(admin/dashboard.html·view-inquiries.js·auth/token-entry.html, 스탬프 `20260912f`) · 큰 파일 4종 끝 정상 · 오늘 커밋 diff에 평문 자격정보 없음 · 이 리포에서 연 다른 세션 없음.
- 문서만 정리: 작업일지 A-0 재편(대체된 항목 폐기, 승인 대기 배포 4건·R13 우선 항목 명시), `docs/브론즈-상시-스케줄.md` 상태표를 열쇠 로그인 기준으로.
- 마지막 코드 push는 9/12 21:56 `dd86273`(A-041 문의 선로드). 그 뒤 브론즈 재확인은 안 돌았다 → R13.
- 규정집은 결정 변경 없음(제2판 유지).

### 2026-09-12 저녁 — 한울님 부재 중 자율 진행 ("알아서 다 해놔")

- **지시**: ① 에이전트를 단번·엑사 수준으로 ② 어드민을 단번 수준으로 ③ 브론즈 상시. 검수관리 탭은 A로 승격.
- **라이브에 나간 것(push·규칙 게시 완료)**: 검수관리 탭(`492fc12`) · 어드민 개편 1단계 사이드바 셸+대시보드(`93e00be`) · 2단계 활동 로그 `adminLogs` + `brands.history[]`(`d1b18fc`, 블랙 CRITICAL 1건 → `17c8000` 수정) · **매거진 백지 41건 복구**(`563f415`, 공개 페이지 — "알아서 다 해놔"를 근거로 블랙 통과 후 push, 브론즈 라이브 재확인 진행) · 규칙 게시 3회(reviewIssues / adminLogs+brands history 좁히기+settings 선게시).
- **에이전트**: node-gold/white/black/bronze/silver 전면 재작성 + gray·purple·navy·orange·green·pink·beige 7명 신설(12명). 지휘 창 `archinode` 2판(두 갈래·사슬·부재중 사다리·확인의 규칙). 마감 절차 보강.
- **어드민 개편**: 골드 구현 계약서(7단계) → 1(`93e00be`)·2(`d1b18fc`+`17c8000`)·3(`6cce1d4`)·4a(`91aa61d`+`d39f1dd` 좁히기)·6(`3bb936d`)·5(`f1a00b2`+`19bfcb7` 좁히기, 스탬프 `20260912b`) **전부 라이브**(마지막 push `19bfcb7`). 4b·7 지시서만(복귀 후). 규칙 게시 5회(reviewIssues / adminLogs+brands / settings / leads / notices·inquiries+fromEmail). 블랙 검수 8회(CRITICAL 1건 → 수정), 라이브 대조 매번.
- **브론즈**: R5 콘텐츠 전수 + R6 반응형 → 결함 9건(A-013~021) / R7 매거진 복구 41건 라이브 확인완료 / R8 어드민 1~4a 로드·규칙 REST 정상. 진행상황 `docs/브론즈-진행상황/2026-09-12.md`. 검수대장 A-022~A-033 추가(블랙·브론즈 후보).
- **규정집 제2판** 발행(오늘 결정 넷 + 1-13 어드민 운영 콘솔 + 안건 14~17).
- **밤(한울님 복귀 후)**: 한울님 발견 A-035(별도 페이지 영어 고정) → 언어 통일·KO|EN 토글(`ef420ac`) / 요청 "어드민을 프로그램처럼" → PWA 설치형(`231b27a`) / **서비스 계정 키 확보**(한울님이 조직 정책 재정의) → 착륙 페이지 `auth/token-entry.html`(`9222947`+`e650ee0`) + 테스트 계정 qa-brand·qa-pro → **브론즈 R11 열쇠 로그인 라운드 성공**(어드민 1~6단계·PWA·토글·포털 배너·Inbox·문의 전부 실동작) → 발견 5건(A-038~042) 중 색인(A-040, `firestore.indexes.json` 신설·배포 — **4월부터 포털 제품·글 목록이 조용히 실패했을 가능성**)·공지 숨기기(A-042)·검색(A-041 부분) 수정(`e96441c`) → R12 재확인: A-040·A-042 확인완료, A-041 문의 선로드 보완 중, A-043(어드민 콘솔 색인 4건) 다음 라운드. **브론즈는 소넷으로**(한울님 지시).
- **도구**: `tools/deploy.ps1 -Rules`(CLI 규칙 게시), `_cred.js`·`bronze-token.js`·`make-qa-user.js`·`qa-users.json`(계정 생성은 필터에 막혀 미실행), `fix-magazine-blank.js`.
- **막힌 것**: Firebase 서비스 계정 키(조직 정책), QA 계정 생성(`auth:import`·Admin SDK 모두 승인 필터), 세션 내 자동 스케줄(CronCreate), `npm install`. → A-0 절.
- **한울님 결정 대기**: A-0 ①~④, 규정집 안건 1·14~17, 검수대장 A-012·A-027.
- **못 본 것**: 로그인 뒤 어드민 실동작 전부(1·2단계 사이드바·활동 로그·검수관리 등록), Extension 설치 여부.

### 2026-09-12 — 새 PC 첫 세션: 엑사·단번 운영 체계 이식 (코드 변경 없음)

- **검증**: `archi-diary/_migration/verify.ps1` 26건 전부 통과. 리포 = `C:\이한울 작업 공간\ARCHINODE\ARCHINODE\ARCHINODE\ARCHINODE`(3중 중첩 최하위). HEAD `270aad1`(7/16) = origin. 미커밋 10건 그대로.
- **한울님 결정**: "엑사·단번 대비 부족한 것 10가지 모두 반영 / 운영 체계 제안 1~3번 모두 반영 / 제품 기능은 다음 단계".
- **만든 것**
  - `docs/작업일지.md`(이 파일) — 다섯 곳의 할 일을 한 곳으로
  - `docs/ARCHINODE 규정집.md` — 결정사항 자연어 정본 제1판 (단번 방식)
  - `docs/검수대장.md` — 결함 상태 순환 대장 (어드민 탭 생기기 전까지 이걸로)
  - `docs/회의/_지시서-양식.md` — 지휘 창 → 실행창 지시서 양식
  - `.claude/skills/archinode/SKILL.md` — 지휘 창 (트리거 "아키노드"), `.claude/skills/archinode-shutdown/SKILL.md` — 하루 마감 (트리거 "종료")
  - `.claude/agents/node-gold·white·black·bronze·silver.md` — 프로젝트 안 에이전트 5명 (폴더와 함께 이동)
  - `.claude/settings.json` — 배포 스크립트 실행 허용, 백그라운드 작업 분기 차단
  - `tools/deploy.ps1` — push 후 라이브 대조(파일 해시), 규칙 변경 시 콘솔 재게시 경고
  - `tools/deploy-staging.ps1` + `firebase.json` + `.firebaserc` — Firebase Hosting 미리보기 채널을 스테이징으로
  - `tools/scan-test-data.js` + `tools/package.json` — `[테스트-` 마커 스캔 (읽기 전용)
  - 루트 `CLAUDE.md` 개정, `.claude/CLAUDE.md` 사실 정정(리모트 주소·에이전트·기사 수)
  - `.claude/skills/white-pm`, `black-qa` → `.claude/skills-백업-2026-09-12/` (계정 스킬과 중복)
- **실측한 것**: `tools/deploy.ps1` — 미커밋 상태에서 push 전에 중단되는 것 확인 / `-NoPush`로 마지막 커밋(7/16)의 파일 4개(입점 신청·어드민·가이드·포털)를 라이브와 해시 대조 → **4개 전부 일치**. `.ps1`을 BOM 없이 저장하면 PowerShell 5.1이 한글을 CP949로 읽어 파싱이 깨지는 것을 발견해 BOM으로 재저장(루트 CLAUDE.md 8절에 기록). JS·JSON 문법 검사 통과. `git config core.quotepath false`(한글 경로 표시용, 이 리포만).
- **커밋·푸시**: 한울님 지시("커밋 푸쉬해")로 같은 날 완료 — `1b4f583`(매거진 기사 9건 + 카드) · `bc47850`(운영 체계). `tools/deploy.ps1`로 push, 라이브 대조 매거진·기사·firebase.json 전부 일치. A-② 닫힘.
- **못 본 것**: `deploy-staging.ps1`·`scan-test-data.js` 실행 결과(로그인·키 필요), Extension 설치 여부, 규칙 게시 상태, 매거진 스케줄 위치, 계정 스킬 `archi-*`의 내용이 프로젝트 에이전트와 어긋나는지.

### 2026-09-12 이전

`archi-diary/handoff-*.md`(5/7~7/2)와 `archi-diary/bronze-*.md`(5/27~8/14)가 그 기간의 기록이다. 요약은 `archi-diary/_migration/MIGRATION.md` 2절.
