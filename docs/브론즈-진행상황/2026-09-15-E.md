# 브론즈-E 진행상황 — 2026-09-15

병렬 라운드 표기: 「브론즈-E」. 다른 병렬 라운드(-A, -B, -B2, -C 등)와 별개 파일.
범위: 공개 폼 5종(자문·트렌드 투고·문의·뉴스레터·디지털 상품 알림) 실제 제출 E2E + Firestore 반영 확인, 322~768px 반응형 확인. 로그인 불필요(비로그인 공개 폼).

## 0단계

- 라운드 시작 시 로컬 HEAD `d7ce260`, origin과 0/0 차이(미푸시 없음) 확인.
- 브라우저 세션이 다른 병렬 라운드와 공유 중임을 실측으로 확인: 시작 시 `firebase.auth().currentUser.email`이 `wool21wool@gmail.com`(내가 로그인한 적 없음)으로 나왔다가, 몇 분 뒤 재확인하니 `null`로 바뀜(다른 라운드가 로그인/로그아웃을 반복 중으로 추정). 이번 라운드가 다루는 5개 폼은 전부 비로그인 공개 폼이라 데이터 소유권(brandId 등) 문제는 없지만, 지휘 창에 참고로 보고.
- `docs/검수대장.md`에 새 항목을 쓰기 직전 `git status`로 다른 프로세스가 만든 미커밋 변경(M 상태)을 발견 — 채번 직전 `grep`으로 최신 A-### 번호를 다시 확인해 충돌 없이 A-064부터 이어붙임(규칙대로 재확인 후 이어쓰기).
- 관리자 로그인 브리지(`tools/bronze-token.js`)는 이번 라운드에서 환경 classifier(Credential Materialization)에 막혀 실행 자체가 거부됨 → 어드민 화면·검수관리 탭 접근 전부 "확인 못 함". 우회 시도 안 하고 결함은 `docs/검수대장.md`에 직접 기록(A-064~A-069, 절차대로).

## 폼별 결과

### 1. 자문 요청 (request-consultation.html → `consultations` 컬렉션)

- 유효성 검사: 동의 체크박스 안 누르면 제출 차단(에러 문구 정상), 설명 100자 미만이면 "Please provide at least 100 characters..." 에러 정상 표시. 단 라벨은 "100 words minimum"인데 실제 검사는 **글자 수(character) 100자** — 단어 수가 아님(문구·실제 동작 불일치, 등록은 안 함/우선순위 낮아 보고만).
- 정상 제출: `[테스트-브론즈] E라운드 컨설팅` 등 마커 포함 값으로 제출 → "Request received" 성공 화면 정상 표시.
- **Firestore 반영 확인(직접 조회 성공)**: `consultations/Mg57GCcnSmNnBC6jFqot` 문서가 정상 필드로 생성됨(companyName·contactName·email·description 등 전부 정상, status: pending).
- **🆕 결함(A-064, High)**: 콘솔에 매번 `ReferenceError: data is not defined`가 재현되며, 이 요청을 관리자에게 알리는 `mail` 컬렉션 문서가 **한 번도 생성되지 않음**(같은 제목 패턴으로 조회해 0건 확인). consultations 저장 자체는 정상이라 방문자는 성공 화면을 보지만, 운영자는 이메일로 알림을 받지 못하는 상태 — 코드가 존재한 이후 계속 이랬을 가능성이 높음(대조군인 뉴스레터 알림 메일은 같은 패턴이지만 변수명이 맞아 정상 동작).

### 2. 트렌드 투고 (submit-trend.html → `trend-submissions` 컬렉션) — **테스트 불가, 결함 아님**

- 코드/라이브 모두 확인: `submit-trend.html`은 더 이상 제출 폼이 없다. "Articles are now published through the Brand Portal. Only approved brands can publish"라는 안내와 함께 `list-your-brand.html`·`brand-portal/login.html`로 보내는 버튼 두 개만 있는 정보성 페이지로 바뀌어 있음.
- 리포 전체를 grep으로 확인한 결과 `trend-submissions` 컬렉션에 `.add()`로 쓰는 공개 폼 코드가 **어디에도 없음**(어드민의 `admin/trend-submissions.html`은 읽기·수정만 함). 즉 실제 투고 경로는 브랜드 포털의 매거진 작성 기능으로 이전된 것으로 보이며, 이는 설계 변경이지 버그가 아니라고 판단해 결함 등록은 하지 않음.
- 지시서에 있던 "정상 제출 → Firestore 반영" 테스트는 대상 자체가 없어 수행 못 함(확인 불가가 아니라 "해당 없음").

### 3. 문의 (contact.html → Firestore 컬렉션 아님, **Formspree 외부 서비스**)

- 목적지 특정: 이 폼은 Firestore를 쓰지 않고 `<form action="https://formspree.io/f/xgodynvd" method="POST">`로 Formspree에 직접 제출된다. 성공 시 office@archinode.org로 실제 이메일이 감(Firestore 문서는 생기지 않음 — "관련 컬렉션"이 존재하지 않는 구조).
- 유효성 검사: 이름 필드를 비우고 제출 시 브라우저 네이티브 검증이 그 필드로 포커스를 이동시키며 제출 차단(정상).
- 정상 제출: 이름 `[테스트-브론즈] 브론즈 E팀`, 이메일 `qa-bronze+contact@archinodekr.com`, 메시지에 마커 포함해 제출 → Formspree가 "Thanks! The form was submitted successfully." 처리(실제 이메일 발송 여부는 어드민 인박스가 아니라 Formspree/office@archinode.org 메일함이라 직접 확인 불가 — "발송 확인 못 함").
- **🆕 결함(A-068, Low)**: 제출 성공 후 브라우저가 archinodekr.com을 완전히 벗어나 무연고 `formspree.io` 기본 감사 페이지로 이동(`_next` 리디렉션 파라미터 미설정). ARCHINODE로 돌아오는 링크 없음.
- **🆕 결함(A-065, High, 반응형)**: 아래 반응형 섹션 참고 — 322·375px에서 "Get in Touch / Send a Message" 2칼럼이 안 접혀 가로 스크롤 + 폼 잘림.

### 4. 뉴스레터 구독 (index.html "Stay Updated" 섹션 → `newsletter-subscribers` 컬렉션)

- 유효성 검사: 커스텀 JS(`email.indexOf('@') < 1`)와 별개로 `type="email" required` 네이티브 검증이 먼저 작동 — "bad"처럼 `@` 없는 문자열은 `input.validity.typeMismatch === true`로 제출 자체가 안 됨(정상). 빈 값도 required로 차단.
- 정상 제출: `qa-bronze+newsletter@archinodekr.com`로 제출 → 콘솔에 `alert("Thank you for subscribing!")` 호출 확인(브라우저 자동화 환경이 네이티브 alert을 자동 억제하지만, 호출 자체가 성공 분기에서만 일어나므로 Firestore add가 성공했다는 신호로 판단). 관리자 알림 메일 코드는 `email` 변수를 올바르게 참조해 request-consultation.html과 달리 정상 동작할 것으로 코드상 확인(콘솔에 해당 실패 경고 없음).
- **직접 Firestore 조회는 못 함**: `newsletter-subscribers` 컬렉션은 규칙상 `read`가 관리자 전용이라, 비로그인 상태에서는 성공 alert과 규칙 통과(콘솔 permission-denied 없음)로 간접 확인만 가능. admin 화면 확인은 로그인 불가로 못 함.
- **🆕 결함(A-067, Medium, 반응형)**: 322px(매우 좁은 폭)에서 입력칸+Subscribe 버튼이 한 줄 유지로 버튼 잘림 + 페이지 전체 가로 스크롤. 375px부터는 정상.

### 5. 디지털 상품 알림 (digital-products.html "Notify Me" → `digital-products-notify` 컬렉션)

- 유효성 검사: 빈 값 제출 시 네이티브 "이 입력란을 작성하세요." 툴팁으로 차단. `not-an-email` 입력 시에도 네이티브 검증으로 차단(제출 자체가 안 일어남, 버튼 텍스트도 "Notify Me" 그대로 유지).
- 정상 제출: `qa-bronze+digitalnotify@archinodekr.com` → "Thanks — we'll send a single email when the first product launches." 성공 문구 정상 표시.
- **직접 Firestore 조회는 못 함**(newsletter와 동일 이유 — read는 관리자 전용, 로그인 불가). 성공 UI + 콘솔 오류 없음으로 간접 확인.
- 반응형 322·375·768px 전부 스크롤 폭 이상 없음, 폼 시각적으로 정상.

## 반응형 확인 (322 / 375 / 768px, `document.documentElement.scrollWidth` vs `clientWidth` 실측 + 스크린샷)

| 화면 | 322px | 375px | 768px |
|---|---|---|---|
| request-consultation.html | 정상(스크롤 없음, 폼 필드 1열 정상) | 확인 안 함(322 정상이라 생략, 코드에 `@media 768px` 존재 확인) | 정상(기존 A-014 관련 확인 이력 있음) |
| submit-trend.html | **A-066 재현**(386px, 64px 초과) | **A-066 재현**(386px, 11px 초과) | 정상(753px) |
| contact.html | **A-065 재현**(393px, 71px 초과) | **A-065 재현**(393px, 18px 초과) | 정상(753px) |
| index.html(뉴스레터) | **A-067 재현**(363px, 41px 초과) | 정상(375px) | 정상(확인) |
| digital-products.html | 정상(322px) | 정상(375px) | 정상(753px) |

부수 발견(요청 범위 밖, 1024px 기본 뷰포트에서 우연히 관찰): 공통 헤더 "List Your Brand" 버튼이 좁은 데스크톱/태블릿 폭에서 세로 4줄로 줄바꿈되며 검은 박스가 다른 헤더 버튼과 겹침 → A-069로 등록(Low, 범위 밖 명시).

## 🧹 만든 테스트 데이터 (지우지 않음)

- `consultations/Mg57GCcnSmNnBC6jFqot` — companyName `[테스트-브론즈] E라운드 컨설팅`, email `qa-bronze+consult@archinodekr.com`, status pending. **A-064 재현 증거물이므로 그대로 둘 것.**
- `digital-products-notify` 컬렉션 — `qa-bronze+digitalnotify@archinodekr.com` 1건 추가(직접 조회는 못 했으나 성공 UI로 간접 확인, 관리자 read 권한으로만 검증 가능).
- `newsletter-subscribers` 컬렉션 — `qa-bronze+newsletter@archinodekr.com` (source: `home`) 추가 시도 2회(정상 이메일 1회 성공 + `bad` 무효 이메일 1회는 네이티브 검증에 막혀 미제출, Firestore엔 안 쌓임).
- Formspree(`xgodynvd`) — 이름 `[테스트-브론즈] 브론즈 E팀`, 이메일 `qa-bronze+contact@archinodekr.com`, 메시지에 "브론즈 검수용 테스트 메시지" 마커 포함 1건 제출(office@archinode.org로 실제 이메일 발송, Firestore 문서 아님이라 삭제 대상도 아님).

## 📋 검수관리 등록

- **어드민 검수관리 탭(reviewIssues) 등록 자체는 못 함** — 관리자 로그인 브리지(`tools/bronze-token.js`)가 이번 라운드에서 환경 classifier에 의해 실행 거부됨(우회 시도 안 함). 절차대로 `docs/검수대장.md`에 직접 기록.
- 신규: **A-064**(High, request-consultation.html 관리자 알림 메일 코드 오류로 항상 실패) · **A-065**(High, contact.html 2칼럼 미반응형·322~600px대 가로 스크롤+폼 잘림) · **A-066**(Medium, submit-trend.html 4단계 안내 미반응형) · **A-067**(Medium, index.html 뉴스레터 폼 322px 버튼 잘림) · **A-068**(Low, contact.html 제출 후 무연고 외부 페이지 이탈) · **A-069**(Low, 요청 범위 밖 1024px 헤더 "List Your Brand" 줄바꿈 겹침, 발견만 기록).
- 재발견/이력 추가: 없음(전부 신규).

## 못 본 것 / 확인 못 함

- 관리자 로그인(브리지 열쇠 발급 환경 classifier 차단) → 어드민 화면에서 `consultations`·`digital-products-notify`·`newsletter-subscribers` 실제 목록 노출, 검수관리 탭 정식 등록, `mail` 컬렉션 관리자 알림 발송 최종 확인(Extension 설치 여부 포함) 전부 확인 못 함.
- 트렌드 투고 Firestore 반영 테스트 — 해당 기능 자체가 공개 사이트에 없어 대상 없음(버그 아님, 위 설명 참고).
- contact.html Formspree 제출이 실제 office@archinode.org 메일함에 도착했는지는 메일함 접근 권한이 없어 확인 못 함(Formspree 자체의 성공 응답만 확인).
- request-consultation.html의 "100 words minimum" 문구와 실제 100자(character) 검사 불일치는 낮은 우선순위로 판단해 정식 등록은 생략, 이 문서에만 기록.
