# 브론즈 마라톤 E6 (2026-09-15 마라톤, 2026-09-16 실행) — R5 콘텐츠 전수(읽기 위주)

이어지는 마라톤: E→E2→E3→E4→E5→E6. 같은 날 라벨(E계열)로 유지, 실제 실행은 09-16.

## 0단계
- 판 대조: `about.html` 로컬=라이브 바이트 동일(diff 0) 확인. 커밋 9bbab47a4(about.html 마지막 변경) 기준 정상 반영.
- 로그인: 이번 라운드는 읽기 위주 — 로그인 생략(직전 E5 병렬 세션 충돌 경고에 따름).

## 범위
공개 페이지 중 지금까지 라운드가 주목적으로 보지 않은: about.html, terms.html, privacy.html, 404.html, list-your-brand.html, for-brands.html, brand-guide.html
확인 항목: 내부 링크/이미지 깨짐, 헤더/푸터 표준, 언어 토글 양방향, sitemap 대조.

## 라운드 로그

### R E6 (읽기 전용, 로그인 없음)
대상: about.html · terms.html · privacy.html · 404.html · list-your-brand.html · for-brands.html · brand-guide.html · contact.html(추가) · submit-trend.html(추가)

**결과 요약**
- 내부 링크 전수(각 페이지 24개 안팎) 전부 200 — 깨진 링크 없음
- 이미지 깨짐 0건(전 페이지 naturalWidth 체크)
- 언어 짝(`data-en`만/`data-ko`만) 불일치 0건(대상 페이지 기준)
- **신규 발견 A**: 위 페이지 중 about·terms·privacy·404·contact·submit-trend 6개 페이지의 푸터 첫 문단(태그라인 "Connecting global architecture & design brands with the Korean market.")이 `data-en`/`data-ko` 속성이 아예 없어 항상 영어 고정. 같은 자리 index.html 1050행은 정상적으로 속성이 붙어 있어 대조군 확인됨. 전체 HTML 스캔(scripts 아님, bash grep)으로 214개 파일 중 9개 파일에서 같은 증상 확인 — for-brands.html은 기존 A-088에 이미 포함, list-your-brand.html·brand-guide.html은 영어 단일/속성 0개 페이지라 제외. **남은 6개(about/terms/privacy/404/contact/submit-trend)는 A-087/A-088과 다른 파일·범위라 신규 항목으로 판단.**
- **신규 발견 B**: brand-guide.html에 `<body data-langs="en">`(list-your-brand.html에는 있음)이 없어서 언어 드롭다운이 기본값(en,ko 둘 다)으로 뜸 — 실측 결과 화면은 "KR" 선택 상태인데 페이지 전체(헤더 네비 포함)가 한 글자도 한국어로 안 바뀌고 100% 영어 그대로 보임(`data-en/ko` 속성 자체가 0개라 번역 불가능한 페이지). A-008("입점 신청 언어 단추 — 결함 아님, 의도된 영어 단일")과 반대로 이쪽은 잠금이 안 걸려 있어 드롭다운만 무의미하게 노출됨. A-084(같은 페이지 카테고리 드롭다운 소문자 중복)와는 다른 부분(언어 스위처 자체).
- 재확인(신규 등록 안 함, 기존 항목과 동일 증상): A-017(공지 띠 영어 고정, about.html 포함 19페이지 목록에 이미 있음) · A-001/BUG-1(firebase-config.js 무가드 — about.html·brand-guide.html 콘솔에서 재현)
- sitemap.xml 대조: about/terms/privacy/contact/for-brands/list-your-brand/submit-trend 전부 sitemap에 있고 실제로도 200. 404.html·brand-guide.html은 sitemap에 없음(검색엔진 비노출 의도로 보임, 결함 아님).
- 헤더/푸터 구조: about/terms/privacy/404/contact/submit-trend 6개는 공유 헤더·푸터 h4 4열 구조 동일(스크린샷 대조). for-brands/list-your-brand/brand-guide는 별도 제작 푸터(기존 A-088 계열).
- 화면 특이사항(버그 아님): list-your-brand.html 언어 드롭다운 클릭 시 스크린샷 1회 타임아웃(도구 렌더링 지연으로 판단, JS 평가는 그 사이에도 정상 응답·재시도 시 정상 캡처 — 페이지 실제 정지 아님, 확인 불가로 기록만).

**등록**: 이번 라운드는 지시대로 로그인 없이 읽기 전용으로 진행 — 검수관리 탭(reviewIssues) 등록은 병렬 세션 계정 충돌 위험 때문에 이번 라운드에서 시도하지 않음. 위 신규 발견 A·B는 다음 로그인 가능 라운드에서 등록 필요(대상 그대로 재사용 가능하게 본 파일에 남김).

