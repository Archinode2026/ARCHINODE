# 브론즈 진행상황 2026-09-12 (마라톤 · 모드 2 운영서버 검수, 열쇠 없음)

| 라운드 | 시작 | 종료 | 통과/실패 | 요약 | 만든 테스트 데이터 |
|---|---|---|---|---|---|
| R5 콘텐츠 전수 | 10:45 | 10:56 | 17페이지 로드 17/17 · 검사 항목 9 중 통과 4 / 실패 5 | 0단계 통과(2f71b39 = 라이브). ★신규 High: 매거진 기사 42/116건이 라이브에서 완전 백지(`<style>` 미닫힘, 오늘 자동 발행 3건 포함). 신규 Medium: 서브카테고리 69페이지 스크립트 오류(`header` 중복 선언), 공지 바 영어 고정 19페이지, 카테고리 KO 모드 nav "가구가구" 중복 18페이지, sitemap 31페이지 누락. 재발견 A-001·A-002·A-012. 내부 링크 깨짐 0, 깨진 이미지 0, placeholder 0 | 없음 |
| R6 반응형·배율 | 10:56 | 11:00 | 6페이지×4폭=24 중 통과 18 / 실패 6(375 전부) | 375px: 헤더 "브랜드 로그인"·"건축을 잇다"가 세로 한 글자씩 쌓여 공지 바와 겹침(공통 헤더 5/5 페이지), 로그인 단추가 뜨는 페이지(brands·request-consultation·magazine)는 가로 스크롤 451px, list-your-brand 푸터 4열이 안 접혀 483px. 768/1280/1920 전부 통과 | 없음 |
| R7 검수판 재검증(A-013 라이브 재확인 + A-016·017·018 재현) | 11:22 | 11:35 | 정적 41/41 통과 · 브라우저 8/8 통과 · 어드민 2/2 통과 · 재현 3/3 | 0단계 통과(라이브=17c8000, 6cce1d4는 미푸시). **A-013 확인완료** — 복구 41편 라이브 `</style>`·`</head>`·`<body` 각 1개, md5 로컬=라이브, 8편 브라우저 렌더(히어로·제목·본문, innerText 2,307~3,240자, 콘솔 오류 0). 어드민 dashboard·consultations 비로그인: js 4개 200, SyntaxError 0. A-016(`header` 중복 선언)·A-017(공지 바 영어 고정)·A-018("가구가구") 3건 모두 라이브 재현됨. 재발견 A-001(`firebase.auth is not a function`·`db is not defined`, 서브카테고리). 등록 후보 1: 어드민 비로그인 리다이렉트 목적지 불일치(dashboard→브랜드 포털 로그인, consultations→전문가 로그인 ?next) | 없음 |
| R8 어드민 개편 1~4a 라이브 로드(비로그인 변형) | ~11:50 | 12:05 | 로드 8/8 · 규칙 4/4 · 매거진 재발 0 | 0단계 통과(라이브 = d39f1dd, `view-leads` 2건 즉시 확인 — 대기 0분. 작업트리의 6단계 `view-users.js`·admin-core 수정분은 미커밋이라 라이브 아님). 어드민 dashboard 비로그인: js 7(core·nav·dashboard·logs·settings·regulations·leads)+admin.css 전부 200, 콘솔 오류 0(새 탭 실측), cdnjs marked 12.0.2·dompurify 3.1.6·gstatic 10.12.0 4종 200(curl — 브라우저 도구가 교차 출처 요청을 기록 안 해 curl로 대체). 별도 어드민 5페이지 core·nav 200·콘솔 0. brand-portal/dashboard 인라인 오류 0 → login.html(article-editor.js는 리다이렉트로 ERR_ABORTED, curl 200 정상). 규칙 REST: settings/config 404·leads/adminLogs/reviewIssues 403 전부 기대대로. 매거진 새 자동 발행 0(origin/main = HEAD, magazine.html md5 로컬=라이브, `</head>` 없는 기사 0 → A-013 재발 없음). 재발견 A-028. 등록 후보 2(Low): 브랜드 포털 로그인 KO 모드 입력칸 안내문 영어 고정 · 어드민 cdnjs 스크립트 integrity 없음 | 없음 |
| R9 어드민 개편 5·6단계 + 캐시 스탬프 라이브 로드(비로그인) | 12:20 | 12:29 | 로드 8/8 · 규칙 4/4 · 매거진 재발 0 · 신규 결함 0 | 0단계 통과(`20260912b` 첫 curl에 11건, 대기 0분. 라이브 md5 = HEAD de50731 = 19bfcb7+문서 커밋, 어드민 19파일 전부 일치). 새 탭 실측: dashboard js 10(core·nav·dashboard·logs·settings·regulations·leads·users·notices·inquiries, 전부 `?v=20260912b`)+admin.css 200, 콘솔 0, 옛 스탬프 `?v=20260912"` 요청 0(라이브 HTML 6페이지 grep 0). 별도 5페이지 core·nav·css 200·콘솔 0. brand-portal/dashboard 인라인 오류 0(→ login.html). cdnjs 2·font-awesome·gstatic 4 curl 200. 규칙 REST notices/inquiries/leads 403·settings/config 404 기대대로. 매거진 새 자동 발행 0(magazine.html md5 로컬=라이브, 카드 112, `</head>` 없는 기사 0). 재발견 A-028·A-031·A-032. 등록 후보 1(Low): 포털·어드민 대시보드의 `firebase-config.js`·`search.js`·`article-editor.js` 캐시 스탬프 없음(A-025 계열) | 없음 |
