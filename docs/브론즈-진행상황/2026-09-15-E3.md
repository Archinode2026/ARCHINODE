# 브론즈 마라톤 — 2026-09-15 E3 라운드

범위: R3 전문가 여정 심층(지시서) + E2 "Access denied. Admin only." 원인 조사.

## 0단계
- 판 대조: `lang.js`를 curl로 받아 로컬과 diff 0 → 라이브 = 로컬 ✓ (커밋 `3cf23db`)
- 로그인: 시작 시 "seed" 탭에 `wool21wool@gmail.com`(어드민) 이미 로그인 상태 확인
- **⚠️ 실시간 병렬 세션 확인**: `node tools/bronze-token.js`를 qa-pro 계정으로 처음 2회 실행 시 `EADDRINUSE :52719`로 즉시 실패(15초 간격 재시도도 동일) — 다른 브론즈 라운드가 그 순간 같은 브리지 포트를 쓰고 있었던 것으로 판단. 이후 재시도(3차)는 정상적으로 포트를 잡고 URL을 발급함 — **지휘 창께 실시간 병렬 라운드 존재를 재확인 보고**(오늘 C·C2·A2 라운드가 이미 같은 문제를 보고한 바 있음, 아직도 진행 중으로 추정)

## E2 "Access denied. Admin only." 원인 조사 (요청받은 대로 코드로만, 고치지 않음)
- `admin/dashboard.html:357-364` 확인: 로그인은 됐지만 `isAdmin(user.email)`이 false면 `alert('Access denied. Admin only.')` → `auth.signOut()` → `index.html`로 리다이렉트. 증상(얼럿+로그아웃+index.html 리다이렉트)이 E2 기록과 정확히 일치.
- `auth/token-entry.html:40-46` `sanitizeTo()` 확인: `--to` 값에 `/`로 시작하거나 `..`·`:` 등이 있으면 **거부하고 기본값 `admin/dashboard.html`로 떨어짐**. 브랜드 계정으로 이 기본값에 착륙하면 위 admin 가드에 걸려 정확히 그 증상이 남.
- 오늘 첫 라운드 기록(`docs/브론즈-진행상황/2026-09-15.md:20`)에 이미 동일 원인(`--to /brand-portal/dashboard.html`처럼 선행 슬래시를 붙여 3회 실패 → 슬래시 제거 후 정상)이 문서화되어 있음.
- **결론(코드 검토 + 이번 라운드 실측 둘 다로 확인): 스크립트나 계정의 결함이 아니라 `--to` 인자에 선행 슬래시를 붙였을 때 발생하는 알려진 사용법 문제.** 이번 라운드에서 `--to brand-portal/dashboard.html`(슬래시 없이)로 qa-brand 로그인 실제로 성공(아래). qa-brand 계정 자체는 정상(정지·삭제 상태 아님).

## 실행 시나리오 (전문가 계정 qa-pro@archinodekr.com 로그인 성공)
1. **좋아요 — 브랜드 상세**: `brands/view.html?id=test-bronze-qa-brand`에서 "Like" 버튼 클릭(이미 좋아요한 상태였음, 토글) → 콘솔에 `Brand like error: {code: permission-denied}` 재현. 화면 카운트는 0 유지. **기존 A-032(검수관리 탭, High)와 완전히 동일한 증상 — 실측으로 재확인, 신규 등록 안 함. A-032에 "제품도 같은 문제일 것" 서술이 있어 코드 검토로 products/view.html의 `toggleLike()`를 확인 → brands와 동일한 패턴(본인 아닌 계정이 `products/{id}.likeCount`를 직접 update, firestore.rules도 `isAdmin()||본인brandId`만 허용)임을 확인하고 A-032에 히스토리 메모로 추가**(상태는 그대로 Pending review 유지, 새 항목 안 만듦). 승인된 제품이 라이브에 0건이라 제품 좋아요는 실클릭 재현은 이번에도 못 함(코드 확인만).
2. **마이페이지(`auth/profile.html`)**: "Liked Brands"에 `[테스트-브론즈] QA Brand`가 정상적으로 뜸(users.likedBrands는 정상 반영됐다는 뜻) → **찜 목록 렌더 정상 확인**. "Liked Products"는 빈 상태(제품 좋아요 자체를 실행한 적 없어 당연). 프로필 수정 폼 존재 확인(저장 버튼 등, 이번 라운드에선 실제 값 변경·저장까지는 시도 안 함 — R3 범위상 우선순위 낮게 판단).
3. **브랜드 대시보드 통계 반영(`brand-portal/dashboard.html`, qa-brand 계정으로 재로그인)**: "BRAND LIKES" 카드가 **0**으로 표시됨 — 전문가가 이미 좋아요를 눌렀음에도(1번 시나리오) 브랜드 쪽 통계에도 전혀 안 잡힘. A-032의 근본 원인(권한 규칙이 likeCount 증가를 막음)이 브랜드 대시보드까지 이어짐을 실측으로 확인(신규 등록 안 함, A-032 범위 내). 같은 화면에서 `Cannot read properties of undefined (reading 'substring')` (`brand-portal/dashboard.html:1151:10`) 콘솔 오류 재현 — 기존 A-025와 동일 계열(오늘 C 라운드가 이미 "재발견" 메모 남김, 이번엔 추가 등록 안 함).
4. **헤더 로그인 상태(`auth-ui.js`)**: `brands/view.html` 페이지에서 qa-pro 로그인 상태로 헤더에 "My Page" 정상 표시(브랜드 계정용 "Brand Login" 정적 링크 앞에 삽입되는 기존 설계 그대로 동작) → **정상 확인**. 반면 `categories/furniture/sofas-armchairs.html`(카테고리 서브페이지)에서는 콘솔에서 `firebase.auth`가 아예 함수가 아님을 확인(`firebase-auth-compat.js` 미로드) → 헤더에 로그인 상태가 전혀 안 뜨고 "Brand Login"만 보임. **기존 A-047과 정확히 같은 원인·증상 — 실측 재확인, 신규 등록 안 함.**

## 판정 보류로 처리한 관찰 (신규 등록 안 함)
- "seed" 탭(원래부터 다른 라운드와 공유되고 있던 탭)에서 `auth/profile.html`·`brands/view.html`을 열 때마다 콘솔에 `firebase.firestore is not a function`(`firebase-config.js:30:21`) + 리소스 400 오류가 반복 재현됨. **완전히 새로 연 탭(tab-1)에서 같은 `auth/profile.html`을 열었더니 이 오류가 전혀 안 남** → 사이트 코드 결함이 아니라 "seed" 탭이 동시에 실행 중인 다른 브론즈 라운드의 요청과 뒤섞이며 생기는 리소스 경합으로 판단, **등록하지 않음**. 참고용으로만 기록.

## 만든 테스트 데이터
- 없음(이번 라운드는 기존 qa-pro·qa-brand 테스트 계정·기존 데이터만 사용, 신규 생성 없음)

## 검수관리 등록
- 신규 등록: 없음
- 히스토리 메모 추가: A-032("제품도 같은 문제" 서술을 코드 검토로 확인한 내용 추가, 상태 변경 없음, `git log -1` 확인 후 Firestore 쓰기 직전 계정 재확인 `wool21wool@gmail.com` — 절차 준수)
- 재확인(등록 안 함, 이미 존재): A-032(브랜드 좋아요 permission-denied, 브랜드 대시보드까지 0으로 이어짐), A-025(대시보드 substring 오류), A-047(카테고리 서브페이지 auth-compat 미로드)

## 못 본 것
- 제품 좋아요 실클릭 재현 — 라이브에 승인된 제품 0건이라 불가능(코드 검토로만 확인, products.likeCount도 동일 규칙 패턴)
- 프로필 정보 실제 수정·저장 — 이번 라운드에서 시도 안 함(우선순위상 후순위로 판단)
- qa-brand 세션에서 Products/Articles/Inbox 등 추가 탭 — 이번 라운드는 통계 카드 확인 직후 세션 안정성을 위해 최소한으로 마무리(병렬 세션 경합 확인된 상태에서 다단계 작업은 A-031 재발 위험)
