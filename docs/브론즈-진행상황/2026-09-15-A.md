# 브론즈-A 진행상황 — 2026-09-15

라운드 이름: 「브론즈-A R1」— 어드민 전 화면 시각적 반응형 훑기 (1920/1280/768px)

## R1 — 어드민 사이드바 전 메뉴 × 3폭 시각 검수

- 시작~종료: 2026-09-15, 약 1라운드 분량
- 로그인: `tools/bronze-token.js` 브리지 열쇠로 `wool21wool@gmail.com` 로그인 — **총 3회 재로그인 필요**(아래 참고)
- 확인 폭: 1920px, 1280px, 768px (대시보드·브랜드·리드 인박스·1:1문의·전문가회원·제품·아티클·공지사항·활동로그·검수관리·규정집 3탭, + 별도 페이지 트렌드투고·뉴스레터·디지털알림·아티클카드)

### ⚠️ 환경 문제 (제품 결함 아님 — 지휘 창에 보고)

같은 Browser pane을 병렬 라운드(B·C로 추정)가 동시에 쓰고 있어 **세션이 3회 튕겼다**:
1. 처음 로그인 직후 다른 탭(다른 라운드)이 `qa-brand@archinodekr.com`으로 재로그인해 내 "seed" 탭까지 그 계정으로 바뀜(Firebase Auth가 origin당 localStorage 공유라 탭이 달라도 같이 바뀜).
2. 재로그인 후 몇 페이지 넘기자 전체 로그아웃 상태로 전환(다른 라운드의 로그아웃 동작으로 추정).
3. `tools/bronze-token.js` 재실행 시 `EADDRINUSE :52719` — 브리지 서버 포트가 고정이라 두 라운드가 동시에 쓰면 하나는 즉시 실패한다.
→ 매번 새 브리지 열쇠를 발급받아 복구했지만, 병렬 라운드가 같은 브라우저 프로필을 쓰는 한 **로그인 필요한 검증은 언제든 튕길 수 있다.** 다음 병렬 마라톤부터는 라운드별 별도 브라우저 프로필/포트가 필요해 보임.

### 발견한 결함 (검수관리 등록 완료)

- **A-029** 규정집 > 검수대장 탭 — 1920px/768px 둘 다 표 칸이 30~70px로 눌려 있어 한글이 세로로 한 글자씩 쪼개져 거의 못 읽음. 표 부모(`.adm-reg-body`)가 856px로 고정되고 넓은 화면에서도 안 넓어짐(`.adm-main` 1148px cap). 다른 어드민 화면 A-052 1440px 폭 규칙이 규정집의 별도 2열 레이아웃(`adm-reg-layout`)엔 안 먹는 듯.
- **A-030** 트렌드 투고함(`admin/trend-submissions.html`) — 카드 요약 텍스트에 `overflow-wrap`이 `normal`이라 띄어쓰기 없는 긴 문자열이 카드 테두리를 뚫고 삐져나옴(실측: scrollWidth 1032px > clientWidth 675px). "dd" 제목의 테스트성 카드에서 재현(마커 없음, 다른 라운드가 만든 데이터로 추정, 손대지 않음).

### 시각 확인 — 이상 없음

- 대시보드, 리드 인박스, 1:1 문의, 전문가 회원, 브랜드, 제품, 아티클, 공지사항, 활동 로그, 검수관리(Review Board 목록), 규정집 Rulebook/Work Journal 탭 — 1920/1280/768px 모두 글자 겹침·잘림·버튼 밀림 없음.
- 뉴스레터·디지털 알림·아티클(카드) 페이지 — 1920px 정상. 단, "Newsletter Newsletter Subscribers" 제목 중복 문구 발견(등록은 생략 — 우선순위 낮음, 구두 보고만).
- 트렌드투고·뉴스레터·디지털알림·아티클(카드) 4개 별도 페이지는 대시보드와 다른 레이아웃(상단 탭바 + 사이드바 이중 네비)을 쓴다 — 2026-09-12 사이드바 셸 개편 대상에서 빠진 것으로 보임(구조적 관찰, 결함 등록은 안 함).

### 확인 못 함

- 768px에서 별도 4개 페이지(트렌드투고 등)는 세션이 튕겨 재확인 못 함.
- 규정집 페이지에서 `find` 도구가 마크다운에서 자동 생성된 `label[for=...]` 값에 괄호·콜론이 섞여 접근성 트리 파싱이 깨짐(콘솔 에러) — 시각적 문제는 아니라 결함 등록은 안 함, 참고만.
