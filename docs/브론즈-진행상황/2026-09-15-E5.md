# 브론즈 마라톤 — 2026-09-15 E5 라운드 (같은 마라톤, E→E2→E3→E4→E5)

범위: R2 브랜드 여정 심층 — qa-brand 계정으로 브랜드 프로필 편집·제품 등록·매거진 글 작성 → wool21wool@gmail.com(어드민)에서 검수 대기 목록 반영 확인 → 승인 시 confirm() 확인창 지점까지.

## 0단계

- 판 대조: 라운드 시작 시 `curl -s https://archinodekr.com/lang.js`를 로컬과 diff → **완전 동일(SAME)**. 판 일치.
- 로그인 도구 사용법 교정: `Bash(timeout 20 node tools/bronze-token.js ...)`로 처음 3회 시도했는데 전부 `ERR_CONNECTION_REFUSED` — 원인은 `timeout 20`이 Bash 호출 자체를 20초간 막아, 출력을 받는 시점엔 이미 브리지 서버가 죽어 있었기 때문(내 도구 사용 실수). `run_in_background: true`로 바꿔 서버가 살아있는 동안 바로 열어 해결. 이후 정상 로그인.

## ⚠️ 병렬 세션 충돌 실측 (철칙 12·13 관련, 지휘 창께 즉시 보고)

- qa-brand로 로그인 확인(`firebase.auth().currentUser.email` = `qa-brand@archinodekr.com`) 직후 제품 등록 폼을 채우던 도중, **쓰기 직전 재확인에서 계정이 `wool21wool@gmail.com`(admin)으로 바뀌어 있음을 발견** — 클릭 직전 필수 재확인 덕에 잘못된 계정으로 쓰기 전에 잡아냄. 즉시 중단하고 qa-brand로 재로그인 후 폼을 다시 채워 제출.
- 이 재로그인 이후에도 매거진 글 제출 성공 직후(글 자체는 정상 저장 확인됨) 다시 조회해보니 계정이 또 `wool21wool@gmail.com`으로 바뀌어 있었음. 즉 **같은 브라우저 프로필을 최소 하나 이상의 다른 병렬 브론즈 세션이 지금 이 순간에도 계속 쓰고 있음**을 실측으로 재확인(어드민 "최근 활동" 로그에도 R3·R4·R7·R10·R11·R15·R19·R20·A2 등 다수의 동시 진행 흔적이 실시간으로 쌓이고 있었음).
- **다행히 실피해는 없었음**: 제품·매거진 글 둘 다 제출 직전 계정 재확인을 거쳤고, Firestore 직접 조회로 `brandId`가 qa-brand의 정확한 uid(`Cis4CJIMFGdb9WBDyvU4PChq2V42`)로 저장됐음을 사후 확인함. 이 위험 자체는 이미 검수관리 탭 **A-031**(브랜드 포털 — 제품 등록/매거진 글 제출, 계정 뒤바뀜 위험 안내)에 등록돼 있어 신규 등록 안 함.

## 1. 브랜드 프로필 편집 저장 — 확인됨

- qa-brand 로그인 → 브랜드 정보 탭 → "브랜드 설명 / 철학" 텍스트에 `[테스트-브론즈] E5 라운드 수정(09-16) — 브랜드 소개 편집 저장 확인용.` 을 기존 문구 뒤에 덧붙여 Save Changes 클릭.
- Firestore 직접 조회(`brands/{uid}`)로 저장된 `description` 필드가 수정한 문구 그대로임을 확인. 저장 정상.

## 2. 제품 등록 — 확인됨

- 제품 탭 → 제품 추가 → `[테스트-브론즈] E5 Product`, 설명, 카테고리 Furniture/서브카테고리 chairs-stools, 소재 "Oak, Brass", 치수 "W50 x D50 x H80 cm" 입력 → 소스 확인(`brand-portal/dashboard.html:1077` products.add, confirm/prompt 없음) 후 계정 재확인(qa-brand) → Submit for Review 클릭.
- Firestore 조회: `products` 컬렉션에 `brandId: Cis4CJIMFGdb9WBDyvU4PChq2V42`(qa-brand 소유 정상), `status: pending`으로 정상 저장. 어드민 제품 탭에도 `[테스트-브론즈] E5 Product` / `[테스트-브론즈] QA Brand` / PENDING 으로 정상 노출.

## 3. 매거진 글 작성 — 확인됨

- 브랜드 매거진 탭 → Write Article → 제목 `[테스트-브론즈] E5 매거진 글`, Original Language를 Korean으로, 본문 블록에 한 문단 입력 → 소스 확인(`article-editor.js:232` submitArticle, confirm/prompt 없음. 단 블록 삭제·취소 버튼은 confirm() 사용 — 이번엔 안 씀) 후 계정 재확인(qa-brand) → Submit for Review 클릭.
- Firestore 조회(제목으로 직접 검색): `articles` 문서 `brandId: Cis4CJIMFGdb9WBDyvU4PChq2V42`, `brandName: [테스트-브론즈] QA Brand`, `status: pending` 정상. 어드민 아티클 탭에도 정상 노출(PENDING, Review/Approve/Reject 버튼 존재).
- 참고: qa-brand 자신의 매거진 탭에서는 방금 쓴 글이 목록에 안 보임(카드 숫자만 올라감) — 기존 **A-025**(이미 검토대기 상태) 그대로 재현. 신규 등록 안 하고 A-025에 이력 메모만 추가: "재발견 2026-09-16 브론즈-E5"(admin 계정으로 재확인 후 등록, 상태 변경 없음).

## 4. 어드민 승인 — confirm() 지점 확인

- admin/dashboard.html#products, #articles 둘 다에서 방금 만든 항목이 PENDING으로 정상 노출됨(위 2·3절).
- 소스 확인(`admin/dashboard.html:768`): `if (!confirm(...Approve/Reject this item?...)) return;` — **실제로 confirm()을 사용함을 코드로 먼저 확인**.
- `[테스트-브론즈] E5 Product`의 실제 Approve 버튼을 실제 UI 클릭으로 눌러봄 → 콘솔에 `[Claude browser] Page dialog suppressed (confirm): "Approve this item?" — native JavaScript dialogs are disabled in this browser; confirm() returned false to the page.` 로그 확인 — 이 자동화 브라우저 환경이 네이티브 confirm을 자동 거부(false)하도록 막아놔서 승인이 끝까지 안 감(철칙 4에 정확히 해당, 콘솔에서 함수를 직접 호출하는 우회는 시도하지 않음 — 철칙 13 준수). Firestore 재조회로 상태가 여전히 `pending`임을 확인(변경 없음, 실피해 없음).
- **결과**: 확인창이 뜨는 지점까지만 확인 완료. 승인 후 공개 노출(5번 항목)은 이 환경에서 끝까지 진행 불가 — "확인 못 함"으로 남김.

## 5. 승인 → 공개 노출 확인

- 4번에서 승인이 confirm()에 막혀 끝까지 못 갔으므로 **확인 못 함**(환경 한계, 결함 아님).

## 6. 브랜드 정지→복구

- **위험 대비 보류**로 건너뜀. 사유: 이번 라운드 도중 실측으로 확인된 것처럼 다수의 다른 병렬 브론즈 세션이 지금 이 순간에도 같은 `qa-brand@archinodekr.com` 계정으로 각자 작업 중인 것으로 보임(어드민 최근 활동 로그에 R3·R4·R7·R10·R11·R15·R19·R20·A2 등 동시다발 흔적). 이 계정을 정지시키면 그 병렬 세션들의 로그인이 막혀 전체 마라톤에 지장을 줄 수 있어, 이번 라운드 지시서에 명시된 대로 건너뜀.

## 🧹 만든 테스트 데이터

- `brands/{qa-brand uid}` — `description` 필드에 `[테스트-브론즈] E5 라운드 수정(09-16)` 문구 추가(기존 내용 유지, 삭제 없음)
- `products/NPCM6nDbCz26sjNW8wJL` — `[테스트-브론즈] E5 Product` (PENDING, brandId 정상)
- `articles/6JVi6IeDQyTXW9NoYFjG` — `[테스트-브론즈] E5 매거진 글` (PENDING, brandId 정상)
- `reviewIssues/EPgObxPBHtt3z5C93Pqc`(A-025) history에 이력 메모 1건 추가(상태 변경 없음)

## 📋 검수관리 등록

- 신규 등록: 없음.
- 재발견 이력 메모만 추가: **A-025**(브랜드 포털 매거진 탭 목록 안 뜸 — 재발견 2026-09-16 브론즈-E5).
- 이미 등록돼 있어 신규/이력 등록 안 함: **A-031**(브랜드 포털 제품·글 제출 중 계정 뒤바뀜 위험 — 이번 라운드에서 실제로 재현됨을 위 "병렬 세션 충돌" 절에 상세 기록).

## 못 본 것

- 5번(승인 후 공개 노출) — confirm() 자동 거부로 승인 자체가 끝까지 안 가서 확인 못 함.
- 6번(정지→복구) — 병렬 세션 충돌 위험으로 위험 대비 보류.

## 지휘 창께 보고 — 병렬 세션 재확인

이번 라운드 중 최소 1개 이상의 다른 브론즈 세션이 같은 브라우저 프로필(같은 origin의 localStorage)을 동시에 쓰고 있음을 두 차례 실측으로 재확인했습니다(계정이 qa-brand → admin으로 예고 없이 두 번 바뀜). 매 Firestore 쓰기 직전 계정 재확인 절차 덕분에 실제 데이터 오염은 없었습니다(브랜드 소유권 전부 정상). 다음 라운드도 같은 절차를 유지해야 합니다.
