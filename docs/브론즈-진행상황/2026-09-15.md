# 브론즈 진행상황 — 2026-09-15

지휘 창 직접 지시(A-051 단건 확인) — 마라톤 아님, 단발 라운드.

## R14 (A-051 재검증 단건)

- **시각**: 2026-09-15 오후, 열쇠 발급~검증 약 10분
- **0단계 판 대조**: 커밋 `7a5a27b` = 라이브 `admin/js/admin-nav.js` (diff 0, "A-051" 마커 라이브에서도 확인) → ✓
- **로그인**: `tools/bronze-token.js --email wool21wool@gmail.com` 열쇠로 어드민 로그인 성공(`firebase.auth().currentUser.email` = wool21wool@gmail.com, 리다이렉트 없음). 단, `--to` 인자가 Git Bash 경로 변환으로 깨져(`/admin/dashboard.html` → `C:/Program Files/Git/admin/...`) 착륙 페이지 대신 루트로 갔다 — 이후 수동으로 `admin/dashboard.html` 재방문
- **통과 1 / 실패 0**
  - ✅ **A-051**(어드민 사이드바 KO\|EN 토글 첫 화면 오표시): 로그인 직후 첫 화면 "KO \| EN" 정상, 하드 네비게이션(재방문) 4회 연속 전부 "KO \| EN" 유지(과거 버그였던 "Korean \| English" 재현 안 됨) · 토글 클릭 시 EN 활성화 + 전체 페이지 영문 전환 정상 · 버튼 `title` 속성 KO="한국어", EN="English" 확인 → **확인완료**
- **검수관리 등록**: 어드민 검수관리(reviewIssues) 게시판에는 A-051 항목 자체가 없음(지휘 창이 `docs/검수대장.md`에만 직접 등록한 건 — A-001~A-011만 문서 대장 이관으로 게시판에 있음). 게시판 신규 등록 안 함, `docs/검수대장.md` A-051 상태만 확인대기 → 확인완료로 직접 갱신
- **만든 테스트 데이터**: 없음
- **확인 못 함**: 없음(지시 범위 내 전부 실측 완료)

## R15 (지휘 창 직접 지시 — A-052~054 실측, A-043·A-044 재검증 단건)

- **시각**: 2026-09-15 오후, 약 40분
- **0단계 판 대조**: 커밋 `e47f4b4` = 라이브 `admin/dashboard.html`·`admin/admin.css` 3개 규칙(`.admin{max-width}`, `#adminSidebar`, `.status`) 전부 diff 0 확인 → ✓
- **로그인**: 어드민(wool21wool@gmail.com) 열쇠 로그인 성공(리다이렉트 없음). 브랜드 테스트 계정(qa-brand@archinodekr.com)도 성공 — 단, `tools/bronze-token.js --to`에 **선행 슬래시("/...")를 붙이면 안 됨**. `auth/token-entry.html`의 `sanitizeTo()`가 첫 글자가 영숫자가 아니면 거부하고 기본값 `admin/dashboard.html`로 떨어진다(코드 확인). 이번 라운드에서 처음엔 몰라서 `--to /brand-portal/dashboard.html`로 3번 실패(어드민 대시보드로 강제 착륙 → 비관리자라 "Access denied. Admin only." 알림 + 로그아웃, 정상 동작이지 버그 아님). `--to brand-portal/dashboard.html`(슬래시 없이)로 고치니 정상 착륙. **다음 라운드를 위한 메모: --to는 선행 슬래시 없이.**
- **통과 4 / 재현 안 됨 2 / 신규 발견 1(A-017, 우선순위 낮춰 등록)**
  - ✅ **A-052**(어드민 본문 1200→1440px): 1920px 뷰포트에서 `.admin` 요소 실측 폭 1440px, `max-width` 계산값도 1440px 일치 → **실측 확인**
  - ✅ **A-054**(상태 배지 한글 세로줄바꿈): 검수관리 탭에서 "검토대기"·"보류" 배지 실측 24건 전부 height 20~22px(한 줄), `white-space: nowrap` 적용 확인, 세로 쌓임 없음 → **실측 확인**
  - ⚠️ **A-053**(사이드바 자체 스크롤): CSS는 라이브 반영됐고, `sidebar.scrollTop`을 JS로 직접 바꾸면 정상 스크롤됨(scrollHeight 910 / clientHeight 500, 아래쪽 메뉴 정상 노출) — **프로그램적으로는 확인**. 그러나 실제 마우스 휠 조작(Claude Browser 자동화 도구의 scroll 액션, 좌표 정확도는 별도 클릭 테스트로 검증됨)으로 사이드바 위에서 스크롤하면 사이드바가 아니라 페이지 전체가 스크롤되고 사이드바 내부는 움직이지 않음(재현 2회). 자동화 도구의 한계인지 실제 브라우저 동작인지 구분 못 해 **검수관리 탭에 A-017로 별도 등록**(Low, 실사람 손으로 재확인 요청)
  - ✅ **A-043**(어드민 콘솔 색인 오류 4건): 대시보드·리드 인박스·1:1 문의·전문가 회원·검수관리 탭 + 별도 페이지 4종(consultations·trend-submissions·newsletter-subscribers·notify-subscribers) 전부 콘솔 오류 0건 → **이번에도 재현 안 됨**(R13에 이어 2연속)
  - ✅ **A-044**(1:1 문의 배지 깜빡임): 브랜드 테스트 계정으로 미답변 문의 1건을 새로 만들어([테스트-브론즈] R15 배지 깜빡임 검증용 문의) 열린 문의 카운트를 1로 만든 뒤, 어드민 1:1 문의 탭에서 「새로고침」을 2회 눌러 각각 10ms·40ms 간격으로 대시보드 카드 숫자(`dashNum-inquiries`)를 폴링(총 709회 샘플) — 값이 "1"에서 한 번도 "0"이나 빈 값으로 안 바뀜 → **이번 라운드에서는 재현 안 됨**(단, 초고속 새로고침 시 한 프레임짜리 찰나의 깜빡임은 폴링 간격상 못 잡았을 가능성은 남음)
- **검수관리 등록**: **A-017**(사이드바 마우스 휠 스크롤 안 됨 의심, Low, 검토대기) — 문서 대장(docs/검수대장.md)의 번호 체계와는 별개로 게시판 자체 번호. A-052~054는 게시판에 항목이 없어(R14와 동일 사유 — 지휘 창이 `docs/검수대장.md`에만 직접 등록한 건) 문서 대장 상태만 갱신 대상.
- **만든 테스트 데이터**: `[테스트-브론즈] R15 배지 깜빡임 검증용 문의`(브랜드 포털 Support → 어드민 1:1 문의, 미답변 상태로 남겨둠 — 지우지 않음)
- **확인 못 함**: A-053의 실제 마우스 휠 동작(자동화 도구 한계 가능성 때문에 사람 손 재확인 필요)

## R16 (지휘 창 직접 지시 — A-009·7b·A-046·A-047·A-048 미확인 항목 실측)

- **시각**: 2026-09-15 저녁, 약 45분
- **0단계 판 대조**: 커밋 `54100e6`(docs 전용) = 라이브 `admin/js/admin-nav.js`·`style.css`·`privacy-en.html` 3개 파일 전부 diff 0 확인 → ✓
- **로그인**: 어드민(wool21wool@gmail.com), 전문가 테스트 계정(qa-pro@archinodekr.com) 열쇠 로그인 모두 성공(리다이렉트 없음)
- **통과 3(실측 확인) / 재현 2(신규 발견 아님, 기존 항목 재확인) / 재현 안 됨 1 / 신규 발견 1(A-018)**
  - ✅ **A-009**(브랜드 정지→복구 이력 배열): `[테스트-브론즈] QA Brand`로 정지→복구 실행, 브랜드 상세 모달 「이력」 절에 이번 정지·복구가 각각 별도 줄(누가·언제·왜)로 기록됨, R11 때 남긴 이전 4건 이력도 그대로 유지돼 총 5줄 누적. 콘솔 오류 0 → **실측 확인**
  - ⚠️ **7b 유입경로 실제 제출**: `list-your-brand.html?ref=qa-brz-r16` 방문 시 `sessionStorage.archinode_ref` 저장은 재확인(R13에 이어 2연속 정상). 폼 전체(브랜드명·국가·웹사이트·카테고리·설명·담당자·이메일·전화·유입경로)를 `[테스트-브론즈]` 마커로 채운 뒤, **실제 제출(Submit) 클릭은 하지 않음** — 이 폼 제출은 Firebase Auth 브랜드 계정을 새로 만들고(지워지지 않음), 브론즈 규칙 철칙 3("전문가 가입·입점 신청의 최종 제출은 대행하지 않는다")에 해당하는 사례라 실행하지 않고 폼 검증까지만 확인. 대신 코드검토로 `list-your-brand.html:622` `referralSource: getReferralSource()`가 실제 제출 payload에 포함되는 것을 확인 — 세션스토리지 값이 Firestore 문서에 실릴 것으로 코드상 보이나 **실측(제출→Firestore→어드민 표시)은 못 함**. 실제 제출·어드민 표시 확인은 한울님이 실브라우저로 하거나, 계정 생성을 감수할지 판단 필요
  - ✅ **A-046**(카테고리 서브페이지 색인 오류): `sofas-armchairs.html`·`pendant-lights.html` 2개 서브페이지 콘솔에서 동일 `failed-precondition` 재현. 소스 확인(`category-products.js`)으로 정확한 조회 조건 특정 — status/category/(subcategory)/createdAt 4단 복합색인 필요(기존 추정보다 한 단계 더 필요) → **재현 확인**
  - ✅ **A-047**(로그인 상태 헤더 미반영): `qa-pro@archinodekr.com` 로그인 상태로 카테고리 서브페이지 2곳 확인, 둘 다 재현(브랜드 로그인만 뜸). 대조군 brands.html은 정상(My Page). 코드 확인으로 **추가 원인** 발견 — 순서 문제 이전에 이 페이지들은 `firebase-auth-compat.js` 스크립트 자체가 안 실려 있어(app-compat·firestore-compat만) `firebase.auth`가 애초에 함수가 아님(BUG-1 계열 함정과 동일) → **재현 확인, 원인 보강**
  - ✅ **A-048**(privacy-en.html 모바일 헤더): 375px 실측, `scrollWidth`=`clientWidth`=375(가로 스크롤 없음), 헤더 한 줄 정돈. 코드 확인 결과 이 페이지 인라인 미디어쿼리는 `.header-actions`/`.logo-sub`를 안 건드려 style.css 수정과 충돌 없음 → **재현 안 됨(정상)**
  - 🆕 **A-018**(신규, Low): 어드민 대시보드를 태블릿 폭(768px)에서 열면 "최근 활동" 표가 화면보다 넓어(scrollWidth 779 / clientWidth 753) 페이지 전체가 가로로 밀림. 1280px·1920px는 정상. `admin.css` `.adm-log-table`에 다른 표(`.adm-reg-body table`)처럼 `overflow-x:auto` 처리가 없는 게 원인으로 추정. 검수관리 탭에 등록 완료
- **검수관리 등록**: **A-018**(어드민 대시보드 태블릿 768px 가로 스크롤, Low, 검토대기)
- **만든 테스트 데이터**: 없음(list-your-brand.html 폼은 채우기만 하고 제출하지 않음 — Firestore에 저장된 것 없음)
- **확인 못 함**: 7b 유입경로의 실제 제출→Firestore 저장→어드민 "Referral Source" 표시(계정 생성 회피 규칙으로 실측 보류, 코드검토로만 확인)

## R17 (지휘 창 직접 지시 — A-001·A-014·A-035·A-049·A-037 실브라우저+로그인 재검증)

- **시각**: 2026-09-15 밤, 약 1시간
- **0단계 판 대조**: `git log -1` 커밋 `97a9dc7`. 확인 결과 로컬 `main`이 origin보다 커밋 2개 앞서 있음(`54100e6`·`97a9dc7`, 둘 다 `docs/` 전용 — R15·R16 결과 기록. 코드 변경 없음). 이번 라운드에서 실제로 검증하는 5건(A-001·A-014·A-035·A-049·A-037)의 수정 커밋(`bc430e0`·`95f9e3b`·`c862bac`)은 전부 `origin/main`에 이미 포함돼 있음을 `git merge-base --is-ancestor`로 확인, 관련 파일 5개(`firebase-config.js`·`style.css`·`auth/login.html`·`brand-portal/dashboard.html`·`admin/js/admin-nav.js`) md5 라이브=로컬 전부 일치 → 이번 라운드 대상 기준으로는 **판 일치 ✓**(단, docs 2개 커밋 미푸시는 지휘 창에 별도 보고)
- **로그인**: 어드민(wool21wool@gmail.com), 브랜드 테스트 계정(qa-brand@archinodekr.com) 열쇠 로그인 모두 성공. **주의(재발 방지 메모)**: 두 계정을 별도 탭에서 동시에 로그인해 두면, Firebase Auth 세션이 브라우저 저장소를 공유해 나중 로그인이 먼저 로그인을 덮어써서 어드민 탭이 갑자기 "Access denied"로 로그아웃된다(실제로 한 번 발생, 어드민 재로그인으로 복구) — 다음부터는 계정별로 순차 처리하고 검수관리 등록처럼 어드민 권한이 필요한 작업 직전에 반드시 `firebase.auth().currentUser.email`로 재확인할 것
- **통과 4 / 부분 통과 1(A-001) / 신규 발견 2(A-019·A-020)**
  - ✅ **A-014**(모바일 헤더 375px): `index.html`·`brands.html`·`request-consultation.html`·`magazine.html` 4페이지 전부 `scrollWidth`=`clientWidth`=375(가로 스크롤 없음), 헤더 항목 각각 한 줄(`getBoundingClientRect` height 16~32px, 세로로 안 쪼개짐), 우측 끝 요소도 화면 밖으로 안 나감 → **실측 확인**
  - ✅ **A-035**(어드민 별도 페이지 5개 언어 유지): 어드민 한국어 상태에서 사이드바로 `consultations.html`→`trend-submissions.html`→`newsletter-subscribers.html`→`notify-subscribers.html`→`articles.html` 5페이지를 차례로 이동, 매번 `html lang="ko"` 유지 + 사이드바 전체 한국어 텍스트 유지 확인. `consultations.html`에서 KO\|EN 토글 클릭 시 `html lang="en"` + 사이드바 영문 전환 정상, 버튼 `title` 속성 "한국어"/"English"(A-051 수정 반영) 확인 → **실측 확인**(단, 본문 영역(`<h1>Consultation Requests</h1>` 등)과 레거시 상단 탭바는 사이드바와 별개로 여전히 영어 고정 — 이번 지시 범위인 "사이드바가 한국어로 유지"는 충족하나, 화면 전체 완전 다국어화는 A-017·A-029 계열 별건으로 남아 있음)
  - ✅ **A-049**(브랜드 포털 상단바 322px): qa-brand 로그인 상태에서 322px로 축소, "브랜드 가이드" rect(w116.5·h33·top37·right306)·"로그아웃" rect(w91.9·h32·top76·right306) 모두 완전한 단어로 한 줄, 화면 위로 안 잘리고(top이 양수) 우측도 페이지 폭(322px) 안. 페이지 `scrollWidth`=`clientWidth`=322 → **실측 확인**
  - ✅ **A-037**(auth 4페이지 noindex): `curl`로 `auth/login·signup·profile·reset-password.html` 4개 파일 head 확인, 전부 `<meta name="robots" content="noindex,nofollow">` 라이브에 존재 → **실측 확인**
  - ⚠️ **A-001**(부분 확인, 부분 재현): (1) 홈(`index.html`) 콘솔 오류 — 새 탭으로 열어 확인한 결과 **0건**(기존 브라우저 탭에 남아있던 카테고리 서브페이지발 오류 로그가 섞여 보였던 것은 탭을 새로 열어 구분함, 철칙 8 관련 도구 한계 메모) → 이 부분은 **해소 확인**. (2) 로그인 상태가 홈 상단에 반영되는지 — qa-brand 계정으로 로그인한 채 `index.html`을 열었더니 상단이 계속 "브랜드 로그인"만 보이고 "Dashboard" 버튼이 안 뜸(같은 계정으로 `brands.html`을 열면 정상적으로 "Dashboard" 뜨는 것과 대조). 원인: `index.html`이 `firebase-auth-compat.js` 스크립트 자체를 안 실어서(A-047과 같은 계열 결함이 홈페이지 자체에 있음) `auth-ui.js`가 조용히 아무 것도 안 함 → **재현(미해결), 검수관리 A-019로 신규 등록**. (3) 트렌드 리포트 최근 목록 — `trend-report.html`을 열면 "최근 브랜드 매거진" 자리에 항상 "유럽 브랜드의 리포트가 곧 도착합니다" 빈 상태 안내만 뜸. 콘솔에는 error 레벨이 아니라 warn 레벨로만 찍혀 있어(`articles load failed` + `failed-precondition`) 일반적인 "콘솔 오류 확인"만으로는 못 잡음 — `db.collection('articles').where(status==approved).orderBy(publishedAt,desc)` 쿼리에 복합색인이 없어서 항상 실패, UI는 실패 시 그대로 빈 상태 유지 → **재현(미해결), 검수관리 A-020으로 신규 등록**. **결론: A-001은 원래 세 증상(콘솔 오류·로그인 미반영·트렌드 목록 안 뜸) 중 콘솔 오류만 실제로 고쳐졌고, 나머지 둘은 다른 원인으로 계속 남아 있음 — "확인완료"로 판정하지 말 것을 지휘 창에 제안**
- **검수관리 등록**: **A-019**(High, 홈 화면 로그인 상태 미반영 — index.html auth-compat 스크립트 누락) · **A-020**(High, 트렌드 리포트 최근 목록이 색인 누락으로 항상 빈 상태)
- **만든 테스트 데이터**: 없음(기존 테스트 계정 열쇠 로그인만 사용)
- **확인 못 함**: 없음(지시 범위 내 5건 전부 실측 완료, 그 과정에서 A-001의 미해결 부분을 추가로 발견)

## R18 (마라톤 지시 — 공개 여정 데이터 반영 통 검증, R1 풀사이클 성격)

- **시각**: 2026-09-15 밤, 약 1시간
- **0단계**: 로컬 `main`(`f46230c`)이 origin보다 docs 커밋 3개(`54100e6`·`97a9dc7`·`f46230c`, 전부 `docs/`) 앞서 있으나 코드 파일은 무관. 이번 라운드에서 실제로 만지는 4개 파일(`request-consultation.html`·`submit-trend.html`·`digital-products.html`·`contact.html`) + `index.html`을 `curl`로 받아 로컬과 diff 0 확인 → **판 일치 ✓**. 로그인: 어드민(wool21wool@gmail.com) 열쇠 로그인 성공(`admin/dashboard.html` 직접 착륙, 리다이렉트 없음, `firebase.auth().currentUser.email` 확인)
- **실행 시나리오 6 · 통과 4 / 구조 변경 발견 1(J3) / 콘솔·데이터 결함 발견 1(J2)**
  - ✅ **J2 자문 요청**(`request-consultation.html`): `[테스트-브론즈] R18 자문 QA` 등 전 필드를 마커로 채워 제출 → "요청 접수 완료" 화면 정상, `admin/consultations.html`에 즉시 반영(PENDING 1건, 내용 그대로) → **데이터 반영 확인**. 단, 제출 시 콘솔에 `ReferenceError: data is not defined`(request-consultation.html:486) 발생 — 관리자 알림용 `mail` 문서 생성 코드가 존재하지 않는 `data` 객체를 참조해 매번 조용히 실패. Firestore `mail` 컬렉션을 직접 조회해 실제로 신규 문서가 생기지 않음을 확인(전체 6건 중 이번 제출분 없음) → **실결함, A-021 등록(High)**
  - ⚠️ **J3 트렌드 투고**(`submit-trend.html`): 페이지를 열어보니 원고 제출 폼 자체가 없어졌음 — `<title>Publish Your Article | ARCHINODE Brand Magazine</title>`로 바뀌어 "매거진 글은 이제 브랜드 포털에서 발행됩니다(승인된 브랜드만)"라는 안내와 입점 신청·포털 로그인 버튼만 있음. 코드 확인 결과 `<form>` 요소 자체가 없어 **제출 자체가 불가능** — 이번 라운드 지시(원고 제출 → 어드민 반영 확인)를 실행할 방법이 없음. `admin/trend-submissions.html`은 여전히 살아 있고 2026-05-12에 들어온 낙서성 원고 1건이 계속 PENDING으로 떠 있음(오래된 잔존 데이터, 손대지 않음) → 버그인지 의도된 개편인지 불명확해 **A-022 등록(Medium, 검토대기)**, CLAUDE.md 화면지도와의 불일치도 함께 기록
  - ✅ **J4 디지털 상품 알림**(`digital-products.html`): `qa-bronze+r18digital@archinodekr.com`으로 알림 신청 → "감사합니다 — 첫 상품 출시 시 1회 알림 메일을 보내드립니다" 정상 표시, `admin/notify-subscribers.html`에 즉시 반영(TOTAL 2, TODAY 1, 이메일·시각 일치) → **데이터 반영 확인**, 콘솔 오류 없음
  - ✅ **뉴스레터 구독**(홈 `index.html` 하단 폼): `qa-bronze+r18newsletter@archinodekr.com`으로 구독 → 네이티브 `alert("Thank you for subscribing!")` (자동화 도구가 자동 억제, 실사용자에게는 실제로 뜸 — 구식 UX라 개선 여지는 있으나 결함 등록은 안 함) 이후 `admin/newsletter-subscribers.html`에 정상 반영(TOTAL 3, TODAY 1) → **데이터 반영 확인**. 단, 이 화면 상단 설명문이 "디지털 상품 출시 알림을 신청한 이메일 목록입니다…"로 `notify-subscribers.html`과 완전히 동일한 문구가 잘못 붙어 있음 → **A-023 등록(Low)**
  - ✅ **J6 문의(Formspree)**(`contact.html`): 빈 폼 제출 시 브라우저 기본 필수값 검증이 정상 작동(포커스가 "이름" 필드로 이동, 제출 안 됨), 이메일 필드에 형식이 아닌 값(`not-an-email`) 입력 시 `checkValidity()` false로 형식 검증도 정상 → **폼 검증 확인**. 실제 Formspree 제출(외부 서비스로 실제 메일 발송)은 지시 범위("검증까지만")에 따라 실행하지 않음
  - **다국어**: J2(자문 요청) 한/영 토글 시 라벨 전부 정상 전환(회사·담당자·이메일 등 → Company/Studio Name·Your Name·Email 등), 카테고리 드롭다운은 두 언어 모두 "Korean Market Entry — 한국 시장 진출"처럼 병기 표시(양쪽 동일, 의도된 디자인으로 보임). J6(contact.html) 한/영 토글도 라벨 전부 정상 전환, 하단 사업자 주소는 두 언어 모두에서 한국어 그대로 유지(법정 표기라 의도된 것으로 보임) — 결함 아님
  - 콘솔·네트워크: 이번 라운드에서 직접 연 4개 공개 페이지(request-consultation·submit-trend·digital-products·contact) 자체 발 신규 오류는 J2의 `mail` 관련 1건 외 없음(네트워크 요청 전부 200). 단, 어드민 탭(seed)의 콘솔 로그에는 이전 라운드부터 브라우저 세션에 누적된 것으로 보이는 카테고리 서브페이지발 구 오류(`header` 중복 선언, `failed-precondition`)와 "Access denied. Admin only." 경고가 섞여 있었음 — 이번 라운드 중 새로 발생한 것이 아니라 브라우저 탭이 이전 세션부터 살아있어 콘솔 버퍼가 누적된 것으로 판단(실측: `firebase.auth().currentUser.email`은 시종 `wool21wool@gmail.com` 유지, 실제 로그아웃 없었음). 철칙 8 관련 도구 한계로 기록만 남김
- **검수관리 등록**: **A-021**(High, 자문 요청 제출 시 관리자 알림 메일이 전혀 생성 안 됨 — `data is not defined` 코드 결함) · **A-022**(Medium, 브랜드 매거진 투고 폼이 통째로 사라져 어드민 트렌드 투고 심사함이 고아 상태 — 버그인지 의도된 개편인지 확인 필요) · **A-023**(Low, 어드민 뉴스레터 구독자 화면 설명문이 디지털 알림 화면 문구를 그대로 복사해 옴)
- **만든 테스트 데이터**:
  - `consultations`: `[테스트-브론즈] R18 자문 QA`(담당자 브론즈 QA, 이메일 `qa-bronze+r18consult@archinodekr.com`, 전화 `010-0000-1801`, 국가 "한국 [테스트-브론즈]") — PENDING 상태로 남김
  - `digital-products-notify`: `qa-bronze+r18digital@archinodekr.com`
  - `newsletter-subscribers`: `qa-bronze+r18newsletter@archinodekr.com`
  - (`mail` 컬렉션에는 위 자문 요청건의 알림 문서가 A-021 결함으로 인해 생성되지 않음 — 확인용으로 기록)
- **확인 못 함**: J6 Formspree 실제 제출→실제 메일 발송 여부(지시 범위상 폼 검증까지만 수행, 최종 제출은 하지 않음). submit-trend.html 개편이 언제·왜 있었는지는 git log 미조사(다음 라운드 또는 지휘 창 확인 필요)

## R19 (마라톤 지시 — A-022 판정 갱신 + 브랜드 여정 심층, R2 성격)

- **시각**: 2026-09-15 밤, 약 1시간
- **A-022 판정 갱신**: 지휘 창이 `git log --follow -- submit-trend.html`로 직접 확인한 결과를 받아, 어드민 검수관리 탭 A-022(`검토대기` → **오탐반려`**)를 갱신. 커밋 `6293e29`(2026-06-01, "Trend Report -> Brand Magazine 통합")가 submit-trend.html을 의도적으로 안내 페이지로 재작성한 것 확인(커밋 날짜·메시지 브론즈가 직접 재확인). history에 지휘 창 메모(admin/trend-submissions.html 고아 데이터 1건 잔존, 루트 CLAUDE.md 화면지도 낡음 — 둘 다 별건) 기록
- **0단계**: 로컬 `main`(`b0d3f4b`)이 origin보다 docs 커밋 여러 개 앞서 있으나 코드 파일 무관. 이번 라운드에서 실제로 확인하는 `submit-trend.html`을 `curl`로 받아 로컬과 diff 0 확인 → **판 일치 ✓**. 로그인: 어드민(wool21wool@gmail.com)·브랜드(qa-brand@archinodekr.com) 열쇠 로그인 모두 성공(리다이렉트 없음)
- **주의(재발 방지 메모, R17에 이어 재확인)**: 한 브라우저에서 다른 탭에 다른 계정으로 로그인하면(예: 브랜드 탭이 열려 있는 상태에서 다른 탭으로 어드민 로그인) 브랜드 탭의 세션이 조용히 덮어써진다(화면은 캐시된 옛 데이터를 보여주지만 `firebase.auth().currentUser.email`은 나중 로그인 계정으로 바뀜). 이번에도 재현 확인, 브랜드 계정 재로그인으로 복구
- **실행 시나리오 6(브랜드 매거진 글 작성·심사 반영, 제품 등록·심사 반영, 브랜드 정보 편집·공개 페이지 반영, 어드민 Articles/Products 탭 반영, 세션 공유 재확인, 공개 브랜드 상세 페이지 확인) · 통과 2 / 신규 결함 5**
  - ✅ **브랜드 매거진 글 제출 → 어드민 반영**: qa-brand 계정으로 "[테스트-브론즈] R19" 글 작성·제출 → Firestore에 `status:pending`으로 정상 저장(콘솔 직접 조회로 확인), 어드민 Articles 탭에 제목·브랜드명·태그·PENDING 상태 정확히 반영, "Articles to Review" 카드도 1로 갱신 → **데이터 반영 확인**. 단, 제출 직후 브랜드 자신의 매거진 탭 목록이 빈 화면으로 깨짐(아래 A-025)
  - ✅ **제품 등록 → 어드민 반영**: qa-brand 계정으로 "[테스트-브론즈] R19 Product"(Furniture/Chairs & Stools) 등록 → Firestore에 `status:pending`으로 정상 저장, 어드민 "Products to Review" 카드 1로 갱신, 브랜드 포털 제품 목록 카드에도 정상 표시(PENDING) → **데이터 반영 확인**. 단, 등록 직후 "Brand data not found. Please contact support." 오탐 경고 발생(아래 A-026, 원인 불확실)
  - ✅ **어드민 승인/거절 확인창 지점**: Articles·Products 탭 모두 Review/Approve/Reject 버튼 확인. Approve/Reject는 `confirm()`을 쓰는 것으로 알려져 있어(철칙 4) **클릭하지 않고 멈춤**. 실행 대신 목록 반영 상태만 읽기로 확인
  - 🆕 **A-024**(신규, **High**): `brands/view.html?id=<slug>` — 승인된 브랜드의 공개 상세 페이지가 **전부** "오류가 발생했습니다 — 브랜드 정보를 불러오는 중 문제가 발생했습니다" 화면만 뜨고 절대 안 열림. 테스트 브랜드(`test-bronze-qa-brand`)뿐 아니라 **실제 입점 브랜드 `oska`도 동일하게 재현**(로그인 여부 무관, 완전 비로그인 상태에서도 재현). 원인: `view.html`이 쓰는 `where('slug','==',X)` 조회가 `firestore.rules`의 `allow read: if resource.data.status=='approved'` 규칙과 구조적으로 안 맞음(Firestore list 쿼리는 쿼리 자체에 규칙과 동일한 필드 조건이 없으면 결과 문서가 실제로 approved여도 사전에 거부함 — `where('status','==','approved')` 단독 쿼리는 콘솔에서 성공, `where('slug',...)`만 쓰는 쿼리는 매번 `permission-denied` 재현). **사실상 브랜드 상세 페이지 기능 전체가 라이브에서 멈춰 있는 상태로 보임** — 문서 대장 A-005("브랜드가 글을 써도 자기 페이지에서 안 보인다")와 증상이 겹칠 수 있어 같이 검토 제안
  - 🆕 **A-025**(신규, **High**): 브랜드가 매거진 글을 제출해도 자기 포털의 "Brand Magazine" 탭 목록이 텅 빈 채로 아무것도 안 뜸("작성한 글이 없습니다" 안내조차 없음). 원인: `brand-portal/dashboard.html:1159`의 `a.content.substring(0,100)`이 실제 저장 스키마(`blocks[].content` + `excerpt`, 최상위 `content` 필드 없음)와 안 맞아 렌더 루프가 첫 글에서 바로 죽음(콘솔 원문 재현 2회: `TypeError: Cannot read properties of undefined (reading 'substring')`). 데이터 자체는 정상 저장됨(Firestore 직접 조회로 확인) — 브랜드는 자기가 쓴 글이 제출됐는지, 심사 상태가 뭔지 알 방법이 전혀 없음
  - 🆕 **A-026**(신규, Medium, **원인 불확실**): 제품 등록 심사 요청 클릭 직후 "Brand data not found. Please contact support." 경고가 떴으나, 실제로는 제품이 정상 등록됨(어드민 반영 확인). 브론즈가 같은 브라우저에서 어드민·브랜드 계정을 다른 탭으로 번갈아 로그인하며 검증한 영향(R17 기존 한계)일 가능성이 있어 재현 조건 불확실 — 참고용으로 등록, 실사용자 단일 탭 환경 재검증 필요
  - 🆕 **A-027**(신규, Low): 브랜드 포털 상단 통계 카드가 PRODUCTS와 ARTICLES를 다른 기준으로 세는 것으로 보임 — 심사 대기 중인 글은 ARTICLES 카드에 즉시 반영(1)되는데, 심사 대기 중인 제품은 PRODUCTS 카드가 계속 0으로 남음(목록 자체엔 정상 표시)
  - 🆕 **A-028**(신규, Low): 홈 화면(index.html) Brand Magazine 섹션에 A-022로 이미 없어진 것이 확인된 옛 투고 방식 안내 문구("Submit your editorial trend report. ARCHINODE editors review within 5 days.")가 그대로 남아 있음 — 2026-06-01 리브랜딩 때 이 카피만 갱신에서 빠진 것으로 추정
  - **브랜드 정보 편집**: Tagline/Slogan 필드에 "[테스트-브론즈] R19 편집 확인" 입력 후 저장 → Firestore에 즉시 반영 확인(콘솔 직접 조회, `updatedAt` 갱신 확인) → **저장 자체는 정상**. 승인 없이도 반영되는지는 A-024로 인해 공개 페이지에서 눈으로 확인 불가(페이지 자체가 안 열림) — Firestore 데이터 수준에서는 승인 절차 없이 즉시 반영됨을 확인
  - 콘솔·네트워크: 이번 라운드에서 새로 발견된 오류는 A-025(`substring` TypeError, 반복 재현)·A-026(원인 불확실 alert)·A-024(`permission-denied`, 반복 재현) 외에 추가 신규 없음. 기존 R18발 `data is not defined` 경고, 카테고리 서브페이지발 구 오류들은 브라우저 세션 누적분(철칙 8 관련, 새로 발생한 것 아님)
- **검수관리 등록**: **A-024**(High, 브랜드 상세 페이지 전체 로딩 실패) · **A-025**(High, 브랜드 포털 매거진 글 목록 렌더 실패) · **A-026**(Medium, 제품 등록 시 오탐 경고, 원인 불확실) · **A-027**(Low, 통계 카드 집계 기준 불일치) · **A-028**(Low, 홈 화면 옛 투고 안내 문구 잔존) / A-022 갱신(오탐반려)
- **만든 테스트 데이터**:
  - `articles`: `[테스트-브론즈] R19`(브랜드 [테스트-브론즈] QA Brand, status pending — 지우지 않음, 승인/거절 확인창 미실행으로 그대로 대기 상태)
  - `products`: `[테스트-브론즈] R19 Product`(Furniture/Chairs & Stools, status pending — 지우지 않음)
  - `brands`: 기존 `[테스트-브론즈] QA Brand` 문서의 `tagline` 필드를 "[테스트-브론즈] R19 편집 확인"으로 변경(신규 문서 아님, 필드 갱신)
- **확인 못 함**: 브랜드 정보 편집이 공개 브랜드 상세 페이지에 승인 없이 반영되는지 육안 확인(A-024로 페이지 자체가 안 열려 불가 — Firestore 데이터 수준 반영은 확인함). A-026의 정확한 재현 조건(단일 탭 실사용자 환경에서도 나는지)
