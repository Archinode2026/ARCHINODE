# 브론즈 마라톤 진행상황 — 2026-09-15-B

병렬 라운드 표기: 「브론즈-B」. 다른 병렬 라운드(-A, -C)와 별개 파일.
범위: R5 공개 사이트 콘텐츠 전수 (로그인 불필요).

| R# | 시작 | 종료 | 통과/실패 | 새로 찾은 문제 | 만든 테스트 데이터 |
|---|---|---|---|---|---|
| R1 | 19:0x | 19:3x | 통과 다수(228개 표본 전수 대체로 정상) / 재발견 2건 | A-045(카테고리 서브페이지 `header` 중복선언) 69/69 전부 재현 확인(기존은 1개 파일만 실측) · A-019(sitemap.xml 매거진 30편 누락) 재발견 — 실 파일은 라이브에 존재(200), sitemap 목록에만 없음 | 없음(로그인 불필요 라운드, 쓰기 없음) |
| R2 | 20:0x | 20:4x | 통과 다수 / 신규 1건 · 재발견·범위확정 1건 | A-024(브랜드 상세 status==approved 필터, 오늘 커밋 9285a0c) 실동작 확인 — 승인 브랜드(oska)·테스트 브랜드(QA Brand) 정상 로드, 존재하지 않는 slug·거절된 slug 둘 다 "브랜드를 찾을 수 없습니다"로 조용히 처리, 콘솔 오류 없음(라이브가 최신 커밋 반영 확인) / products/view.html도 같은 패턴으로 정상(검토중 제품은 "제품을 찾을 수 없습니다") / A-047 표본을 10개(대분류별 1개)로 넓히고 이어서 카테고리 서브페이지 69개 **전수** 정적 스캔 — auth-compat 누락·로드순서 역전 둘 다 69/69로 범위 확정, "약 70개" 문구를 69개로 좁힘 / A-056 신규 — `auth/reset-password.html` firestore-compat 미탑재로 콘솔 오류(기능 영향은 없어 보임) | 없음(로그인 불필요 라운드, 쓰기 없음). 단, 브라우저에 실 어드민 계정(`wool21wool@gmail.com`) 세션이 이미 로그인돼 있는 걸 발견(내가 로그인한 게 아님) — `brand-portal/login.html`이 그 세션을 감지해 `admin/dashboard.html`로 자동 리디렉션시킴. 어드민 화면은 즉시 벗어났고 아무 버튼도 안 눌렀음(철칙 2) |

## R1 상세

**환경 이상 — 중요**: 라운드 시작 시 `tabs_create`가 "Browser pane 없음"으로 실패해 첫 `navigate`가 자동으로 만든 기본 탭(`seed`)을 썼는데, 잠시 후 그 탭이 다른 병렬 라운드에 의해 Admin Dashboard로 넘어가 있는 걸 발견 → 즉시 `tabs_create`로 `tab-2`를 새로 만들어 옮김. 그런데 이후에도 `tab-2`가 한 차례 더 "Admin Dashboard"·`auth/token-entry.html` 방문 이력을 보인 시점이 있었음(내가 navigate하지 않은 페이지) — 이 환경에서 병렬 라운드 간 탭 격리가 완전하지 않을 수 있음을 지휘 창에 보고 필요. **이 문제 때문에 `read_console_messages`·`read_network_requests`는 신뢰도가 낮다고 판단해 이번 라운드는 실브라우저 콘솔 실측 대신 동일 출처 `fetch()` 기반 정적 스캔(페이지 200개+ HTML을 직접 받아 header/footer 중복, data-en/ko 짝, 이미지 src 해석)으로 R5를 수행함. 콘솔 실측이 된 것은 `categories/bathroom/bathroom-accessories.html` 1건(header 중복선언 오류 직접 재현)뿐.

**스캔 범위**: sitemap.xml 180건 전체 목록 확보 → 카테고리 서브페이지 69개 전수(header/footer 개수, data-en/data-ko 균형, `header` 변수 중복선언 패턴) · 매거진 기사 15편 표본(헤더/푸터/이미지 경로) · 공개 페이지 10여개(index·about·brands·contact·digital-products·for-brands·list-your-brand·submit-trend·request-consultation·privacy·privacy-en·terms) · brands/view.html·products/view.html 정적 구조.

**발견 1 — A-045 범위 확정**: `const header =`(245줄대)와 `var header =`(283줄대)가 같은 페이지에 공존하는 패턴을 정규식으로 69개 카테고리 서브페이지 전부에서 확인(69/69, 100%). 기존 등록은 1개 파일만 실측한 상태였음. `bathroom-accessories.html` 실브라우저 로드로 콘솔 `Uncaught SyntaxError: Identifier 'header' has already been declared` 직접 재현 — 두 번째 `<script>` 블록 전체(검색 오버레이·모바일 메뉴 토글·스크롤 상단이동 등)가 파싱 자체가 안 되어 죽는 것으로 추정, 다른 68개도 같은 코드 패턴이라 같은 증상일 가능성 높음(전부 실브라우저 재현은 안 함). → `docs/검수대장.md` A-045 history에 반영.

**발견 2 — A-019 재발견**: 로컬 `magazine/*.html` 116개 vs 라이브 `sitemap.xml` 안의 매거진 항목 85개(라우터 `view.html` 제외 실 기사 84개) — 30편이 sitemap에 없음. 그 30편 중 5편을 실제로 열어보니 전부 라이브에서 200 정상(글 자체는 존재, sitemap 목록에서만 빠짐 — 검색엔진 노출 저하). → `docs/검수대장.md` A-019 history에 반영.

**정상 확인(문제 없음)**: 카테고리 서브페이지 69개 전부 header/footer 태그 개수 1개씩·data-en/data-ko 개수 완전 일치. 매거진 표본 15편 전부 header/footer 1개씩·data-en/ko 일치·이미지 경로(`../assets/magazine/placeholders/*.svg`) 전부 실제 200 해석됨. 공개 페이지 표본(index·about·brands 등 15개) 전부 데이터-언어 속성 균형, 이미지 깨짐 없음(단, brands.html·products/view.html·brands/view.html의 `${...}` 표시는 JS 렌더 전 템플릿 리터럴이라 정적 스캔의 오탐, 실결함 아님). `privacy-en.html` 모바일(375px) 헤더 — A-048과 동일 지점 재확인, 문제 없음(이미 09-15 다른 라운드가 확인완료 처리한 것과 일치).

**확인 못 함**: 로그인 반영(A-047) 관련 실측은 이번 라운드 범위 밖(로그인 불필요 라운드). 228페이지 전수 실브라우저 콘솔 검사는 탭 환경 문제로 대체(정적 스캔), 나머지 매거진 101편·표본 밖 공개 페이지는 미검사.

## 등록 (R1)
- A-045 history 추가(재발견, 범위 69/69로 확정) — 새 번호 안 만듦
- A-019 history 추가(재발견, 30편 누락 재확인) — 새 번호 안 만듦
- 신규 A-### 등록 없음(둘 다 기존 결함의 재발견/범위 확정)

## R2 상세

**0단계(약식)**: 커밋 `9285a0c`(brands/view.html status==approved 필터)가 라이브에 반영됐는지 `curl`로 직접 대조 — 로컬·라이브 둘 다 `.where('status', '==', 'approved')` 3곳 동일 문구로 존재, 라이브 최신 커밋(`80107ac`) 기준 일치 확인. 로그인은 이번 라운드 필요 없음(브리지 열쇠 안 씀). 단, 브라우저에 **실 어드민 세션이 이미 로그인된 채로** 남아있는 걸 발견(`auth/profile.html`을 열었더니 "Hello, wool21wool@gmail.com"으로 표시) — 내가 만든 세션이 아니고 다른 창(한울님 본인 또는 다른 병렬 라운드)이 남긴 것으로 추정. 철칙 2("실데이터 처리 금지")에 따라 이 세션으로 어떤 버튼도 누르지 않았고, `brand-portal/login.html`을 열었더니 로그인된 어드민 이메일을 감지해 자동으로 `admin/dashboard.html`로 리디렉션시키는 것도 확인했지만 그 화면에서 즉시 벗어나 아무 항목도 조회·클릭하지 않음. 지휘 창에 이 잔존 세션 존재를 보고 필요(다음 라운드가 같은 것에 걸릴 수 있음, 또는 한울님이 의도적으로 열어둔 것일 수 있음).

**우선 확인 — A-024(브랜드 상세 승인 필터)**: `brands.html`에서 실제 노출되는 승인 브랜드는 현재 2개뿐(`oska`, `[테스트-브론즈] QA Brand` — 둘 다 status approved, Firestore 직접 조회로 전수 확인: 총 5개 브랜드 문서 중 approved 2·rejected 3). 이 두 slug로 `brands/view.html?id=<slug>` 실측 — 둘 다 정상 로드, 콘솔 오류 0. 이어서 존재하지 않는 slug(`nonexistent-slug-zzz`)와 실제 rejected 상태인 slug(`테스트-5q2o68`) 두 가지로 "조용히 거절되는지" 확인 — **둘 다** "브랜드를 찾을 수 없습니다 / 요청하신 브랜드 페이지가 존재하지 않습니다" 화면으로 동일하게 처리되고 콘솔 오류 없음(승인 안 된 브랜드라고 다른 에러를 보여주지 않아, 미승인 브랜드 존재 여부가 방문자에게 새지 않음 — 의도된 동작으로 보임). **판정: 정상, 실동작 확인 완료.**

같은 방식으로 `products/view.html`도 확인 — 현재 approved 상태 제품이 하나도 없어(전수 조회: 2개 문서 다 pending) 승인된 제품으로는 못 열어봤지만, pending 상태 제품 id로 직접 접근하니 "제품을 찾을 수 없습니다 / 요청하신 제품이 존재하지 않거나 아직 검토 중입니다"로 정상 처리(콘솔 오류 없음). **승인된 제품이 생기면 정상 노출까지 재확인 필요 — 지금은 확인 못 함(데이터 없음).**

**trend-report.html·digital-products.html·auth 4페이지(login/signup/profile/reset-password)·brand-portal/login.html**: 전부 로드, 콘솔 오류 확인.
- trend-report·digital-products·auth/login·auth/signup·auth/profile: 콘솔 오류 없음, 화면 정상.
- **auth/reset-password.html: 콘솔 오류 재현(신규, A-056).** oobCode 없이 열면 firebase.firestore()가 없어서 나는 오류와 별개로 "Link Expired" 화면은 정상 표시 — 기능 자체는 살아있어 보이나 매번 콘솔 오류가 남는다. 새 탭으로 두 번 재현해 캐시·탭오염 아님을 확인.
- brand-portal/login.html: 위에 적은 대로 어드민 세션 자동 리디렉션 때문에 폼 자체 테스트는 못 함. 리디렉션 직전 순간 같은 firestore 오류 문구가 콘솔에 잠깐 보였으나 이 페이지는 firestore-compat을 정상 탑재하고 있어(auth/reset-password.html과 다름) 직전 페이지(reset-password.html)의 잔상일 가능성이 높다고 판단, 이 페이지 자체의 결함으로는 등록 안 함(확인 못 함으로 남김).

**A-047 표본 확대 + 전수 확정**: 대분류 10개에서 서브페이지 1개씩(`bathroom-accessories`·`building-automation`·`art-wall-decor`·`decorative-panels`·`beds-bedroom`·`countertops`·`architectural-lighting`·`acoustic-solutions`·`fire-pits-heaters`·`fitness-equipment`) 정적 소스 조회 — 10/10 전부 `firebase-auth-compat.js` 누락 + `auth-ui.js`가 `firebase-config.js`보다 먼저 로드. 이어서 sitemap.xml 기준 카테고리 서브페이지 **69개 전수**를 스크립트로 스캔 — 두 증상 모두 **69/69**(100%)로 확정. 기존 "약 70개"는 카테고리 서브페이지만 놓고 보면 69개로 좁혀짐.
부수 발견: 카테고리 **상위** 페이지 10개(`categories/furniture.html` 등, 서브폴더 파일이 아닌 것)는 `auth-ui.js`는 싣지만 Firebase 스크립트가 아예 하나도 없음(app-compat조차 없음) — 그래도 auth-ui.js 16줄의 `typeof auth === 'undefined'` 가드 덕에 콘솔 오류 없이 조용히 무동작. 증상(로그인 헤더 미반영)은 같지만 원인이 다르고 A-047이 지금 다루는 69개 범위 밖이라 새 번호는 안 만들고 A-047 이력에 참고만 남김. header/footer 중복선언(A-045) 패턴은 이 상위 10개 페이지엔 없음(1개씩 정상) — 확인 완료.

**매거진 전수 재확인(R1 15편 표본 → 116편 전수)**: 로컬 `magazine/*.html` 116개 전부를 header/footer 태그 개수·data-en/ko 균형·`</head>`/`<body>`/`</html>` 존재 여부로 스캔 — **116/116 문제 없음**. 이미지 src(로컬 상대경로, 쿼리스트링 붙은 JS 파일 제외) 96건 해석 — 전부 실제 파일 존재, 누락 0건(magazine/view.html의 템플릿 리터럴 오탐 6건은 R1과 동일한 이유로 제외).

**확인 못 함**: brand-portal/login.html의 비로그인 상태 폼 자체 테스트(어드민 세션 자동 리디렉션에 막힘) · 승인된 제품이 없어 products/view.html 정상 노출 케이스 실측 · A-047의 실브라우저(로그인 상태) 재현은 R16이 이미 2개 확인했고 이번엔 정적 스캔으로 범위만 넓힘(전 69개를 로그인 상태로 실브라우저 재현하진 않음).

## 등록 (R2)
- A-047 history 추가 — 표본 10개로 확대 재확인 + 69개 전수 확정(69/69), 상위 카테고리 10페이지 관련 관찰 추가. 새 번호 안 만듦.
- **A-056 신규 등록** — `auth/reset-password.html` firestore-compat SDK 미탑재로 `firebase.firestore is not a function` 콘솔 오류(기능 영향은 안 보임, Low).

