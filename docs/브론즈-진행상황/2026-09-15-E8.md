# 브론즈 마라톤 E8 (2026-09-15 마라톤, 2026-09-16 실행) — brand-guide.html 결함 등록 + 병렬 충돌로 조기 중단

이어지는 마라톤: E→E2→E3→E4→E5→E6→E7→E8. 같은 날 라벨(E계열)로 유지, 실제 실행은 09-16.

## 0단계
- 판 대조: about.html 로컬=라이브 바이트 diff 0 (커밋 136e7be 기준). 통과.
- 로그인: 브리지 열쇠 발급 시 Bash 도구의 백그라운드 출력이 실시간으로 안 잡혀(60초 만료 안에 못 읽음) 1차 시도는 만료로 실패. Monitor 도구로 스트리밍 방식 전환해 브리지 주소를 실시간으로 받아 60초 안에 열어 성공. `wool21wool@gmail.com`으로 어드민 로그인 확인.

## 1. brand-guide.html 반응형 결함 등록 — 완료, 단 번호 충돌 발견

E7에서 발견했던 brand-guide.html 가로 스크롤(322px·375px, scrollWidth 406 vs clientWidth) + 부제 줄바뀜 안 됨 + 고정 언어 버튼이 안내 띠 가림 결함을 검수관리 탭에 등록함. 등록 전 A-002·A-008·A-084(brand-guide.html 기존 항목)·A-059(for-brands.html 제목 겹침, docs 파일 기준)와 원문 대조해 중복 아님을 확인 후 등록.

**결과: 라이브 어드민 보드가 자동으로 "A-060"을 부여함.**

### ⚠️ 심각한 발견 — 라이브 검수관리 보드와 `docs/검수대장.md` 번호 체계가 어긋나 있음

새로 등록된 항목이 "A-060"을 받았는데, `docs/검수대장.md`에는 **이미 A-060이 존재**하며 완전히 다른 내용이다:

| | 라이브 어드민 보드 A-060 (방금 등록) | `docs/검수대장.md` 88행 기존 A-060 |
|---|---|---|
| 내용 | brand-guide.html 휴대폰 폭 가로 스크롤 + 언어버튼 겹침 | contact.html·for-brands.html 검색엔진용 구조화 데이터(JSON-LD) 문제 |
| 등록일 | 2026-09-16 (방금) | 2026-09-15 |

같은 대조를 A-058·A-059에도 해봤다: 라이브 보드의 A-058(공개 6화면 푸터 태그라인 영어 고정)·A-059(brand-guide.html 언어 드롭다운 안 눌림)는 E7이 실제로 등록한 내용과 정확히 일치하지만, `docs/검수대장.md`의 A-058·A-059는 **for-brands.html 헤더 버튼·제목 문제**로 완전히 다른 내용이다(별도 세션, 아마 "브론즈-B2" 트랙으로 추정 — 최근 커밋 로그에 `브론즈-B2 R91~R94`가 A-123~A-125를 등록 중인 것으로 보아, B2 트랙이 자체적으로 번호를 매겨 `docs/검수대장.md`에 직접 적어 넣고 있고, 이게 라이브 어드민 보드의 자동 카운터와는 별개로 진행되고 있음).

**즉 두 개의 독립된 번호 체계(라이브 어드민 보드 자동 카운터 vs `docs/검수대장.md`에 직접 적어 넣는 방식)가 같은 A-0XX 번호를 서로 다른 결함에 중복 배정하고 있다.** 이건 A-054("검수관리 게시판 — 새 항목 번호 매기기")가 이미 경고했던 바로 그 위험이 실제로 발생한 사례로 보인다. 브론즈는 관찰·등록만 하는 역할이라 번호 체계를 직접 정리하지 않았다. **지휘 창 확인 및 조정 필요** — 라이브 보드 A-058~A-060 3건과 `docs/검수대장.md` A-058~A-060 3건, 총 6건이 서로 다른 내용인데 번호만 겹친 상태다.

## 2. list-your-brand.html·404.html 반응형 확인 — 병렬 충돌로 착수 직후 중단

list-your-brand.html으로 이동 후 `resize_window(322×700)`를 3회 연속 시도(각 시도 사이 확인 포함) — 매번 "성공"으로 응답하지만 실제 페이지의 `clientWidth`·`innerWidth`는 계속 1425/1440으로 고정되어 전혀 줄어들지 않음(데스크톱 레이아웃 그대로 스크린샷에도 확인됨). 3번째 시도에서는 `outerWidth`가 157로 튀는 등 브라우저 창 프레임과 페이지 뷰포트 보고값이 서로 어긋나는 비정상 상태까지 관측됨 — E7이 겪은 "다른 세션이 resize_window를 동시에 건드림"과 같은 패턴.

지시서의 "충돌이 또 나면 그 즉시 멈추고 뭘 못했는지만 정확히 적어서 보고해라(2~3회면 충분)" 원칙에 따라 3회 확인 후 즉시 중단함. **list-your-brand.html·404.html의 322/375/768px 반응형 확인은 이번 라운드에서도 못 함 — 다음 라운드 과제로 재이월.**

## 다음 라운드 과제
1. **(지휘 창 확인 필요)** 라이브 검수관리 보드 vs `docs/검수대장.md`의 A-058·A-059·A-060 번호 충돌 정리 — 어느 쪽이 정본인지, 겹친 번호를 어떻게 재배정할지 결정 필요.
2. list-your-brand.html·404.html 322/375/768px 반응형 확인 (세 라운드 연속 이월).
3. 병렬 충돌(리사이즈뿐 아니라 다른 세션의 동시 등록)이 계속되면 매 라운드 짧게 끊고 즉시 보고하는 방식 유지.
